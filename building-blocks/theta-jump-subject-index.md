# The actual theta jump operator

The [Weil ground-state chapter](theta-weil-jump-form.md) defines the complete positive jump energies, keeps both pole evaluations, and proves the score projection that turns the full Weil comparison into a Poincare inequality with sharp target $1/2$. The radical identities provide infinitely many genuine equality modes. The lower inequality remains open.

The same chapter gives the actual prime return law, the closed Dirichlet form, the unconditional essential-spectrum bottom $1/2$, and an exact tail cancellation with a superexponentially small deficit. Its negative-index theorem realizes every finite nonreal-zero block by actual compact tests and counts distinct conjugate pairs, with multiplicity weighting a block rather than increasing its dimension. The even sharp gap alone is equivalent to RH.

Three specific failed comparisons are retained with their proofs: a bounded-congestion factorization path, pointwise curvature greater than $1/4$, and domination by fixed finite observations, even with the stated pole moments. These failures do not refute the sharp gap or constructions outside their specified classes.

The [semilocal scattering foundation](semilocal-weighted-scattering.md) identifies the theta form with an ordinary weighted trace on compact even mean-zero tests. Its [subject index](scattering-subject-index.md) follows the distinct local-factor, signed-phase and trace-norm constructions. The [arithmetic index](arithmetic-subject-index.md) follows exact integer probability laws, division identities and the formal $W$ consumer.

All theta jump and spectral results indexed here are written analysis. Advanced domain, smoothing and zero-mode expansion results are being collected separately; this index does not assert that the entire historical corpus has been integrated.
