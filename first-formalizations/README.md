# First formalizations

First formalizations of mathematics in proof assistants, with attribution to the original authors, Lean 4 verification records, and a documented comparison across formal libraries.

1. [Literal physical theta kernel: parity, positivity, and moments](literal-physical-theta-kernel.md). Formalization of Jacobi's literal differential kernel $\Phi(u)$, its exact parity, positivity, and exponential moments in `PhysicalThetaParity.lean` and `PhysicalThetaMoments.lean`.
2. [Actual critical sign criterion: Mellin continuation and pole obstruction](actual-critical-sign-criterion.md). Complete formalization in `ActualCriticalSignCriterion.lean` and `CriticalTransformContinuationRH.lean` proving that eventual arithmetic nonpositivity $W(x) \le 0$ implies the Riemann Hypothesis via double-pole clearing and identity theorem continuation.
3. [Suzuki pole normalizer and Goldbach variation](suzuki-pole-normalizer-variation.md). Formalization in `SuzukiCoefficientVariation.lean`, `SuzukiGoldbachVariation.lean`, and `SuzukiPoleNormalizer.lean` of Suzuki's $\omega$-variation, the pole-normalized residue $2\omega/U(1+2\omega)$, and the second variation connecting it to centered additive Goldbach pairs.
4. [Complete actual causal ramp prime weights](actual-ramp-prime-weights.md). Formalization in `ActualRampPrimeWeights.lean` of the exact arithmetic corner rules, piecewise support plateau/slope identities, and monotonicity for actual-prime causal ramp integrals.
5. [Critical chirped Gram matrix finite algebra](critical-chirped-gram-finite.md). Formalization in `CriticalChirpedGramFinite.lean` and `CriticalChirpedEndpointFinite.lean` of the finite Hermitian row-bound and diagonal dominance algebra for chirped Weil packets.
6. [Finite hyperbola divisor transfer and cofactor decomposition](actual-prime-cofactor-future-finite.md). Formalization in `ActualPrimeCofactorFutureFinite.lean` of the exact finite hyperbola identity $\Lambda * 1 = \log$ and hard-cutoff cofactor decomposition for arbitrary complex weights.
7. [Actual completed zeta xi and Archimedean factor](actual-archimedean-factor-and-xi.md). Formalization in `ActualArchimedeanFactor.lean` of the entire function $\xi(s)$, the Archimedean logarithmic derivative, and the local zero multiplicity residue limit $(w-s)\xi'(w)/\xi(w) \to m$.
8. [Exact integer-endpoint correction for Möbius hyperbola sum](hyperbola-endpoint-moebius-correction.md). Formalization in `HyperbolaEndpoint.lean` of the exact integer-endpoint count $2\sum_{d \mid N} \mu(d) c(N/d) = 1 + (-1)^{\Omega(N)} \in \{0, 2\}$ and its double-sum jump identity.
9. [Möbius–Stokes boundary decomposition and complete renewal endpoint](mobius-stokes-finite-boundary.md). Formalization in `MobiusStokesFiniteBoundary.lean` of the exact Möbius boundary split $B_{D, N} = F_p(\max(D, N/p), N) - F_p(D/p, \min(D, N/p))$ and complete divisor renewal $\sum_{d=1}^N \mu(d)\lfloor N/d\rfloor = 1$.
10. [Simultaneous-admission endpoint geometric identity and packet bounds](simultaneous-admission-endpoint-finite.md). Formalization in `SimultaneousAdmissionEndpointFinite.lean` of the exact geometric identity $E_d(q) = q^d - q^{2d} \le 1/4$, finite signed/Möbius packet bounds, and squarefree prime product divisor bound $\|\sum_{d \mid \prod Q} \mu(d) v_d\|^2 \le 4^{|Q|-1}$.
11. [Cross-horizon Möbius clock: reversal and step increments](cross-horizon-mobius-clock.md). Formalization in `CrossHorizonMobiusClockFinite.lean` of the finite prefix reversal $(1-q)\sum q^i \sum \mu = \sum \mu(1-q^{\lfloor N/d\rfloor})$, step difference $G_N(q) - G_{N-1}(q) = (1-q)\sum_{d \mid N} \mu(d) q^{N/d-1}$, and energy coboundary.
12. [Growing-prime scattering defect factorization and block comparison](growing-prime-scattering-bounds.md). Formalization in `GrowingPrimeScatteringBounds.lean` of paired scattering multiplier bounds $|a_p(z)| \le 3$, defect factorization $a_p(z) = (1-d_p(z))(1-d_p(-z))$, exponential product defect bounds, and low-high frequency splitting.
13. [Odd-centered Goldbach pairs and symmetric weight reflection](goldbach-weighted-odd-pairs.md). Formalization in `GoldbachWeightedOddPairs.lean`, `GoldbachOddPairs.lean`, and `GoldbachOddCentering.lean` of the exact weighted reflection expansion $\sum o(a)o(N-a)w(a) = \sum \Lambda(a)\Lambda(N-a)w(a) - 4\sum\Lambda(a)w(a) + 4\sum w(a)$ and even-support power-of-2 rigidity.
14. [Largest-prime squarefree packet decomposition and admission flux](largest-prime-squarefree-packets.md). Formalization in `LargestPrimeSquarefreePackets.lean`, `PrimeAdmissionEnergyFlux.lean`, and `PrimeAdmissionX4Obstruction.lean` of the fiber partition $\operatorname{SqFree}(D, T) = \bigsqcup_p p \cdot Q_p(D, T)$, packet reconstruction $\sum \mu(d)v_d = -\sum_p \sum_q \mu(q) v_{pq}$, and positive admission fluxes.
15. [Arithmetic frozen-source Plancherel identity and layercake log-moment](charge-frozen-plancherel.md). Formalization in `ChargeFrozenPlancherel.lean`, `AngularFourierConvolution.lean`, and `LogarithmicLayercake.lean` of the exact real-line Plancherel identity $H_N = 2\pi\int a_N^2$, prefix/terminal decomposition $H_N = 2\pi[\int_{(0,\log N]} a_N^2 + |\psi(N)-N|^2/N]$, and logarithmic layercake moment bound.
16. [Factorial quotient constraints, triangular uniqueness, and finite renewal](factorial-quotient-constraints-and-renewal.md). Formalization in `FactorialQuotientConstraints.lean`, `FactorialFiniteRenewal.lean`, and `FactorialBinaryCarry.lean` of the quotient constraints $\sum_{q \in S_N} A_q \lfloor q/k \rfloor = 1$, triangular uniqueness, and finite renewal $\sum_{k=1}^N g_{\lfloor N/k \rfloor}(t) = 1 - e^{-Nt}$.
17. [Actual local zero-circle residues and compact Mellin analyticity](actual-zero-circle-residues.md). Formalization in `ActualZeroCircleResidues.lean`, `CompactPhysicalMellinAnalytic.lean`, and `ActualPhysicalResidues.lean` of the exact circle residues $\oint_{|w-s|=R} F(w) \frac{f'(w)}{f(w)} dw = 2\pi i m F(s)$, entire Mellin analyticity, and prime scattering history comparisons.
18. [LCM difference product divisibility for finite families](lcm-difference-product-divisibility.md). Formalization in `LcmDifferenceProduct.lean` of the unconditional integer divisibility $\prod_{i=1}^k n_i \mid \operatorname{lcm}(n_1, \ldots, n_k) \cdot \prod_{1 \le i < j \le k} |n_j - n_i|$ for arbitrary finite families of natural numbers.
19. [Exact actual heat density pairing and prime source Gram evaluation](actual-heat-density-pairing.md). Formalization in `ActualFiniteHeatSource.lean`, `AdditiveHeatPairing.lean`, and `ActualHeatDensityPairing.lean` of the exact $L^2$ energy of the actual finite prime heat source $\int_0^\infty \operatorname{source}(N, t)^2 dt = \sum_{i, j \le N} \frac{c_i c_j}{x_i + x_j} - 2\sum_{i \le N} c_i \log(\frac{x_i+N}{x_i+1}) + [2N\log(2N) - 2(N+1)\log(N+1) + 2\log 2]$ and mixed heat-density integral $\int_0^\infty e^{-xt}\operatorname{density}(N, t)dt = \log(\frac{x+N}{x+1})$.
20. [Compact actual prime-error heat source, terminal charge cancellation, and Laplace identity](actual-compact-heat-endpoint.md). Formalization in `ActualCompactHeatEndpoint.lean` and `FinitePrimeSourceIntegration.lean` of the exact Laplace integral representation $\operatorname{compactHeat}(N, t) = t \int_1^N (\psi(x) - x) e^{-xt} dx$, zero net charge $\operatorname{compactHeat}(N, 0) = 0$, and exact decomposition into Goldbach heat packets and terminal error charge.
21. [Non-asymptotic divisor sums on square-root denominators and Farey boundary mass bounds](cyclotomic-boundary-divisor-sums.md). Formalization in `CyclotomicBoundarySums.lean` and `CyclotomicDivisorPairs.lean` of the exact non-asymptotic divisor sum $\sum_{n=1}^X \frac{\tau(n)}{\sqrt{n}} \le 2\sqrt{X} H_X$, inductive square-root sum $\sum_{n=1}^X \frac{1}{\sqrt{n}} \le 2\sqrt{X}$, and explicit Farey boundary mass cost bounds $4\sqrt{X} H_X^2 \log X$.
22. [Centered Goldbach heat scale increment and even packet compensation](centered-goldbach-heat-scale-increment.md). Formalization in `CenteredGoldbachEvenCompensationFinite.lean`, `CenteredGoldbachFiniteAlgebra.lean`, `CenteredGoldbachAdjacentTotalsFinite.lean`, and `CenteredGoldbachHeatScaleIncrementFinite.lean` of the finite shell nonnegativity forcing the even Goldbach packet to compensate the odd interior deficit $\operatorname{oddInteriorDeficit} - \operatorname{oddTransitionPacket} \le \operatorname{evenScaleIncrement}$, arithmetic lower bound for even prime pairs, and the two-terminal finite Abel second-difference identity.
23. [Actual prime block scattering multipliers and exact Weil paired-weight parity](actual-prime-block-weil-weight-parity.md). Formalization in `ActualScatteringMomentSigns.lean`, `ActualScatteringMellinSignBridge.lean`, `ActualPrimeBlockMellinParity.lean`, and `ActualPrimeBlockWeilWeightParity.lean` of the opposing multiplier signs, prime defect negativity $A(p) < 0$, composite prime-block conjugate Mellin parity $(-1)^{|ps|} \operatorname{amplitude}(ps, h)$, and exact paired Weil weight sign inversion $(-1)^{|ps|} \operatorname{Re}[\operatorname{pairedWeight}] \ge 0$.
24. [Two-boundary cyclotomic Farey mass bounds and controlled interaction reduction](two-boundary-cyclotomic-farey-reduction.md). Formalization in `CyclotomicBoundaryBridge.lean`, `CyclotomicFareyBounds.lean`, and `CyclotomicFareyPartition.lean` of the early conductor mass bound $2Y/N$, recent conductor mass bound $2H_N/\sqrt{N}$, and the two-boundary interaction reduction $|\operatorname{controlledInteraction}(X, \lfloor\sqrt{X}\rfloor)| \le 2\sqrt{X}\log X H_X(1 + 3H_X)$.
25. [Zero-charge prime packet pair-kernel expansion and far-lag dipole decay](zero-charge-prime-packet-pair-expansion.md). Formalization in `ActualTerminalPrimeTraceFinite.lean` of the zero-charge pair kernel expansion $\sum \frac{a_i a_j}{k x_j + x_i} = -\frac{1}{k^2}(\sum a_i x_i)(\sum a_j / x_j^2) + O(k^{-3})$, vanishing of the $O(k^{-1})$ monopole, and the terminal prime-power error compensation $\sum_{n=1}^{N+1} c_n = 0$.
26. [Finite-dimensional Hermitian Gram inversion and off-critical zero-pair linear deficit](hermitian-gram-inversion-and-zero-pair-deficit.md). Formalization in `ActualWeilZeroPairAlgebra.lean` and `ActualAnnulusGramAlgebra.lean` of the reciprocal bump identity $(1-z)(1-z^{-1}) = 2-z-z^{-1}$, reflected zero symmetry, the off-critical linear deficit $2\operatorname{Re}[W(2-ur-\bar{u}/r)] \le 4\|W\| + 2\|W\|/r - 2br$, and two-bump Hermitian Gram diagonal dominance $(d-\|p\|)(|a|^2+|b|^2) \le d(|a|^2+|b|^2) - 2\operatorname{Re}(b\bar{a}p)$.
27. [Exact finite Abel summation for Möbius generating functions and nonnegative centered Poisson kernels](finite-abel-mertens-and-centered-poisson.md). Formalization in `ActualFiniteAbelMertens.lean` and `ActualCenteredPoissonKernel.lean` of the terminal-retained Abel identity $\sum_{n=1}^N \mu(n)q^n = M(N)q^N + (1-q)\sum_{k=0}^{N-1} M(k)q^k$, finite termwise derivative $E_N'(t) = -\sum_{n=1}^N \mu(n)ne^{-nt}$, derivative bound $|E_N'(t)| \le \sum_{n=1}^N ne^{-nt}$, and positive geometric tail factoring $q^{\max(n, m)} - q^{n+m} = (1-q)\sum_{k=0}^{\min(n, m)-1} q^{\max(n, m)+k} \ge 0$.
28. [Finite sinc form decomposition, prime birth norm, and diagonal comparison](finite-sinc-prime-diagonal-decomposition.md). Formalization in `ActualFiniteSincPrimeDiagonal.lean` of the normalized symmetric arc sinc kernel $K(\delta, k) = \frac{\sin(k\delta)}{\pi k}$, complete finite sinc form, prime diagonal extraction, literal prime birth norm $q(p) = K(2p) - 2K(p+1) + K(2)$, unconditional non-negativity of the prime diagonal under positive birth norms, and the finite comparison bounding prime diagonal by the complete birth diagonal before any Abel limit.
29. [Scattering multiplier magnitude and actual prime block amplitude bounds](scattering-multiplier-magnitude-and-block-amplitudes.md). Formalization in `ActualScatteringMomentMagnitude.lean` and `ActualPrimeBlockMagnitude.lean` of the two-sided geometric bounds $(p^h)^M/8 \le -(W^+ W^-) \le 2(p^h)^M$ for scattering multipliers under prime valuations, and list induction proving exponential-product bounds $(1/8)^{|ps|} \prod p^{h \cdot d(p)} \le \operatorname{amplitude}(ps, d, h) \le 2^{|ps|} \prod p^{h \cdot d(p)}$.
30. [Pairwise metric variance identity and separated carry window lower bounds](pairwise-metric-variance-and-carry-window-bounds.md). Formalization in `CarryWindowVariance.lean` of the universal algebraic variance identity $\sum (f(i)-f(j))^2 = 2n\sum f(i)^2 - 2(\sum f(i))^2$, exact pairwise index quadratic form evaluation $\sum (i-j)^2 = \frac{n^2(n^2-1)}{6}$, and the universal cubic lower bound $\sum_{i=0}^{n-1} (f(i)-c)^2 \ge \frac{L^2 n(n^2-1)}{12}$ for $L$-separated sequences around an arbitrary center.
31. [Exact Fourier–Mellin representation of the causal prime source](fourier-mellin-representation-causal-prime-source.md). Formalization in `ChargeFrozenFourier.lean` of the exact Fourier–Mellin identity connecting the time-domain causal prime source $a_N(v) = \sum_{n=2}^N \Lambda(n) e^{-v/2}\mathbf{1}_{v \ge \log n} - e^{-v/2}\min(e^v, N)\mathbf{1}_{v \ge 0}$ to the truncated critical explicit formula $\int_{\mathbb{R}} e^{-i\xi v} a_N(v) dv = \frac{-1 + \sum_{n=2}^N \Lambda(n) n^{-w(\xi)} - \int_1^N x^{-w(\xi)} dx}{w(\xi)}$ with $w(\xi) = 1/2 + i\xi$.
32. [Logarithmic-mean renewal cell increment factorization and positivity](logarithmic-mean-renewal-cell-increment.md). Formalization in `CausalRenewalFiniteCells.lean` of the logarithmic-mean cell increment factorization $\operatorname{cellIncrement}(A, a, b) = 2(\log b - \log a)(A - 2L(a, b))$, the cubic ratio gap $\sqrt{n+1}^3 < 3\sqrt{n}^3$, exact increment positivity under signed Euler bounds, and finite local damping/centered delay identity.
33. [Causal relaxation filter energy dissipation and critical contraction](causal-relaxation-filter-energy-dissipation.md). Formalization in `CausalFilterEnergy.lean` of the exact energy balance for the causal filter $r' = f - \gamma r$, dissipative identity $\int_0^T (f-r)^2 = \int_0^T f^2 - r(T)^2 - (2\gamma-1)\int_0^T r^2$, critical contraction $\int_0^T (f-r)^2 \le \int_0^T f^2$ for $\gamma \ge 1/2$, and universal gain bound $\gamma^2 \int_0^T r^2 \le \int_0^T f^2$.
34. [Inner product space admission increment cancellation and error localization](inner-product-admission-increment-cancellation.md). Formalization in `AdmissionIncrementCancellationFinite.lean` of the model admission identity $\|F-U\|^2 - \|F\|^2 = \|U\|^2 - 2\langle F, U\rangle$, exact increment polarization error identity for row updates, universal norm bound showing the unadmitted remainder interacts solely through the local face $U$ and admitted piece $V$ without bare $2\|R\|$ error, bilinear correction, and Boolean-face reflection pairing.
35. [Centered complete-admission consumer and finite Goldbach mass positivity](centered-complete-admission-consumer.md). Formalization in `CenteredConsumerStructuralFinite.lean` of the clock-scaled centered successor recursion $q^{n+1}P_{n+1}(q) = q(q^n P_n(q)) + \mu(n+1)q^{2(n+1)}$, dyadic/horizon prefix energy splitting, complete multiplicative-history convolution $(\mu * \log)(n) = \Lambda(n)$, finite bilinear double-Möbius sum $P_N(q)^2 = \sum \mu(a)\mu(b)q^{a+b}$, complete finite Goldbach box mass identity $G(N) = (\sum \Lambda(n))^2 \ge 0$, and history observability consumer.
36. [Two-exponential-rate Weil Gram factorization and packet derivative positivity](two-rate-weil-gram-algebra.md). Formalization in `TwoRateWeilGramAlgebra.lean` of the exact algebraic factorization $AB(a-b)^2 - ab(A-B)^2 = (bA-aB)(bB-aA)$, strict positive definiteness from opposite ratio bounds, and the rational zero-packet derivative identities proving $\frac{d}{da}(ag(a)) > 0$ and $-\frac{d}{da}(g(a)/a) > 0$ for every conjugate/reflected zero packet satisfying $q^2 \le 2p^2$.
37. [Three-exponential-rate Weil Gram determinant bilinear factorization](three-rate-weil-gram-algebra.md). Formalization in `ThreeRateWeilGramAlgebra.lean` of the denominator-cleared degree-8 polynomial factorization $\operatorname{gramDetNumerator}(a, b, c, A, B, C) = H_{\mathrm{num}} \cdot J_{\mathrm{num}}$ into the product of two second-divided-difference numerators, the exact rational formula for the $3 \times 3$ Gram determinant, and strict positivity from $H_{\mathrm{num}} > 0$ and $J_{\mathrm{num}} > 0$.
38. [Prime seed compound Poisson process and time-addition convolution semigroup](prime-seed-compound-poisson-semigroup.md). Formalization in `PrimeSeedCompoundPoisson.lean` and `PrimeSeedSemigroup.lean` of $n$-jump convolution powers `jumpPower μ n`, Poisson mixture laws, normalized jump reconstruction, antidiagonal summation, and the exact convolution semigroup identity $\operatorname{seedProcess}(S, u) * \operatorname{seedProcess}(S, v) = \operatorname{seedProcess}(S, u+v)$.
39. [Uniform Bernstein Laplace exponent bound on the closed half-plane for the all-prime seed](prime-seed-laplace-exponent-uniform-bound.md). Formalization in `PrimeSeedLaplaceExponent.lean` of the Laplace kernel bound $\|1 - e^{-sv}\| \le 2$ on $\operatorname{Re}(s) \ge 0$, non-negativity of the real part $\operatorname{Re}(\Phi(s)) \ge 0$, exact tail identification, and the uniform right half-plane bound $\|\Phi(s) - \Phi_P(s)\| \le \frac{64}{\log P}$ for all $P \ge 8$.
40. [Renewal Bernoulli cell moments and double-primitive deficit](renewal-bernoulli-cell-moments-and-double-primitive.md). Formalization in `RenewalBernoulliCellMoments.lean` of the vanishing moments $\int_0^1 f_0 = 0$, $\int_0^1 r f_0 = 0$, $\int_0^1 f_1 = 0$, surviving moments $\int_0^1 r^2 f_0 = -1/360$, $\int_0^1 r f_1 = 1/144$, $\int_0^1 f_2 = -7/576$, surviving phase identity evaluating to $-\frac{(2z+7)(2z+9)}{2880}$, and nonpositive/strictly negative double primitives $\int_0^r (r-t)f_0(t)dt = -\frac{r^2(1-r)^2}{24} \le 0$ and $\int_0^r (r-t)f_1(t)dt = -\frac{r^3[1+3r(1-r)]}{144} < 0$ on $(0, 1]$.
41. [Kappa coefficient positivity and unit total probability mass](kappa-coefficient-positivity-and-unit-mass.md). Formalization in `KappaCoefficientPositivity.lean` of the continuous interpolation $\tau(x) = x(\log(x+1)-\log x)$, strict derivative positivity $\tau'(x) > 0$, discrete difference $\kappa_m = \tau(m+1) - \tau(m)$, coefficient positivity $\kappa_m > 0$ for all $m$, base value $\kappa_0 = \log 2$, partial sum formula $\sum_{m=0}^N \kappa_m = (N+1)\log\frac{N+2}{N+1}$, and total unit mass $\sum_{m=0}^\infty \kappa_m = 1$ (`HasSum kappa 1`).
42. [Full-kappa slope finite resummation and rectangular mass](full-kappa-slope-finite-resummation.md). Formalization in `FullKappaSlopeFinite.lean` of the discrete slope difference $\kappa_m = g_m - g_{m+1}$ with $g_m = m\log\frac{m}{m+1}$, closed factorial prefix identity $H(n) = \log(n!/(n+1)^n)$, rectangular prefix identity $K(A, B) = \operatorname{prefixG} A + \operatorname{prefixG} B - \operatorname{prefixG}(A+B)$, exact boundary identity $K(A, B) = H(\min(A, B) - 1) + H(\max(A, B) - 1) - H(A + B - 1)$, and strict positivity $K(A, B) > 0$ with coordinatewise monotonicity.
43. [Integer carry phase cocycle and analytic primitive](integer-carry-phase-cocycle-and-analytic-primitive.md). Formalization in `IntegerCarryPhase.lean` and `IntegerCarryPrimitiveAnalytic.lean` of the integer carry phase $\operatorname{phase}(r, x) = r B_1(x) - B_1(rx)$, unit primitive bounds $-1/8 \le u(x) \le 0$, multiplicative cocycles `phase_mul_cocycle` and `primitiveFormula_mul_cocycle`, exact floor carry identity $\operatorname{phase}(m, x) = \lfloor m\{x\}\rfloor - (m-1)/2$, zero integer values $P(m, N) = 0$, $C^0$ continuity, almost-everywhere derivative, and uniform error $0 \le -u(x) - (-P(r, x))/r \le \frac{1}{8r^2}$.
44. [Integer carry subcell nonpositivity and integration by parts](integer-carry-subcell-nonpositivity-and-integration-by-parts.md). Formalization in `IntegerCarryPrimitiveBounds.lean`, `IntegerCarryPrimitiveIntegral.lean`, and `IntegerCarryIntegrationByParts.lean` of the quadratic subcell product bound $s(1-s) \le (j+s)(m-j-s)$, machine-verified unconditional nonpositivity $-m/8 \le P(m, x) \le 0$ on the entire real line $\mathbb{R}$, real counterexample $P(3/2, 1) = 1/12 > 0$, definite integral nonpositivity $-m/8 \le \int_1^x \operatorname{phase}(m, y) dy \le 0$, and exact integration by parts with vanishing origin defect $P(m, 1) = 0$.
45. [Dyadic factorial deficit recurrence and uniform margin](dyadic-factorial-deficit-recurrence-and-uniform-margin.md). Formalization in `DivisionDyadicFactorial.lean` of the exact deficit recurrence $A_{n+1} - A_n = H_n + 1 - \log(4n+2)$ for $A_n = n H_n - \log((2n)!/n!)$, Euler–Mascheroni half-shift bound $H_n > 1/2 + \log(n+1/2)$, strict increment lower bound $A_{n+1}-A_n > 3/2 - \log 4$, linear growth $A_n > n(3/2 - \log 4)$, and uniform negative normalized deficit $\frac{1}{n}\log\frac{(2n)!}{n!} - H_n < \log 4 - 3/2 < 0$.
46. [Dyadic divisor prime discrepancy and factorial identification](dyadic-divisor-prime-discrepancy-and-factorial-identification.md). Formalization in `DivisionDyadicPrimeAverage.lean` of the vanishing upper-cutoff extension $\sum_{d \le N}\psi(2N/d) = \log((2N)!)$, exact discrepancy-deficit duality $D_N = \sum_{d=1}^N (\psi(\lfloor 2N/d\rfloor) - \psi(\lfloor N/d\rfloor) - N/d) = \log((2N)!/N!) - N H_N = -A_N$, unconditional strict negativity $D_N < -N(3/2 - \log 4) < 0$ for all $N \ge 1$, and normalized bound $D_N/N < \log 4 - 3/2 < 0$.
47. [Finite arithmetic Weil prime margin and exhaustive even prime power classification](finite-weil-norm-preserving-counterexample-and-even-power-classification.md). Formalization in `FiniteWeilPrimeCertificate.lean` and `FiniteWeilEvenPowerCertificate.lean` of the certified 24-prime window bounds at $p=10007$, exact positive rational margin $\frac{24 \cdot 13}{1632} - \frac{14}{400} - \frac{49}{344} = \frac{251}{18275} > 0$, global base and exponent bounds $q < 116$ and $k < 14$, and the unconstrained bidirectional classification $(3 \cdot 10007 \le 4 q^k \wedge 3 q^k \le 4 \cdot 10007) \iff (k = 2 \wedge q \in \{89, 97, 101, 103, 107, 109, 113\})$ refuting universal normalized finite-history monotonicity.
48. [Radius-two edge cell geometry and reversal reduction](radius-two-edge-cell-geometry-and-reversal-reduction.md). Formalization in `RadiusTwoEdgeCellGeometry.lean` and `RadiusTwoEdgeReversalFinite.lean` of window emptiness at stages $M=2, 3$ from $2/3 < \log 2$, exhaustive 16-cell classification among 48 labels at $(M, L)=(4, 0)$, explicit Bochner interval integrals, 6 exact reversal relations reducing rank from 16 to at most 10, and spatial reflection reduction to 5 independent coordinates with bidirectional reconstruction isomorphisms.
49. [Uncut edge parity obstruction and symmetric gradient bound](uncut-edge-parity-obstruction-and-gradient-bound.md). Formalization in `UncutEdgeParityFinite.lean` of the universal factor-4 symmetric edge gradient bound $a \sum w(e) \|F(\mathrm{dst})-F(\mathrm{src})\|^2 \le 4 \cdot \mathrm{vertexEnergy}$, algebraic exclusion of odd-reflection targets from the 5-dimensional even-hat Galerkin span, and monotone convergence detection of positive canonical stage.
50. [Radius-two Rayleigh quotient gap and certified spectral deficit](radius-two-rayleigh-quotient-gap-and-spectral-deficit.md). Formalization in `ThetaRadiusTwoRayleighCore.lean` and `ThetaRadiusTwoJ3RationalCore.lean` of the exact piecewise quintic convolution polynomial $H(x)$, Bochner interval integration on both branches, the exact rational payment $2(6/25 - 6859/25600) = -143/2560$, the Rayleigh deficit $E/N < 1/2 - 143/(5120 J(3))$, Machin/Taylor rational certificates for $\pi$ and $e$, and propagation of the upper enclosure $J(3) < 10^{544}$ to the uniform spectral gap $\text{gap} > 10^{-546}$.
51. [Moore–Penrose pseudoinverse contraction and closed-form Young optimizers](pseudoinverse-contraction-and-closed-form-young-optimizers.md). Formalization in `SignedCoreGramContraction.lean` of the complex mean polarization identity, the singular Schur pseudoinverse contraction equivalence $(\forall x, \|Wx\|^2 \le \|A^{1/2}x\|^2) \iff (\forall y \in \operatorname{range}(A^{1/2}), \|W A^{\dagger/2}y\| \le \|y\|)$, closed-form algebraic expressions for the optimal 2-block Young parameters `youngEpsilon` and `youngConstant`, margin balancing $h - \varepsilon = c$ and $g - C^2/\varepsilon = c$, and the solvability criterion $0 \le c \iff C^2 \le h g$.
52. [Complete hard-clock weighted LCM arithmetic class and off-diagonal deficit](hard-clock-weighted-lcm-class-and-off-diagonal-deficit.md). Formalization in `HardClockWeightedLcmClass.lean` of the exact prime-local evaluation $W(p) = p(p-2)$, the squarefree prime induction $W(\prod_{p \in P} p) = \prod_{p \in P} p(p-2)$, local factor nonnegativity and square domination $0 \le p(p-2) \le p^2$, and the unconditional nonpositivity of the off-diagonal defect $W(k) - k^2 \le 0$.
53. [Continuous-to-discrete renewal transfer via exponential hats](continuous-to-discrete-renewal-exponential-hat-transfer.md). Formalization in `IntegerBirthExponentialHats.lean` of the exact affine exponential hat integral on $[0, 1]$ (`integral_exp_affine`), the hyperbolic sinc multiplier identity $\operatorname{leftHat}(t) + e^t \operatorname{rightHat}(t) = \frac{4\sinh^2(t/2)}{t^2}$ (`hat_factor`), the integer cell decomposition of $[0, \infty)$, the single-birth collapsing identity $\int_0^\infty e^{-tu} \widetilde{c}(u) du = \frac{4\sinh^2(t/2)}{t^2} \sum_{j=0}^\infty e^{-tj} c(j)$ (`actual_birth_hat_identity`), and the exact complete arithmetic defect formula (`actual_all_birth_defect_identity`).
54. [Non-local killing positivity and prime jump boundary gap](non-local-killing-positivity-and-prime-jump-boundary-gap.md). Formalization in `PhysicalKillingPositivity.lean` of the geometric crossing interval $(-R-a, -R-a+\min(R, a)/2)$ (`crossing_interval`), the strictly positive jump edge mass $\operatorname{edgeMass}(k, a) > 0$ for the killed extension of any non-zero constant core $c \ne 0$ (`constant_killed_edge_positive`), and the strictly positive killed Dirichlet energy floor $\operatorname{killedEnergy}(R, c) \ge \frac{\log 2}{\sqrt{2}} \operatorname{edgeMass}(k, \log 2) > 0$ forced by the first prime $p = 2$ (`constant_killed_energy_positive`).
55. [Degenerate Schur complement cancellation and Woodbury reduction for theta edges](degenerate-schur-cancellation-and-woodbury-reduction-for-theta-edges.md). Formalization in `ThetaDirectEdgeFiniteAlgebra.lean` and `ThetaSignedCoreFiniteBlock.lean` of the 1-dimensional Schur residual identity (`schur_residual_identity`), degenerate Cauchy-Schwarz numerator cancellation $d=0 \implies b=0$ forcing $m=0$ when $d_y=0$ (`psd_mixed_eq_zero_of_diagonal_eq_zero`, `degenerate_residualMixed_eq_zero`), the 3-column Woodbury quadratic expansion $c^T G c$ for $c=(1, -r^T)^T$ (`woodbury_quadratic_expansion`), and the 5-component signed core decomposition with coercive lower bound certificate $c^* > 0$ (`signedCore_quadratic_decomposition`, `cStar_pos_implies_finiteBlock_coercive`).
56. [Causal prime birth energy increment and discrepancy work](causal-prime-birth-energy-increment-and-discrepancy-work.md). Formalization in `ChargeFrozenBirthWork.lean` and `ChargeFrozenBirthMetric.lean` of the piecewise birth increment dissection (`increment_piecewise`), closed ramp energy $\int_{\log N}^{\log(N+1)} (e^v-N)^2 e^{-v} dv = 1 + rac{N}{N+1} - 2N\log(1+1/N)$ (`ramp_square_integral`), total increment norm (`increment_square_integral_evaluated`), cross-work discrepancy identity $\int_0^\infty a_N \delta_N = (\psi(N)-N)(rac{\Lambda(N+1)}{N+1} - \log(1+1/N))$ (`source_increment_integral`), and complete step energy increment law (`full_square_integral_increment`).
57. [Arithmetic birth Gram kernel and universal positive semidefiniteness](arithmetic-birth-gram-kernel-and-universal-semidefiniteness.md). Formalization in `ChargeFrozenBirthGram.lean` and `ChargeFrozenBirthFiniteGram.lean` of the off-diagonal rank-1 factorization $\int_0^\infty \delta_m \delta_N = (\Lambda(m+1)-1)(rac{\Lambda(N+1)}{N+1} - \log(1+1/N))$ (`birth_gram_integral`), identification with the discrete kernel $K(m, n)$ (`birth_gram_eq_kernel`), double-sum finite energy representation (`finite_energy_eq_kernel`), and universal positive semidefiniteness $\sum_{m, n \in s} c_m c_n K(m, n) \ge 0$ on every finite index set (`finite_kernel_quadratic_nonnegative`).
58. [Renewal cell trapezoid identity and finite rectangle bounds](renewal-cell-trapezoid-identity-and-finite-rectangle-bounds.md). Formalization in `ActualHeatRenewalRectangle.lean` of the finite right-endpoint rectangle error bound (`finite_right_rectangle`, `finite_right_rectangle_div`), exact cell trapezoid Peano kernel identity $rac{b-a}{2}(G(a)+G(b)) - \int_a^b G = \int_a^b rac{(t-a)(b-t)}{2} G''$ (`trap_cell_identity`), single-cell bound $\le rac{(b-a)^2}{4}\int |G''|$ (`trap_cell_bound`), and global multi-cell trapezoid error bounds (`finite_trapezoid_cells`, `finite_trapezoid_endpoints`).
59. [Arcsine charge correction and discrete birth summability](arcsine-charge-correction-and-discrete-birth-summability.md). Formalization in `ChargeBirthSummability.lean` and `ChargeArcsinBound.lean` of the non-asymptotic mean-value bound $0 \le rcsin z - z \le z^3/3$ (`arcsin_sub_bounds`), scale decay $0 \le \operatorname{correction}(y) \le rac{1}{3y^{3/2}}$ (`correction_bounds`), discrete birth difference identity $\operatorname{correction}(n) = \operatorname{discreteBirth}(n, 1) - \operatorname{discreteBirth}(n, 0)$ (`correction_eq_discrete_difference`), and absolute summability of von Mangoldt weighted discrete jumps across all prime powers (`weighted_discrete_difference_norm_summable`).
60. [Collective all-prime seed square-integrability and transposed memory defect](collective-all-prime-seed-square-integrability-and-memory-defect.md). Formalization in `PrimeSeedAllPrimeSquare.lean`, `AllPrimeMemoryKernel.lean`, and `MemoryChargeFubini.lean` of the three-region prime tail bounds, universal age bound $v \cdot \operatorname{allPrimeSeed}(v) \le 64\log 2 + 30$ (`allPrimeSeed_age_bound`), unconditional $L^2$ integrability (`integrable_allPrimeSeed_square`, `allPrimeSeed_memLp_two`), bivariate Fubini integrability (`memory_charge_product_integrable`), and transposed memory defect identity (`allPrimeSeed_defect_causalMemory`).
61. [Machine formalization of Landau's positive-measure singularity theorem and critical sign criterion](machine-formalization-of-landau-positive-measure-singularity-theorem.md). Formalization in `LandauSingularity.lean`, `ActualCriticalConvergence.lean`, and `ActualCriticalSignCriterion.lean` of the impossibility of holomorphic extension across the abscissa of convergence (`no_holomorphic_extension`), the upper bound $\sigma_0 \le 1/2$ (`finite_abscissa_le_half`), and the formal deduction of the full Riemann Hypothesis from eventual nonpositivity of $W(x)$ (`RiemannHypothesis_of_nonpos_after`, `RiemannHypothesis_of_eventually_nonpos`).
62. [Centered Goldbach total-four obstruction and successor heat shift](centered-goldbach-total-four-obstruction-and-successor-heat-shift.md). Formalization in `CenteredGoldbachPositiveTransferObstruction.lean` and `CenteredGoldbachSuccessorHeatFinite.lean` of the exact formula $c_3(4) = (\log 2 - 1)^2 - 2(\log 3 - 1)$ (`centered_coefficient_four_exact`), strict negativity $c_3(4) < 0$ (`centered_coefficient_four_neg`), refutation of even-coefficient nonnegativity (`not_all_even_centered_coefficients_nonnegative`), refutation of Hankel positive semidefiniteness (`centered_hankel_diagonal_not_nonnegative`), endpoint shift identity (`shiftedCenteredHeat_endpoint`), opposite sign changes at totals 2 and 4 (`successor_coefficient_two_neg`, `successor_coefficient_four_pos`), and integrated shell covariance law (`shiftedShell_sub_original`).
63. [Universal dual autocorrelation trace pairing for discrete prime packets](universal-dual-autocorrelation-trace-pairing.md). Formalization in `AutocorrelationRegularity.lean` and `LogarithmicPrimeWindow.lean` of the exact real-space duality formula relating smooth windowed prime sums directly to continuous causal source integrals (`actual_packet_signed_windows`), window differentiation formula (`window_hasDerivAt`), physical moment factorization $\hat{f}(1/2)\hat{f}(-1/2)$ (`densityMoment_autocorrelation`), and support confinement vanishing.
64. [Collective all-prime seed weak derivative and mutually singular Radon measures](collective-all-prime-seed-weak-derivative-and-mutually-singular-radon-measures.md). Formalization in `PrimeSeedAllPrimeDerivative.lean` and `PrimeSeedFiniteDerivative.lean` of the mutual singularity $\mu_{\mathrm{all}}^+ \perp \mu_{\mathrm{all}}^-$ (`allPrimeDerivative_mutuallySingular`), infinite total variation $\mu_{\mathrm{all}}^\pm(\mathbb{R}) = \infty$ via $\sum_p 1/p = \infty$ (`allPrimeDerivativePositive_mass`, `allPrimeDerivativeNegative_mass`), local finiteness on left half-lines (`allPrimeDerivativePositive_Iic_finite`), and the weak distributional derivative identity against bounded test functions (`allPrimeSeed_weak_derivative`).
65. [Regular source Möbius floor-inverse obstruction via Banach–Steinhaus](regular-source-mobius-floor-inverse-obstruction.md). Formalization in `RegularSource.lean` of the separated floor quotient scaling $c_e + t \le c_d$, the finite tent envelope lower bound $I_{f_t}(t^3) \ge t^2/64$ (`signedMobiusInverse_smoothTestSource_lower`), the divergence of functional operator norms for $r < 2/3$ (`regularMobiusFunctional_not_uniformly_bounded`), and the deduction via `banach_steinhaus` of a fixed square-root-bounded Lipschitz sequence exceeding any eventual $C N^r$ bound (`exists_fixed_regular_source_with_unbounded_inverse`).
66. [Successor-cell transfer phase rigidity and dyadic shell obstruction](successor-cell-transfer-phase-rigidity-and-dyadic-shell-obstruction.md). Formalization in `SuccessorCellTransferFinite.lean` of the exact scalar sign (`two_three_sign_phase_rigidity`), operator (`two_three_operator_phase_rigidity`), and complex (`two_three_complex_phase_rigidity`) phase rigidity forcing $s_2 = s_3 = 1$ and $\chi \equiv 1$ from the fourth cell, and the dyadic shell parity inversion of the binary tree depth gauge under tripling (`treeTwoGauge_three_low_shell`, `treeTwoGauge_three_high_shell`).
67. [Growing prime scattering product defect and interior strip decay](growing-prime-scattering-product-defect-and-interior-strip-decay.md). Formalization in `GrowingPrimeScatteringBounds.lean` of the exact multiplier tail factorization (`paired_tail_factorization`), the single-prime uniform geometric decay in the critical strip interior (`actual_uniform_tail_bound`), the finite block product deviation bound (`actual_uniform_block_comparison`), and infinite weighted low/high block decomposition (`actual_block_weighted_tsum_comparison`).
68. [Canonical quotient fractional approximation and Mertens $L^2$ lower bound](canonical-quotient-fractional-approximation-and-mertens-l2-lower-bound.md). Formalization in `FractionalApproximation.lean` of the quotient mass evaluation $\sum_{q=1}^N c_q(N) = M(N)$ (`quotient_coefficient_sum`), periodic reciprocal error representation (`reciprocalError_periodic`), the explicit $L^2(0, 1)$ lower bound $\operatorname{dualMeanSquareError}(N) \ge \frac{(M(N)+1)^2}{16N}$ (`dualMeanSquareError_lower`), and the Mertens oscillation obstruction (`mertens_sq_le_dual_error`, `mertens_sq_normalized_tendsto_of_dual_error`).
69. [Compact-strip theta payment and explicit resolvent coercivity](compact-strip-theta-payment-and-explicit-resolvent-algebra.md). Formalization in `ThetaCompactStripRationalCore.lean` of the order-theoretic coercivity payment theorem (`compact_payment`), Schur substitution bound $C \le 3^B / 2$ at rational scale $\varepsilon(B)$ (`schur_at_epsilonOf`), explicit uniform resolvent bound $P \le 4 \cdot 10^{1046} \cdot 3^B$ (`explicit_resolvent_bound`), logarithmic arrival deficit absorption (`explicit_arrival_payment`), and aggregated decimal strip lower bound $-10^{503} < \mathrm{total}$ (`explicit_strip_lower`).
70. [Archimedean logarithmic derivative and analytic vanishing order duality](archimedean-logarithmic-derivative-and-analytic-vanishing-order-duality.md). Formalization in `ActualArchimedeanFactor.lean` of the Archimedean logarithmic derivative (`gamma_factor_log_derivative`), the general analytic logarithmic residue theorem in punctured filters (`analytic_log_derivative_order`), the entire $\xi$ logarithmic derivative boundary series (`xi_prime_boundary`), and the exact zero multiplicity equality $\operatorname{ord}_s(\xi) = \operatorname{ord}_s(\zeta)$ (`xi_zeta_zero_order`, `zeta_log_derivative_order`).
71. [Even Goldbach three-phase Fourier reconstruction of Chebyshev error](even-goldbach-three-phase-fourier-reconstruction-of-chebyshev-error.md). Formalization in `GoldbachThreePhaseFourier.lean` of the continuous-to-algebraic harmonic identity (`threeAngularHarmonic_eq`), the harmonic relation to the prime error (`complexAngularHarmonic_prime_error`), and the exact non-asymptotic inversion recovering $\psi(2k+3) - (2k+3)$ from the complex Goldbach angular harmonic (`actual_prime_error_from_complex_angular_harmonic`).
72. [Truncated prime scattering amplification and non-contractivity obstruction](truncated-prime-scattering-amplification-and-non-contractivity-obstruction.md). Formalization in `CompactLocalScatteringSigns.lean` of the geometric sum factorization (`finite_history_factorization`), zero-frequency attenuation (`compact_attenuation_zero`), resonant amplification at $t = \frac{\pi}{M \log p}$ (`compact_amplification_special`), and the universal obstruction across all primes and history truncations (`original_prime_compact_signs`).
73. [Same-prime Dirichlet series and unconditional half-plane analyticity](same-prime-dirichlet-series-and-unconditional-half-plane-analyticity.md). Formalization in `SamePrimeDirichlet.lean` of the exact multiplicity power series representation (`powerTerm_hasSum`, `samePrimeTerm_eq_cpow`), uniform majorant summability (`uniform_majorant`), locally uniform convergence (`expanded_uniformConvergence`), and unconditional holomorphy and analyticity on $\operatorname{Re}(s) > 1/2$ (`H_differentiableOn`, `H_analyticAt`).
74. [Discrete prime error energy balance and signed drift decomposition](discrete-prime-error-energy-balance-and-signed-drift-decomposition.md). Formalization in `PrimeEnergy.lean` of the weighted discrete integration-by-parts theorem (`weighted_energy_balance`), the prime error energy identity (`prime_energy_balance`), diagonal increment log-square bound (`primeIncrementEnergy_le_log_square`), conditional drift bound (`prime_error_square_upper_of_drift_nonpos`), and initial drift positivity obstruction (`primeErrorDrift_two_pos`).
75. [Causal successor Jacobian defect kernel and charge-conserving compensation](causal-successor-jacobian-defect-kernel-and-charge-conserving-compensation.md). Formalization in `SuccessorCharge.lean` of the diffeomorphism pull-back formula (`jacobian_mul_weight_sigma`), the exact defect kernel representation (`integral_successor_sub`), the uniform kernel bound $|K(u)| \le 1$ (`kernel_abs_le`), and exact preservation of integral and charge under rank-one boundary compensation (`integral_compensated`, `charge_compensated`).
76. [Causal successor-predecessor adjoint duality and isometry-projection algebra](causal-successor-predecessor-adjoint-duality-and-isometry-projection-algebra.md). Formalization in `SuccessorCriticalLift.lean` of the $L^2$ adjoint relation (`successor_predecessor_adjoint`), left-inverse identity (`successor_predecessor`), projection relation (`predecessor_successor`), and non-negative lintegral isometry and co-isometry theorems (`predecessor_square_lintegral`, `successor_square_lintegral`).
77. [Banach–Steinhaus regular source Möbius floor-inverse obstruction](banach-steinhaus-regular-source-mobius-floor-inverse-obstruction.md). Formalization in `RegularSource.lean` of the Banach space of regular sources, the failure of uniform boundedness of the Möbius functional below exponent $2/3$ (`regularMobiusFunctional_not_uniformly_bounded`), and the deduction via `banach_steinhaus` of a fixed regular counterexample trajectory with unbounded $O(N^r)$ inverse (`exists_fixed_regular_source_with_unbounded_inverse`).
78. [Causal source convolution distinct-prime and density decomposition](causal-source-convolution-distinct-prime-and-density-decomposition.md). Formalization in `PrimeSourceDistinctFilter.lean` of local $L^2$ integrability of the prime source (`source_memLp`), the exact identification of the filtered diagonal with $\tau(s)$ (`filtered_diagonal_eq_tau`), the source-tau residual expansion (`filtered_source_distinct`), and the closed-form density convolution $(D * D)(s) = s e^{s/2}$ (`density_convolution`).
79. [Finite signed logarithmic window prime-source integration bridge](finite-signed-logarithmic-window-prime-source-integration-bridge.md). Formalization in `LogarithmicPrimeWindow.lean` of the critical window differentiation rule (`window_hasDerivAt`), vanishing boundary conditions (`window_one_eq_zero`, `window_endpoint_eq_zero`), single-window residual formula (`window_residual_frozen_source`), and the general finite signed superposition bridge (`signed_windows_frozen_source`).
80. [Arithmetic potential V(x) identity and critical source residual identification](arithmetic-potential-v-identity-and-critical-source-residual-identification.md). Formalization in `PrimeSourceVEvaluation.lean` of the exact formula for the arithmetic potential $V(x)$ (`rawV`, `V`), the filtered distinct prime pair integration (`filtered_distinct_arithmetic`), the master identification theorem $\mathcal{L}_0(S * S)(s) - \tau(s) = e^{-3s/2} V(e^s)$ (`actual_source_eq_normalized_V`), and the boundary condition $V(1) = 0$ (`V_one`).
81. [Riemann zeta centered logarithmic derivative removable singularity](riemann-zeta-centered-logarithmic-derivative-removable-singularity.md). Formalization in `RiemannZetaCenteredLogDeriv.lean` of the entire normalized zeta function $U(s) = (s-1)\zeta(s)$ (`differentiable_U`), its derivative $U'(1) = \gamma$ (`hasDerivAt_U_one`), and the proof that the centered logarithmic derivative $G(s) = -\zeta'(s)/\zeta(s) - s/(s-1)$ has a removable singularity at $s=1$ with analytic value $-\gamma - 1$ (`analyticAt_Gfilled_one`, `Gfilled_one`).

82. [Critical transform off-line zeta zero double-pole obstruction](critical-transform-off-line-zeta-zero-double-pole-obstruction.md). Formalization in `CriticalTransformPoles.lean` of the analyticity of the filled critical transform at $s = 1$ (`analyticAt_filledTransform_one`), and the exact meromorphic pole of order $-2$ with non-zero trailing coefficient $M(\rho)m^2$ at every off-line zero $\rho$ with $\operatorname{Re}(\rho) > 1/2$ (`pole_at_zeta_zero`, `not_analyticAt_zeta_zero`).
83. [Same-prime proper-power history residual global monotonicity](same-prime-proper-power-history-residual-global-monotonicity.md). Formalization in `SamePrimeHistoryResidual.lean` of non-negativity $s(v) \ge 0$ (`s_nonneg`), global monotonicity under joint continuous-discrete index growth (`s_monotone`), and the universal delay inequality $s(v-r) \le s(v)$ for all $r \ge 0$ (`s_delay_le`).
84. [Chronological divisibility Turán determinant and sub-multiplicativity](chronological-divisibility-turan-determinant-and-sub-multiplicativity.md). Formalization in `ChronologicalDivisibility.lean` of the dilation probability monotonicity (`probability_monotoneOn`), the multiplicative Turán determinant non-negativity $S(x/a)S(x/b) - S(x)S(x/(ab)) \ge 0$ (`determinant_nonneg`), and the exact sub-multiplicativity of composite divisibility (`probability_mul_le`).
85. [Chronological power moment ratio strict monotonicity](chronological-power-moment-ratio-strict-monotonicity.md). Formalization in `ChronologicalPowerRatio.lean` of the inverse-square-root mass bound $A(n) \le 2\sqrt{n} - 1$ (`A_bound`), the controller strict monotonicity (`h_strictMono`), the moment comparison inequality $D(n) < B(n)(1 + h(n))$ (`D_lt_B_mul`), and the strict decreasing monotonicity of the normalized moment ratio $B(n)/D(n)$ (`normalizedRatio_succ_lt`, `normalizedRatio_lt`).
86. [Successor-cell discrete-continuous density comparison and PNT bridge](successor-cell-discrete-continuous-density-comparison-and-pnt-bridge.md). Formalization in `SuccessorCellDensityComparison.lean` of the one-sided integral-sum error (`inv_sqrt_integer_density_error`), the master prime vs. continuous density comparison bound (`actual_prime_continuous_density_bound`), and the normalized child dilation interval integral transfer bound (`normalized_child_dilation_integral_density_bound`).
87. [Successor feedback terminal-strip bound and error-difference driver](successor-feedback-terminal-strip-bound-and-error-difference-driver.md). Formalization in `SuccessorFeedbackBoundary.lean` of the exponential decay of the boundary strip error bounded by $4 M e^{-R/2}$ (`terminal_strip_bound`), the infinite relative charge identity (`profile_relative_charge`), and the physical error-difference representation of the feedback driver $d_j = \frac{1}{2} \int_1^\infty \frac{E(x+j+1) - E(x+j)}{\sqrt{x}} dx$ (`driver_eq_difference_integral`).
88. [Successor-operator iterated physical shift and driver-series summability](successor-operator-iterated-physical-shift-and-driver-series-summability.md). Formalization in `SuccessorFeedbackSource.lean` of the exact iterate shift formula $(T^j \mathcal{L} H)(v) = e^{v/2} H(e^v + j)$ (`successor_iterate_physicalLift`), uniform defect bound $|\operatorname{defect}(T^j \mathcal{L} H)| \le 2 M$ (`iterate_defect_bound`), and unit disk summability of the driver generating series (`driver_series_summable`).
89. [Physical theta uniform differentiation and Riemann xi differential equation](physical-theta-uniform-differentiation-and-riemann-xi-differential-equation.md). Formalization in `PhysicalThetaUniform.lean` of the uniform derivative envelopes (`seed_uniform_bound`, `slope_uniform_bound`, `curvature_uniform_bound`), term-by-term twice differentiation of the physical theta sum (`seedSum_hasDerivAt`, `slopeSum_hasDerivAt`), and the exact differential identity $(\operatorname{seedSum})''(u) - \frac{1}{4}\operatorname{seedSum}(u) = \Phi(u)$ (`seedSum_differential_identity`).
90. [Goldbach three-phase first harmonic exact Chebyshev error identity](goldbach-three-phase-first-harmonic-exact-chebyshev-error-identity.md). Formalization in `GoldbachThreePhaseEndpoint.lean` of the exact one-leg reference sum (`one_leg_reference_exact`), powers-of-two count formula $\operatorname{twoPowerPrefix}(Y) = (\log 2) \lfloor \log_2 Y \rfloor$ (`twoPowerPrefix_eq_power_count`), and the exact non-asymptotic first harmonic identity (`three_first_harmonic_prime_error`).
91. [All-prime seed three-region decomposition and L² square integrability](all-prime-seed-three-region-decomposition-and-l2-square-integrability.md). Formalization in `PrimeSeedAllPrimeSquare.lean` of the three-region prime decomposition (`finite_seed_three_region_bound`), age bound (`allPrimeSeed_age_bound`), and unconditional $L^2(\mathbb{R})$ square integrability (`allPrimeSeed_memLp_two`).
92. [Filtered prime-density convolution and three-component potential decomposition](filtered-prime-density-convolution-and-three-component-potential-decomposition.md). Formalization in `PrimeDensityFilter.lean` of the exact prime power pair evaluation (`prime_power_pair_evaluation`), symmetrized mixed order convolution (`prime_power_mixed_evaluation`), and density-density self-convolution (`density_square_normalized`).
93. [Arithmetic prime-transport operator zeta-conjugation and point spectrum](arithmetic-prime-transport-operator-zeta-conjugation-and-point-spectrum.md). Formalization in `ArithmeticTransport.lean` of the exact zeta-conjugation identity (`zeta_mul_primeTransport`), Möbius inversion formula (`primeTransport_conjugation`), one-dimensional kernel characterization (`primeTransport_kernel_iff`), and exact point spectrum $\{\log k\}_{k \ge 1}$ (`primeTransport_eigenvalues`).
94. [Physical prime-block scattering two-sided Mellin parity law](physical-prime-block-scattering-two-sided-mellin-parity-law.md). Formalization in `ActualPrimeBlockMellinParity.lean` of single-prime opposing multiplier signs (`actual_prime_multiplier_product_negative`), positive block amplitude (`actual_block_amplitude_positive`), and the exact sign parity theorem (`actual_block_two_sided_mellin_parity`).
95. [Even-seed prime-block paired Weil weight parity law](even-seed-prime-block-paired-weil-weight-parity-law.md). Formalization in `ActualPrimeBlockWeilWeightParity.lean` of the even seed modulus identity (`even_seed_paired_weight`), prime block parity transformation (`actual_block_paired_weight_parity`), and unconditionally non-negative parity-signed weight (`actual_even_seed_parity_signed_weight_nonnegative`).
96. [Composite prime-block scattering amplitude magnitude sandwich](composite-prime-block-scattering-amplitude-magnitude-sandwich.md). Formalization in `ActualPrimeBlockMagnitude.lean` of the two-sided geometric-exponential amplitude bounds $(1/8)^{|ps|} \prod p^{h d(p)} \le \mathcal{A} \le 2^{|ps|} \prod p^{h d(p)}$ (`actual_block_amplitude_bounds`).
97. [Smooth compact carrier polar residue nullification and scattering preservation](smooth-compact-carrier-polar-residue-nullification-and-scattering-preservation.md). Formalization in `ActualPhysicalResidues.lean` of the continuous physical paired weight (`physical_paired_weight_continuous`), universal polar vanishing under scattering (`finite_block_pole_corrections_vanish`), and existence of smooth narrow carriers (`exists_physical_pole_null_carrier`).
98. [Finite heat source formula, exponential decay, and L² integrability](finite-heat-source-formula-exponential-decay-and-l2-integrability.md). Formalization in `ActualFiniteHeatSource.lean` of the exact closed formula (`source_formula`), Chebyshev zero-limit identity (`source_at_zero`), uniform exponential bound (`source_norm_bound`), and $L^2(0, \infty)$ square integrability (`source_square_integrable`).
99. [Exact three-component Gram energy decomposition for arithmetic heat sources](exact-three-component-gram-energy-decomposition-for-arithmetic-heat-sources.md). Formalization in `ActualHeatDensityPairing.lean` of the continuous density energy (`density_square_closed_formula`), discrete-continuous cross term (`mixed_density_integral`), and the closed three-component Gram formula for the heat source $S_N(t)$ (`actual_source_gram`).
100. [Critical Landau abscissa of convergence theorem](critical-landau-abscissa-of-convergence-theorem.md). Formalization in `ActualCriticalConvergence.lean` of the convergence abscissa bound $\sigma_c \le 1/2$ (`finite_abscissa_le_half`), universal Laplace integrability on $\operatorname{Re}(s) > 1/2$ (`laplace_integrable_above_half`), and open half-plane holomorphy (`laplace_analyticOnNhd_above_half`).
101. [Finite prime-block zero circle residue perturbation bound](finite-prime-block-zero-circle-residue-perturbation-bound.md). Formalization in `ActualZeroCircleResidues.lean` of the analytic zero circle residue formula (`analytic_zero_circle_coefficient`), signed circle difference identity (`finite_prime_block_signed_circle_difference`), and exponential perturbation comparison bound inside the critical strip (`finite_prime_block_circle_comparison`).
102. [Exact finite prime-cofactor future transfer identity](exact-finite-prime-cofactor-future-transfer-identity.md). Formalization in `ActualPrimeCofactorFutureFinite.lean` of the exact complex hyperbola divisor identity (`weighted_log_eq_complete_future`) and proper-cofactor cutoff transfer theorem (`cofactor_cutoff_transfer`).
103. [Exact finite Abel-Mertens identity and exponential derivative bounds](exact-finite-abel-mertens-identity-and-exponential-derivative-bounds.md). Formalization in `ActualFiniteAbelMertens.lean` of the exact discrete Abel-Mertens identity (`truncated_mobius_abel`), exponential derivative formula (`hasDerivAt_truncatedMobiusExp`), and absolute derivative bound (`abs_truncatedMobiusDerivative_le`).
104. [Finite sinc prime diagonal decomposition and birth norm majorization](finite-sinc-prime-diagonal-decomposition-and-birth-norm-majorization.md). Formalization in `ActualFiniteSincPrimeDiagonal.lean` of the exact sinc diagonal decomposition (`finite_sinc_prime_diagonal_decomposition`), non-negativity (`finite_prime_diagonal_nonneg`), and full-set majorization (`finite_prime_diagonal_le_full`).
105. [Exact finite quadrature error bounds for heat renewal cells](exact-finite-quadrature-error-bounds-for-heat-renewal-cells.md). Formalization in `ActualHeatRenewalRectangle.lean` of the right-endpoint total-variation error bound (`finite_right_rectangle`, `finite_right_rectangle_div`) and composite trapezoid Peano error bound (`finite_trapezoid_endpoints`).
106. [Exact damped causal ramp prime-power correlation weights](exact-damped-causal-ramp-prime-power-correlation-weights.md). Formalization in `ActualRampPrimeWeights.lean` of the piecewise ramp weight evaluation (`orientedWeight_slope_prime`), von Mangoldt summand splitting (`secondPrimeTerm_slope`), and monotonicity in truncation cutoff (`actualPrimeCross_mono_cutoff`).
107. [Exact two-sided physical scattering Mellin bridge and history dictionaries](exact-two-sided-physical-scattering-mellin-bridge-and-history-dictionaries.md). Formalization in `ActualScatteringMellinSignBridge.lean` of positive/negative history dictionaries (`positive_history_dictionary`, `negative_history_dictionary`) and two-sided physical Mellin factorization (`actual_physical_two_sided_mellin`).
108. [Exact zero-charge pair kernel expansion and terminal prime trace](exact-zero-charge-pair-kernel-expansion-and-terminal-prime-trace.md). Formalization in `ActualTerminalPrimeTraceFinite.lean` of the zero-charge asymmetric Cauchy pair kernel expansion (`zero_charge_pair_kernel_expansion`), Chebyshev charge cancellation (`packetCoefficient_sum_zero`), and terminal prime packet expansion (`packet_pair_kernel_expansion`).
109. [Reciprocal bump algebra and off-line zero-pair negativity bound](reciprocal-bump-algebra-and-off-line-zero-pair-negativity-bound.md). Formalization in `ActualWeilZeroPairAlgebra.lean` of reciprocal bump identity (`reciprocal_bump_factor`), conjugate reflection swap (`reflected_bump_factor`), and paired off-line zero negativity bound (`central_pair_upper_norm`).
110. [Critical Gamma norm identity and Lorentzian majorization bound](critical-gamma-norm-identity-and-lorentzian-majorization-bound.md). Formalization in `AdditiveHeatGamma.lean` and `AdditiveHeatWeight.lean` of the critical Gamma norm identity (`gamma_half_line_norm_sq`), cosh denominator bound (`physical_denominator_le_cosh`), and rational Lorentzian majorization (`heat_weight_le_physical`).
111. [Exact physical-additive Gram kernel balance decomposition](exact-physical-additive-gram-kernel-balance-decomposition.md). Formalization in `AdditivePairBalance.lean` of the kernel balance identity (`kernel_identity`), complete finite Gram decomposition (`complete_finite_gram_identity`), and strict indefiniteness proofs (`balance_not_positive_quadratic`, `balance_not_negative_quadratic`).
112. [Exact two-sided prime scattering multiplier magnitude envelope and cardinal parity](exact-two-sided-prime-scattering-multiplier-magnitude-envelope-and-cardinal-parity.md). Formalization in `ActualScatteringMomentSigns.lean` and `ActualScatteringMomentMagnitude.lean` of opposite signs (`literal_multiplier_signs`), cardinal parity law (`actual_prime_block_product_parity`), and power envelope bounds (`actual_prime_multiplier_magnitude`).
113. [Exact centered distinct-prime tent Mellin representation](exact-centered-distinct-prime-tent-mellin-representation.md). Formalization in `ActualCenteredMellin.lean` of the tent decomposition (`V_eq_N_sub_T`), arithmetic cutoff formula (`V_eq_real_cutoff`), and closed Mellin transform (`hasMellin_V`).
114. [Exact critical-multiple Mellin integral identity and half-plane convergence](exact-critical-multiple-mellin-integral-identity-and-half-plane-convergence.md). Formalization in `ActualCriticalMellin.lean` of vanishing below $x=1$ (`W_zero`), shifted zeta convolution identity (`hasMellin_W`), and endpoint-justified integral representation (`integral_W_Ioi_one`).
115. [Exact critical Laplace moment identity and compact initial difference](exact-critical-laplace-moment-identity-and-compact-initial-difference.md). Formalization in `ActualCriticalLaplace.lean` of Laplace-to-Mellin exponent conversion (`density_smul_exp`), tail moment identification (`moment_eq_mellin_negativeWTail`), and compact initial difference formula (`moment_eq_initial_sub_filledTransform`).
116. [Exact finite van der Corput differencing and aggregate shift energy](exact-finite-van-der-corput-differencing-and-aggregate-shift-energy.md). Formalization in `FiniteShiftEnergy.lean` and `AggregateShiftEnergy.lean` of shift conservation on the common envelope (`sum_shiftEnvelope_shifts`), finite shift energy inequality (`finite_shift_energy`), outer weighted differencing (`weighted_finite_shift_energy`), and aggregate differencing preserving all cross-terms (`aggregate_finite_shift_energy`).
117. [Discrete potential-occupation duality and finite flow traffic bounds](discrete-potential-occupation-duality-and-finite-flow-traffic-bounds.md). Formalization in `AncestryFlow.lean` of discrete Green potential-occupation duality (`finite_flow_potential`), absorption mass conservation (`finite_flow_absorption`), Foster-Lyapunov drift bounds (`finite_flow_drift_bound`), and total traffic majorization (`finite_flow_traffic_bound`).
118. [Möbius divisor history splitting and off-diagonal Goldbach negativity](mobius-divisor-history-splitting-and-off-diagonal-goldbach-negativity.md). Formalization in `CenteredGoldbachHistoryShellFinite.lean` of binary prime correlation splitting (`primeCoefficient_history_split`), diagonal non-negativity and dominance (`diagonalCoefficient_nonneg`, `log_pair_le_diagonalHistory`), universal off-diagonal non-positivity (`offDiagonalCoefficient_nonpos`), certified negative witness (`offDiagonalCoefficient_four_six_neg`), and odd-total parity obstruction (`diagonal_label_odd_of_odd_total`).
119. [Exact centered Goldbach truncated heat algebra and successor difference](exact-centered-goldbach-truncated-heat-algebra-and-successor-difference.md). Formalization in `CenteredGoldbachTruncatedHeat.lean` of truncated heat Cauchy square (`truncatedHeat_square`, `centered_finiteHeat_square`), support ceiling (`truncatedCoefficient_eq_zero_of_two_mul_lt`), interior uncentered identification (`truncatedCoefficient_interior`), Chebyshev expansion (`truncatedCoefficient_interior_prime_psi`), and exact additive successor difference (`centeredGoldbach_successor`).
120. [Exact successor transfer defect of growing and original charge densities](exact-successor-transfer-defect-of-growing-and-original-charge-densities.md). Formalization in `ChargeDensityEvaluation.lean` of cutoff integration and shift (`cutoff_mass`, `successor_cutoff`), cutoff defect formula (`cutoff_defect`), growing mode defect vanishing (`original_defect_growth`), and closed geometric density defect evaluation (`original_density_defect`).
121. [Exact divisor-current totient contraction and successor conservation](exact-divisor-current-totient-contraction-and-successor-conservation.md). Formalization in `BirthDivisorCoupling.lean` of the two-sided neighbor divisor current (`weightedCurrent`) and universal invariant contraction (`sum_weightedCurrent`).
122. [Exact frozen-source atomic defect decomposition and positivity](exact-frozen-source-atomic-defect-decomposition-and-positivity.md). Formalization in `ChargeFrozenSource.lean` of origin value (`causalSource_at_origin`), atomic source decomposition (`source_decomposition`), tail non-negativity (`kernel_tail_nonnegative`), exact defect expansion (`original_defect_source`), and unconditional defect non-negativity (`original_defect_source_nonnegative`, `original_defect_causalSource_nonnegative`).
123. [Frustrated-cycle energy decomposition and harmonic conductance lower bound](frustrated-cycle-energy-decomposition-and-harmonic-conductance-lower-bound.md). Formalization in `OddTriangleConductance.lean` of frustrated triangle energy decomposition (`triangle_cycle_decomposition`), parallel weighted route decomposition (`weighted_route_decomposition`), and exact harmonic conductance lower bound (`weighted_route_lower_bound`).
124. [Exact complete Weil divisor-packet prime energy identity](exact-complete-weil-divisor-packet-prime-energy-identity.md). Formalization in `CompactWeilDivisorEnergyFinite.lean` of incoming square logarithm identity (`incoming_square_eq_log`), Dirichlet history energy non-negativity (`historyEnergy_nonneg`), pointwise Mertens boundary regrouping (`boundaryEnergy_eq_pointwise`), and global and pointwise prime deficit decompositions (`prime_deficit_eq_history_add_boundary`, `prime_deficit_eq_history_add_pointwise`).
125. [Exact coarse-primitive prime-error energy and dyadic test bounds](exact-coarse-primitive-prime-error-energy-and-dyadic-test-bounds.md). Formalization in `CoarsePrimitive.lean` of terminal mass control (`coarse_terminal_mass_sq_le`), exact summation by parts with floor jumps (`coarsePrime_summation_by_parts`), dyadic Mellin block representation (`coarsePrime_mellin_block`), and universal test-function estimate (`coarsePrime_test_bound`).
126. [Exact centered-divisor forcing inversion and Chebyshev error duality](exact-centered-divisor-forcing-inversion-and-chebyshev-error-duality.md). Formalization in `MertensArithmetic.lean` of log-factorial prime decomposition (`logFactorial_eq_sum_psi`), error recurrence forcing (`sum_integerError_eq_forcing`), Möbius floor mass contraction (`moebius_floor_mass`), general Möbius floor inversion (`moebius_floor_inversion`), Chebyshev error inversion (`integerError_eq_moebius_recurrenceForcing`), and centered divisor forcing inversion with $2\gamma$ shift (`centeredDivisorForcing_inverse`).
127. [Exact joint-Mellin principal part at arbitrary-multiplicity zeta zeros](exact-joint-mellin-principal-part-at-arbitrary-multiplicity-zeta-zeros.md). Formalization in `ZetaPole.lean` of zeta analyticity on $\mathbb{C} \setminus \{1\}$ (`zeta_analytic_off_pole`), connected non-vanishing (`zeta_not_locally_zero`), general logarithmic derivative factorization (`analytic_logDerivative_principal_part`), and exact joint Mellin principal part with residue $-m/\rho$ (`zeta_jointMellin_principal_part`).
128. [Exact dyadic coarse-Mellin sum and zeta log-derivative identity](exact-dyadic-coarse-mellin-sum-and-zeta-log-derivative-identity.md). Formalization in `CoarseInitialIdentity.lean` of unconditional prime error kernel integrability (`primeErrorReal_kernel_integrable_unconditional`), dyadic half-line partition (`coarse_dyadic_Ioc_union`), HasSum convergence of dyadic blocks (`coarseMellinBlock_hasSum_integral`, `coarseMellinSum_eq_integral`), and exact identity with the zeta logarithmic derivative (`coarseMellinSum_eq_logDerivative`).
129. [Exact dyadic Mellin-block geometric decay and primitive energy coercivity](exact-dyadic-mellin-block-geometric-decay-and-primitive-energy-coercivity.md). Formalization in `CoarseDyadicBound.lean` and `CoarseBlockAnalytic.lean` of entire differentiability and analyticity of dyadic Mellin blocks (`coarsePrime_finiteMellin_differentiable`, `coarseMellinBlock_analytic`), primitive energy norm majorization (`coarsePrime_mellin_norm_le`), negative power bound (`coarseMellinBlock_power_bound`), and uniform geometric decay bound (`coarseMellinBlock_geometric_bound`).
130. [Exact finite Rayleigh numerator of the Weil divisor packet](exact-finite-rayleigh-numerator-of-the-weil-divisor-packet.md). Formalization in `CompactWeilDivisorRayleighFinite.lean` of complete divisor edge collapse (`complete_divisor_edge_sum`) and ordered-pair Rayleigh numerator (`ordered_divisor_rayleigh_numerator`).
131. [Exact boundary form-smallness and two-block Young coercivity](exact-boundary-form-smallness-and-two-block-young-coercivity.md). Formalization in `BoundaryFormSmallness.lean` of relative port form-smallness (`tail_port_relative_bound`), explicit cutoff scale selection (`cutoff_for_relative_bound`), two-block Young parameter estimation (`two_block_port_upper`), and subspace restriction invariance (`restrict_form_smallness`).
132. [Exact coherent endpoint-packet deviation and linear-supply bounds](exact-coherent-endpoint-packet-deviation-and-linear-supply-bounds.md). Formalization in `CoherentPacketAlgebra.lean` of complex norm-square expansion (`complex_normSq_add`), coherent total deviation (`coherent_total_deviation`), Cauchy cross-square deviation (`coherent_deviation_of_cross_square`), quadratic correction identity (`quadratic_correction_identity`), and linear supply vanishing ratio (`bounded_port_over_linear_supply`).
133. [Exact causal-filter energy identity and passivity contraction](exact-causal-filter-energy-identity-and-passivity-contraction.md). Formalization in `CausalFilterEnergy.lean` of right-differentiable causal cross-integral (`causalFilter_cross_integral`), dissipated energy formula (`causalFilter_energy_identity`), $L^2$ contraction at rate $\ge 1/2$ (`causalFilter_energy_contraction`), and response gain bound (`causalFilter_response_bound`).
134. [Exact Archimedean scattering phase and xi-zeta zero-multiplicity equivalence](exact-archimedean-scattering-phase-and-xi-zeta-zero-multiplicity-equivalence.md). Formalization in `ActualArchimedeanFactor.lean` of real gamma factor logarithmic derivative (`gamma_factor_log_derivative`), Archimedean scattering multiplier (`actual_arch_multiplier`), completed zeta boundary series (`completed_zeta_prime_boundary`), analytic zero order equivalence (`xi_zeta_zero_order`), isolated zeros (`xi_zeros_isolated`), and logarithmic derivative residue limits (`xi_log_derivative_order`, `zeta_log_derivative_order`).
135. [Exact centered Riesz-Mellin identity for distinct prime pairs](exact-centered-riesz-mellin-identity-for-distinct-prime-pairs.md). Formalization in `ActualCenteredMellin.lean` of distinct tent decomposition (`V_eq_N_sub_T`), explicit cross sum (`etaSum_eq_Icc`), finite real cutoff formula (`V_eq_real_cutoff`), and closed Riesz-Mellin transform identity on $\operatorname{Re}(s) > 1$ (`hasMellin_V`).
136. [Exact prime-block scattering Mellin parity and amplitude bounds](exact-prime-block-scattering-mellin-parity-and-amplitude-bounds.md). Formalization in `ActualPrimeBlockMellinParity.lean` and `ActualPrimeBlockMagnitude.lean` of prime-block regularity preservation (`physical_block_regular`), Mellin transform factorization (`actual_block_mellin`), zero moment preservation (`actual_block_preserves_zero_moment`), two-sided Mellin reflection parity $(-1)^k$ (`actual_block_two_sided_mellin_parity`), and two-sided geometric amplitude bounds (`actual_block_amplitude_bounds`).
137. [Exact two-sided scattering moment signs and single-prime magnitude bounds](exact-two-sided-scattering-moment-signs-and-single-prime-magnitude-bounds.md). Formalization in `ActualScatteringMomentSigns.lean` and `ActualScatteringMomentMagnitude.lean` of sign opposition (`literal_multiplier_signs`), product negativity (`actual_prime_multiplier_product_negative`), block parity factorization (`actual_prime_block_product_parity`), and two-sided single-prime magnitude bounds (`literal_multiplier_magnitude`, `actual_prime_multiplier_magnitude`).
138. [Exact reciprocal bump algebra and Weil zero-pair negativity](exact-reciprocal-bump-algebra-and-weil-zero-pair-negativity.md). Formalization in `ActualWeilZeroPairAlgebra.lean` of reciprocal bump collapse (`reciprocal_bump_factor`), conjugate reflection swap (`reflected_bump_factor`), paired zero-side real part identity (`paired_bump_factor`), and sharp linear negativity bounds (`central_pair_upper`, `central_pair_upper_norm`).
139. [Exact zero-circle residue formula and prime-block perturbation bounds](exact-zero-circle-residue-formula-and-prime-block-perturbation-bounds.md). Formalization in `ActualZeroCircleResidues.lean` of analytic circle integral residue formula (`analytic_zero_circle_coefficient`), residue evaluation for $\xi$ and $\zeta$ (`xi_zero_circle_coefficient`, `zeta_zero_circle_coefficient`), prime-block centered weight factorization (`finite_prime_block_xi_circle_coefficient`, `finite_prime_block_zeta_circle_coefficient`), signed perturbation difference (`finite_prime_block_signed_circle_difference`), and interior strip comparison bound (`finite_prime_block_circle_comparison`).
140. [Exact zero-charge pair kernel expansion and terminal prime trace](exact-zero-charge-pair-kernel-expansion-and-terminal-prime-trace.md). Formalization in `ActualTerminalPrimeTraceFinite.lean` of zero-charge leading singularity cancellation (`zero_charge_pair_kernel_expansion`), Chebyshev error equivalence (`packetError_eq_primeErrorReal`), terminal-compensated zero net charge (`packetCoefficient_sum_zero`), discrete terminal Laplace/heat transform (`packet_heat_eq_discrete_terminal`), and universal prime packet pair kernel expansion (`packet_pair_kernel_expansion`).
141. [Exact prime-block Weil weight parity and definite positivity](exact-prime-block-weil-weight-parity-and-definite-positivity.md). Formalization in `ActualPrimeBlockWeilWeightParity.lean` of Mellin reflection invariance for even functions (`mellin_reflection_even`), squared-norm reduction (`even_seed_paired_weight`), block paired weight parity law (`actual_block_paired_weight_parity`), real representation for even seeds (`actual_even_seed_block_weight`), and guaranteed parity-compensated positivity (`actual_even_seed_parity_signed_weight_nonnegative`).
142. [Exact Dirichlet cofactor cutoff transfer and proper future splitting](exact-dirichlet-cofactor-cutoff-transfer-and-proper-future-splitting.md). Formalization in `ActualPrimeCofactorFutureFinite.lean` of weighted Dirichlet hyperbola identity (`weighted_log_eq_complete_future`) and exact cutoff transfer from prime-power rows to logarithmic moments and low-divisor/proper high-divisor cofactors (`cofactor_cutoff_transfer`).
143. [Exact oriented ramp prime weights and piecewise summand algebra](exact-oriented-ramp-prime-weights-and-piecewise-summand-algebra.md). Formalization in `ActualRampPrimeWeights.lean` of the three-region piecewise profile (`orientedWeight_plateau`, `orientedWeight_internal_transition`, `orientedWeight_outer_zero`, `orientedWeight_slope_prime`), exact algebraic summand decoupling (`secondPrimeTerm_slope`), and global cross-energy monotonicity (`actualPrimeCross_mono`, `actualPrimeCross_mono_cutoff`).
144. [Exact compact prime-error heat flow and Laplace endpoint duality](exact-compact-prime-error-heat-flow-and-laplace-endpoint-duality.md). Formalization in `ActualCompactHeatEndpoint.lean` of comparison with discrete Goldbach packets (`compactHeat_eq_goldbach_packet`), finite source minus terminal decomposition (`compactHeat_eq_source_sub_terminal`), exact Chebyshev error Laplace integral duality (`compactHeat_eq_error_integral`), and universal zero net charge (`compactHeat_at_zero`).
145. [Exact critical convergence abscissa and Landau singularity barrier](exact-critical-convergence-abscissa-and-landau-singularity-barrier.md). Formalization in `ActualCriticalConvergence.lean` of convergence abscissa bounded by critical line $\sigma_c \le 1/2$ via Landau's theorem (`finite_abscissa_le_half`), Laplace integrability for $\sigma > 1/2$ (`laplace_integrable_above_half`), holomorphy on $\operatorname{Re}(s) > 1/2$ (`laplace_analyticOnNhd_above_half`), and analytic continuation matching filled transform (`continuation_analyticOnNhd`, `continuation_eq_filledTransform`).
146. [Exact critical Mellin eventual sign criterion for the Riemann Hypothesis](exact-critical-mellin-eventual-sign-criterion-for-the-riemann-hypothesis.md). Formalization in `ActualCriticalSignCriterion.lean` of the cutoff-based RH implication (`RiemannHypothesis_of_nonpos_after`) and filter-theoretic eventual sign theorem (`RiemannHypothesis_of_eventually_nonpos`), closing all analytic steps without custom axioms.
147. [Exact critical square-root Mellin aggregation and zeta multiplier](exact-critical-square-root-mellin-aggregation-and-zeta-multiplier.md). Formalization in `ActualCriticalMellin.lean` of the critical aggregation sum representation (`W_eq_Icc`), support vanishing (`W_zero`), real-valuedness (`W_real`), shifted zeta multiplier identity (`hasMellin_W`, `hasMellin_W_filled`), and semi-infinite integral formula (`integral_W_Ioi_one`).
148. [Exact Laplace-Mellin measure duality and negative-tail isomorphism](exact-laplace-mellin-measure-duality-and-negative-tail-isomorphism.md). Formalization in `ActualCriticalLaplace.lean` of the density-power identity (`density_smul_exp`), Laplace moment equivalence with negative tail Mellin transform (`moment_eq_mellin_negativeWTail`), and explicit half-plane decomposition (`moment_eq_initial_sub_filledTransform`).
149. [Exact critical tail Radon measure and measurable cutoff algebra](exact-critical-tail-radon-measure-and-measurable-cutoff-algebra.md). Formalization in `ActualCriticalMeasure.lean` of the Borel measurability of floor-cutoff sums (`measurable_cutoff`, `measurable_W`), power-exponential identity (`real_weight_identity`), mass-to-Mellin-norm equivalence (`laplace_mass_eq_norm`), and non-empty convergence set (`convergenceSet_nonempty`).
150. [Exact Selberg symmetry formula and logarithmic derivation algebra](exact-selberg-symmetry-formula-and-logarithmic-derivation-algebra.md). Formalization in `SelbergIdentity.lean` of the logarithmic derivation on Dirichlet convolutions (`log_weighted_convolution`), operator identity $\Lambda_2 = \mu * \log^2$ (`selbergWeight_eq_moebius_logSquare`), non-negativity (`selbergWeight_nonneg`), and the exact finite Selberg symmetry formula (`selberg_exact_identity`).
151. [Exact same-prime and distinct-prime convolution decomposition](exact-same-prime-and-distinct-prime-convolution-decomposition.md). Formalization in `SelbergDistinctPrimes.lean` of the orthogonal decomposition $(\Lambda * \Lambda)(n) = W_{\text{same}}(n) + W_{\text{distinct}}(n)$ (`vonMangoldt_convolution_same_distinct`), proper-power evaluation and support of $W_{\text{same}}$ (`samePrimePairWeight_prime_pow`, `samePrimePairWeight_eq_zero_unless_proper_power`), and coprime divisor representation of $W_{\text{distinct}}$ (`distinctPrimePairWeight_eq_coprime_sum`).
152. [Exact disproof of Selberg centered primitive positivity pairing](exact-disproof-of-selberg-centered-primitive-positivity-pairing.md). Formalization in `SelbergCenteredPairing.lean` of the exact evaluations of centered Selberg area (`selbergCenteredArea_two`, `three`, `four`), prime primitive area (`primePrimitiveArea_two`, `three`, `four`), and the certified strict negative upper bound refuting non-negative discrete pairing (`selbergCenteredPairing_two_lt`).
153. [Exact Selberg-Riesz area logarithmic-time reflection integral](exact-selberg-riesz-area-logarithmic-time-reflection-integral.md). Formalization in `SelbergLogReflection.lean` of the measurable exponential substitution theorem (`integral_exp_substitution`) and the exact continuous logarithmic-time reflection integral formula for the Selberg centered area primitive (`selbergCenteredArea_log_reflection`).
154. [Exact Selberg-Riesz area continuous reflection identity](exact-selberg-riesz-area-continuous-reflection-identity.md). Formalization in `SelbergReflection.lean` of the modified error deficit identity (`primeError_sub_primitive_quotient`), reflected moment integration (`integral_primeError_reflected_birthMoment`), double hyperbola representation (`reflected_primePrimitive_sum`), and the exact Selberg continuous reflection formula (`selbergCenteredArea_reflection`).
155. [Exact Selberg-Riesz area Cauchy energy contraction bound](exact-selberg-riesz-area-cauchy-energy-contraction-bound.md). Formalization in `SelbergReflectionBound.lean` of the reciprocal quadratic change of variables (`integral_reciprocal_square`), jump-admissible Cauchy-Schwarz inequality (`interval_integral_mul_sq_le`), filtered energy bound (`selbergCenteredArea_sq_le_filtered`), and the sharp $N^{3/2}$ prime-energy bound (`abs_selbergCenteredArea_le`).
156. [Exact spectral mode reflection integral and resonance algebra](exact-spectral-mode-reflection-integral-and-resonance-algebra.md). Formalization in `SelbergSpectralModes.lean` of the exact continuous reflection convolution for spectral modes with resonance bifurcation (`integral_reflected_spectralMode`) and the finite spectral packet reflection sum (`integral_reflected_spectral_sum`).
157. [Exact weighted Selberg symmetry formula and signed Möbius tent positivity](exact-weighted-selberg-symmetry-formula-and-signed-moebius-tent-positivity.md). Formalization in `SelbergTent.lean` of the exact weighted Selberg identity (`selberg_weighted_identity`), triangular tent specialization (`selberg_tent_identity`), and unconditional non-negativity of the signed Möbius tent sum (`selberg_moebius_tent_nonneg`).
158. [Exact prime primitive weighted energy contraction and dissipation identity](exact-prime-primitive-weighted-energy-contraction-and-dissipation-identity.md). Formalization in `PrimePrimitiveContraction.lean` of the right-differentiable integration by parts formula (`coarsePrefix_square_weighted_parts`), the exact energy dissipation identity (`primePrimitive_weighted_contraction_identity`), and the strict prime primitive energy contraction theorem (`primePrimitive_weighted_contraction`).
159. [Exact finite first Mellin moment of the prime error and primitive](exact-finite-first-mellin-moment-of-the-prime-error-and-primitive.md). Formalization in `PrimeFirstMoment.lean` of the exact finite first Mellin moment identity (`primeError_first_moment`), right-differentiable integration by parts formula (`coarsePrefix_first_moment`), and arithmetic primitive moment representation (`primePrimitive_first_moment`).
160. [Exact prime error primitive area and first-order Riesz representation](exact-prime-error-primitive-area-and-first-order-riesz-representation.md). Formalization in `PrimePrimitiveFormula.lean` of the Riesz mean equivalence (`primePrimitiveArea_eq_weighted_sum`), exact Abel step-function integration (`integral_psi_floor_eq_finite`), and exact prime error primitive integral (`integral_primeErrorReal_eq_area`).
161. [Exact hyperbola product grouping and sub-root Möbius annihilation](exact-hyperbola-product-grouping-and-sub-root-moebius-annihilation.md). Formalization in `HyperbolaProduct.lean` of the factor-pair hyperbola bijection (`sum_divisors_eq_sum_factor_pairs`), sub-root coefficient annihilation (`hyperbolaProductCoefficient_small`), and general weighted hyperbola identity (`hyperbolaProductCoefficient_weighted_sum`).
162. [Exact Mertens-to-prime-error hyperbola transfer and remainder bound](exact-mertens-to-prime-error-hyperbola-transfer-and-remainder-bound.md). Formalization in `HyperbolaMertens.lean` of centered discrete Abel summation (`centered_partial_summation`), small-quotient partial summation (`smallQuotientSigned_partial_summation`), and explicit Mertens transfer remainder bound (`primeError_Mertens_prefix_remainder`).
163. [Exact logarithmic-time prime filter dynamics and energy identity](exact-logarithmic-time-prime-filter-dynamics-and-energy-identity.md). Formalization in `PrimeLogFilter.lean` of linear causal filter ODE dynamics for logarithmic prime errors (`primeLogResponse_hasDerivWithinAt_right`) and exact Lyapunov energy dissipation identity (`primeLogFilter_energy_identity`).
164. [Exact reverse prime log filter energy identity and two-sided norm equivalence](exact-reverse-prime-log-filter-energy-identity-and-two-sided-norm-equivalence.md). Formalization in `PrimeLogFilterReverse.lean` of the exact sum-of-squares reverse energy identity (`primeLogFilter_reverse_energy_identity`), reverse coercivity bound (`primeLogFilter_reverse_energy_bound`), and two-sided norm equivalence (`primeLogFilter_energy_comparison`).
165. [Exact arithmetic function Möbius-Vaughan identity and Type I/II decomposition](exact-arithmetic-function-moebius-vaughan-identity-and-type-i-ii-decomposition.md). Formalization in `MobiusVaughan.lean` of the exact ring-theoretic Vaughan identity for $\mu$ (`moebius_vaughan_identity`), kernel compact support bounds (`vaughanSmallCoefficient_eq_zero_of_lt`), and weighted Type I / Type II sum decomposition (`moebius_vaughan_weighted_sum`).
166. [Exact causal filter energy dissipation identity and critical contraction](exact-causal-filter-energy-dissipation-identity-and-critical-contraction.md). Formalization in `CausalFilterEnergy.lean` of the right-differentiable cross-integral identity (`causalFilter_cross_integral`), general energy dissipation identity (`causalFilter_energy_identity`), and critical contraction theorem (`causalFilter_energy_contraction`).
167. [Exact arithmetic prime transport operator and spectral classification](exact-arithmetic-prime-transport-operator-and-spectral-classification.md). Formalization in `ArithmeticTransport.lean` of the prime transport operator, its exact conjugation to logarithmic multiplication (`zeta_mul_primeTransport`), complete point spectrum classification (`primeTransport_eigenvalues`), and Möbius kernel characterization (`primeTransport_kernel_iff`).
168. [Exact discrete prime energy balance and cross-correlation drift control](exact-discrete-prime-energy-balance-and-cross-correlation-drift-control.md). Formalization in `PrimeEnergy.lean` of the general weighted discrete integration-by-parts identity (`weighted_energy_balance`), exact prime energy balance (`prime_energy_balance`), explicit diagonal increment energy bound (`primeIncrementEnergy_le_log_square`), and conditional square-root bound from drift non-positivity (`prime_error_square_upper_of_drift_nonpos`).
169. [Exact autocorrelation smoothness and signed logarithmic prime packet duality](exact-autocorrelation-smoothness-and-signed-logarithmic-prime-packet-duality.md). Formalization in `AutocorrelationRegularity.lean` of the group convolution identity (`autocorrelation_eq_convolution`), $C^\infty_c$ regularity (`autocorrelation_contDiff`), boundary clamping (`source_packet_regular`), and signed prime packet duality (`actual_packet_signed_windows`).
170. [Exact prime-pair density causal filter evaluations and Riesz tent weights](exact-prime-pair-density-causal-filter-evaluations-and-riesz-tent-weights.md). Formalization in `PrimeDensityFilter.lean` of the exact causal filter evaluations of prime-power birth pairs (`prime_power_pair_evaluation`), symmetrized mixed prime-density convolutions (`prime_power_mixed_evaluation`), and background density autocorrelation (`density_square_normalized`).
171. [Exact finite Jensen-Schur positive kernel reduction and pointwise potential duality](exact-finite-jensen-schur-positive-kernel-reduction-and-pointwise-potential-duality.md). Formalization in `PositiveKernelCauchy.lean` of row-wise Cauchy inequalities (`kernel_row_cauchy`), real and complex Jensen-Schur positive kernel reductions (`positive_kernel_cauchy_reduction`), and pointwise potential certificate duality (`pointwise_weight_pays_port`).
172. [Exact centered distinct-prime tent Mellin transform and pole cancellation](exact-centered-distinct-prime-tent-mellin-transform-and-pole-cancellation.md). Formalization in `ActualCenteredMellin.lean` of the cutoff sum decomposition (`V_eq_N_sub_T`), real arithmetic representation (`V_eq_real_cutoff`), and regularized Mellin transform with complete pole cancellation at $s=1$ (`hasMellin_V`).
173. [Exact distinct-prime Riesz-Dirichlet Mellin representation and proper-power reindexing](exact-distinct-prime-riesz-dirichlet-mellin-representation-and-proper-power-reindexing.md). Formalization in `DistinctPrimeRieszMellin.lean` of full Mangoldt pair Dirichlet series (`fullPair_hasSum`), prime-power bijective reindexing (`primePowerLabel_injective`), same-prime series evaluation (`samePair_hasSum`), distinct-prime series (`distinctPair_hasSum`), and Riesz cutoff Mellin transform (`hasMellin_distinctPrime_cutoffSum`).
174. [Exact eta-Riesz-Dirichlet Fubini exchange and Mangoldt cross-term Mellin transform](exact-eta-riesz-dirichlet-fubini-exchange-and-mangoldt-cross-term-mellin-transform.md). Formalization in `EtaRieszDirichlet.lean` of the infinite-to-finite eta series reduction (`tsum_tents_eq_cutoff`), the general Fubini-justified Dirichlet Mellin exchange (`hasMellin_cutoffSum`), and the von Mangoldt cross-term Mellin transform (`hasMellin_crossTerm`).
175. [Exact Mellin transform of the continuous density baseline and parameter differentiation](exact-mellin-transform-of-the-continuous-density-baseline-and-parameter-differentiation.md). Formalization in `EtaBaselineMellin.lean` of the Heaviside step Mellin transform (`hasMellin_step`), logarithmic step transform via parameter differentiation (`hasMellin_log_step`), shifted powers (`hasMellin_cpow_step`), and the rational baseline transform (`hasMellin_B`).
176. [Exact quadratic eta-kernel parabolic decomposition and Mellin transform](exact-quadratic-eta-kernel-parabolic-decomposition-and-mellin-transform.md). Formalization in `EtaRieszKernel.lean` of the parabolic excess decomposition (`kernel_eq_tent_add_square`), algebraic factorization (`kernel_power_eq`), exact Mellin transform (`hasMellin_kernel`), and real $L^1$ norm integral (`integral_norm_kernel`).
177. [Exact double pole persistence and unremovable zeta-zero spectral singularities](exact-double-pole-persistence-and-unremovable-zeta-zero-spectral-singularities.md). Formalization in `LogDerivativePole.lean` of logarithmic derivative factorization (`logDeriv_of_factorization`), double pole persistence theorem (`double_pole_persistence`), finite order of zeta zeros (`zeta_zero_finite_order`), and double pole persistence at off-critical zeros (`actual_zeta_zero_double_pole`).
178. [Exact same-prime Dirichlet series analyticity and uniform Euler product convergence](exact-same-prime-dirichlet-series-analyticity-and-uniform-euler-product-convergence.md). Formalization in `SamePrimeDirichlet.lean` of the 2D prime-power majorant summability (`majorant_summable`), geometric summation of proper powers (`powerTerm_hasSum`), expanded-to-Euler equivalence (`expanded_eq_H`), uniform convergence (`expanded_uniformConvergence`), and analyticity on $\text{Re}(s) > 1/2$ (`H_analyticAt`).
179. [Exact Mertens-to-Chebyshev prime error transfer and quadratic harmonic majorants](exact-mertens-to-chebyshev-prime-error-transfer-and-quadratic-harmonic-majorants.md). Formalization in `MertensPrimeTransfer.lean` of the divisor harmonic bound (`divisorCard_harmonic_bound`), centered divisor harmonic bound (`centeredDivisorCoefficient_harmonic_bound`), and effective Mertens-to-Chebyshev transfer theorem (`primeError_bound_of_Mertens_quotients`).
180. [Exact discrete hyperbola cutoff decomposition and Chebyshev remainder bounds](exact-discrete-hyperbola-cutoff-decomposition-and-chebyshev-remainder-bounds.md). Formalization in `MertensCutoff.lean` of inductive reciprocal square root bound (`reciprocal_sqrt_sum_bound`), floor square root sum bound (`floor_sqrt_sum_bound`), large quotient remainder bound (`largeQuotientAbsolute_bound`), and cutoff decomposition remainder bound (`primeError_cutoff_remainder_bound`).
181. [Exact factorial-geometric Mertens convolution and quotient-floor square bounds](exact-factorial-geometric-mertens-convolution-and-quotient-floor-square-bounds.md). Formalization in `FactorialGeometricMertens.lean` of the quotient triangle duality (`quotient_triangle`), quotient-floor multiplicity bound (`quotient_floor_square_bound`), and geometric Mertens representation (`response_geometric`).
182. [Exact discrete Abel summation of Chebyshev errors and uniform signed first moments](exact-discrete-abel-summation-of-chebyshev-errors-and-uniform-signed-first-moments.md). Formalization in `PrimeSignedAverage.lean` of discrete Abel summation for prime errors (`integerError_partial_summation`), harmonic first moment identity (`signedPrimeErrorAverage_eq`), logarithmic prime mass bounds (`logarithmicPrimeMass_bounds`), and unconditional uniform bound (`signedPrimeErrorAverage_abs_le`).
183. [Exact reflected convolution transfer and prime log L2 stability](exact-reflected-convolution-transfer-and-prime-log-l2-stability.md). Formalization in `PrimeLogConvolutionTransfer.lean` of logarithmic prime error measurability (`primeLogError_measurable`), local $L^2$ integrability (`primeLogError_complex_memLp_two`, `primeLogResponse_complex_memLp_two`), and the reflected convolution transfer inequality (`primeLog_reflected_convolution_transfer`).
184. [Exact same-prime aggregation kernel positivity and discrete cell coercivity](exact-same-prime-aggregation-kernel-positivity-and-discrete-cell-coercivity.md). Formalization in `SamePrimeAggregationKernel.lean` of kernel increment formula (`F_succ_sub`), quotient lower bound (`quotient_ge`), increment coercivity (`F_succ_sub_ge_one`), discrete growth bound (`F_ge`), and continuum cell coercivity/positivity (`cell_kernel_pos`).
185. [Exact same-prime birth convolution and filtered partition multiplicity](exact-same-prime-birth-convolution-and-filtered-partition-multiplicity.md). Formalization in `SamePrimeBirthConvolution.lean` of continuous birth convolution (`birth_convolution`), causal filter on ramps (`filtered_ramp`), filtered birth convolution (`filtered_birth_convolution`), and partition multiplicity identity (`filtered_pairs_by_total`).
186. [Exact same-prime block calibration and proper-power multiplicity](exact-same-prime-block-calibration-and-proper-power-multiplicity.md). Formalization in `SamePrimeBlockCalibration.lean` of exponent partition summation identity (`sum_pairs_by_total`), moving cutoff freezing (`actual_convolution_eq_block`), filtered prime block autoconvolution (`filtered_primeBlock_by_total`), and master all-prime calibration (`actual_tau_calibration`).
187. [Exact same-prime history activation threshold and cell monotonicity](exact-same-prime-history-activation-threshold-and-cell-monotonicity.md). Formalization in `SamePrimeFirstSquare.lean` of the proper prime-power floor (`four_le_prime_power`), dead-time inactivity (`s_eq_zero_of_le_log_four`), exact activation criterion (`s_pos_iff`), and cell-by-cell strict monotonicity of renewal profiles (`H_strictMonoOn_cell`).
188. [Exact causal probability delay dissipation and same-prime residual bounds](exact-causal-probability-delay-dissipation-and-same-prime-residual-bounds.md). Formalization in `SamePrimeHistoryProbability.lean` of history measurability (`s_measurable`), causal delay integrability (`delayed_s_integrable`), probability averaging contraction (`delayed_s_integral_bounds`), and universal non-positivity of delay residuals (`samePrime_residual_bounds`).
189. [Exact same-prime history strict monotonicity and strict delay dissipation](exact-same-prime-history-strict-monotonicity-and-strict-delay-dissipation.md). Formalization in `SamePrimeStrictDelay.lean` of renewal profile strict monotonicity (`H_strictMonoOn`), history function strict monotonicity (`s_strictMonoOn`), strict delay loss (`s_delay_lt`), and strict causal delay dissipation (`samePrime_residual_neg`).
190. [Exact same-prime aggregation cell derivatives and global renewal monotonicity](exact-same-prime-aggregation-cell-derivatives-and-global-renewal-monotonicity.md). Formalization in `SamePrimeAggregationMonotone.lean` of tent normalization (`H_eq_normalized_tent`), cell profile identification (`H_eq_cell`), exact cell derivative (`cell_hasDerivAt`), intra-cell monotonicity (`cell_monotoneOn`), and global ray monotonicity (`H_monotoneOn`, `h_monotoneOn`).
191. [Exact prime seed square energy and geometric cell telescoping](exact-prime-seed-square-energy-and-geometric-cell-telescoping.md). Formalization in `PrimeSeedSquare.lean` of intra-cell square energy (`cell_square_integral`), infinite geometric telescoping (`hasSum_cell_square_integrals`), global line $L^2$ integrability (`integrable_seed_square_and_integral`), and exact prime seed energy evaluation $\int_\mathbb{R} \text{seed}_{\log p}^2 = 1/p^2$ (`integral_prime_seed_square`).
192. [Exact prime seed unweighted and half-weighted mass identities](exact-prime-seed-unweighted-and-half-weighted-mass-identities.md). Formalization in `PrimeSeedMass.lean` of clock-cell partition integration (`integral_of_clock_cells`), unweighted prime seed mass $2/p$ (`integral_prime_seed`), half-weighted prime seed Dirichlet mass $\frac{\log p}{p(p-1)}$ (`integral_prime_weighted_seed`), and power-floor equivalence (`seed_log_eq`).
193. [Exact finite-prime memory L2 regularity and successor commutator integral representation](exact-finite-prime-memory-l2-regularity-and-successor-commutator-integral-representation.md). Formalization in `PrimeSeedMemory.lean` of prime seed $L^2$ membership (`seed_memLp_two`), finite-prime family $L^2$ stability (`finitePrimeSeed_memLp_two`), and exact Lie-Volterra memory commutator integral representation (`finitePrimeSeed_memory_commutator`).
194. [Exact prime seed pointwise envelopes and explicit finite-prime square tail bounds](exact-prime-seed-pointwise-envelopes-and-explicit-finite-prime-square-tail-bounds.md). Formalization in `PrimeSeedPointwise.lean` of uniform exponential decay (`seed_le_exp_neg_half`), piecewise clock envelopes (`prime_seed_first_envelope`, `prime_seed_second_envelope`), finite prime log-square tail (`finite_prime_log_square_tail`), and explicit prime square tail bound (`finite_prime_square_tail`).
195. [Exact prime seed L1 divergence, L2 summability, and non-existence of integrable majorants](exact-prime-seed-l1-divergence-l2-summability-and-non-existence-of-integrable-majorants.md). Formalization in `PrimeSeedPrimeSums.lean` of total $L^1$ mass divergence (`not_summable_prime_seed_integrals`), individual $L^2$ summability (`summable_prime_seed_square_integrals`), finite aggregate mass (`integral_finset_prime_seed`), and non-existence of any integrable majorant (`no_integrable_majorant_prime_seeds`).
196. [Exact prime seed delay renewal equation and causal uniqueness theorem](exact-prime-seed-delay-renewal-equation-and-causal-uniqueness-theorem.md). Formalization in `PrimeSeedDelay.lean` of clock shift scaling (`seed_add_clock`), inhomogeneous delay equation (`seed_delay_equation`), finite complete-history unrolling (`seed_finite_history`), and unconditional causal uniqueness (`seed_unique`).
197. [Exact finite-prime Lévy jump measures and first-moment reconstruction](exact-finite-prime-levy-jump-measures-and-first-moment-reconstruction.md). Formalization in `PrimeSeedLevy.lean` of age-divided seed integrability (`integrable_seed_div_age`), finite-measure instance for Lévy jump measures (`levyMeasure_isFiniteMeasure`), pointwise age inversion (`age_mul_levyDensity`), and exact first-moment reconstruction formula (`integral_age_mul_levyDensity`).
198. [Exact prime seed jump discontinuities, derivative mass balance, and variation budget](exact-prime-seed-jump-discontinuities-derivative-mass-balance-and-variation-budget.md). Formalization in `PrimeSeedJumps.lean` of proper-power jump amplitude (`seed_power_jump`), jump negativity (`seed_power_jump_neg`), cell derivative (`seed_hasDerivAt_in_cell`), infinite jump summation (`hasSum_seed_negative_jumps`), derivative mass balance (`seed_derivative_mass_balance`), and total variation budget (`seed_derivative_variation_budget`).
199. [Exact all-prime jump process causal support and initial-gap theorem](exact-all-prime-jump-process-causal-support-and-initial-gap-theorem.md). Formalization in `PrimeSeedCausalSupport.lean` of prime seed lower inactivity (`seed_prime_eq_zero_before_log_two`), Lévy measure initial gap (`allPrimeLevyMeasure_initial_gap`), convolution power support gap (`jumpPower_initial_gap`), sub-threshold atomicity (`finiteJumpLaw_initial_region`), and strict causal non-negativity (`allPrimeProcess_no_negative_age`).
200. [Exact prime block continuous Chebyshev equivalence and bilinear source representation](exact-prime-block-continuous-chebyshev-equivalence-and-bilinear-source-representation.md). Formalization in `PrimeBlockSource.lean` of the Chebyshev prime-power double sum (`psi_eq_prime_power_sum`), age-floor equivalence (`birth_activation_iff`), single prime block power sum (`primeBlock_eq_power_sum`), the fundamental bridge theorem (`primeBlockSum_eq_psi`), and bilinear arithmetic source product decomposition (`source_product`).
201. [Exact prime seed signed derivative measure and weak derivative representation](exact-prime-seed-signed-derivative-measure-and-weak-derivative-representation.md). Formalization in `PrimeSeedDerivativeMeasure.lean` of the positive and negative derivative measures (`seedDerivativePositive_mass`, `seedDerivativeNegative_mass`), finite measure instances (`seedDerivativePositive_finite`, `seedDerivativeNegative_finite`), signed measure zero total mass (`seedDerivative_total_mass`), and weak derivative representation (`seedDerivative_represents_weak_derivative`).
202. [Exact prime seed derivative Jordan decomposition, mutual singularity, and total variation](exact-prime-seed-derivative-jordan-decomposition-mutual-singularity-and-total-variation.md). Formalization in `PrimeSeedDerivativeVariation.lean` of mutual singularity (`seedDerivative_mutuallySingular`), canonical Jordan decomposition (`seedDerivativeJordan`), total variation measure identity (`seedDerivative_totalVariation`), and closed-form total variation mass (`seedDerivative_totalVariation_mass`).
203. [Exact all-prime jump process finite-activity infinite-first-moment dichotomy](exact-all-prime-jump-process-finite-activity-infinite-first-moment-dichotomy.md). Formalization in `PrimeSeedInfiniteMean.lean` of singleton first-moment reconstruction (`singleton_levyMeasure_firstMoment`), all-prime Lévy measure first-moment divergence (`allPrimeLevyMeasure_firstMoment`), process first-moment divergence (`allPrimeProcess_firstMoment`), and age non-integrability (`not_integrable_allPrimeProcess_age`).
204. [Exact prime seed weak derivative identity and order-zero variation bound](exact-prime-seed-weak-derivative-identity-and-order-zero-variation-bound.md). Formalization in `PrimeSeedWeakDerivative.lean` of single-cell integration by parts (`seed_cell_weak_derivative`), global telescoping weak derivative formula (`seed_weak_derivative`), and order-zero operator bound (`seed_weak_derivative_bound`).
205. [Exact all-prime Lévy measure finiteness and pure-jump probability semigroup](exact-all-prime-levy-measure-finiteness-and-pure-jump-probability-semigroup.md). Formalization in `PrimeSeedAllPrimeLevy.lean` of prime jump bounds (`prime_levy_mass_le`), all-prime summability (`summable_prime_levy_masses`), finite jump measure (`allPrimeLevyMeasure_isFiniteMeasure`), continuous density identification (`allPrimeLevyMeasure_eq_withDensity`), and convolution probability semigroup (`allPrimeProcess_add`, `allPrimeProcess_original_series`).
206. [Exact prime seed L² strong cutoff convergence and uniform operator approximation](exact-prime-seed-l2-strong-cutoff-convergence-and-uniform-operator-approximation.md). Formalization in `PrimeSeedSquareConvergence.lean` of pointwise cutoff stabilization (`cutoff_seed_eventually_eq`), strong $L^2$ error convergence (`cutoff_seed_square_error_tendsto`), Cauchy-Schwarz memory bound (`cutoff_memory_error_bound`), and uniform operator convergence on the $L^2$ unit ball (`cutoff_memory_uniform_unit_ball`).
207. [Exact prime seed process cutoff total variation convergence and uniform rate](exact-prime-seed-process-cutoff-total-variation-convergence-and-uniform-rate.md). Formalization in `PrimeSeedCutoffConvergence.lean` of Lévy measure splitting (`levyMeasure_primesBelow_add_tail`), activity defect formula (`cutoff_mass_difference`), quantitative event error bound (`seedProcess_cutoff_event_bound`), and uniform total variation convergence (`seedProcess_cutoff_uniform`).
208. [Exact dyadic Chebyshev shell inverse-square bound and PNT-free prime summability](exact-dyadic-chebyshev-shell-inverse-square-bound-and-pnt-free-prime-summability.md). Formalization in `PrimeSeedActivityBound.lean` of prime log majorization (`sum_log_primes_le_psi`), inverse-square shell bound (`prime_shell_activity_le`), dyadic fiber bounds (`logFiber_weight_le`), and unconditional prime series summability without PNT (`summable_prime_reciprocal_mul_log`).
209. [Exact all-prime Lévy tail measure and explicit logarithmic activity bound](exact-all-prime-levy-tail-measure-and-explicit-logarithmic-activity-bound.md). Formalization in `PrimeSeedActivityTail.lean` of reciprocal square tail bounds (`reciprocal_square_tail_le`), dyadic shell tail sums (`dyadic_prime_activity_tail_le`), fiber bijection (`primeTailEquiv`), exact measure splitting (`allPrimeLevyMeasure_cutoff_split`), and explicit logarithmic bound $\nu_{\ge P}(\mathbb{R}) \le \frac{32}{\log P}$ (`primeTailLevyMeasure_mass_le_log`).
210. [Exact compound Poisson subordination principle and activity defect coupling bound](exact-compound-poisson-subordination-principle-and-activity-defect-coupling-bound.md). Formalization in `PrimeSeedLawComparison.lean` of convolution and jump power monotonicity (`conv_mono_of_finite`, `jumpPower_mono`), Poisson subordination (`finiteJumpLaw_domination`), sub-probability event coupling lemma (`probability_event_bound_of_domination`), and sharp activity defect bound (`finiteJumpLaw_event_bound`).
211. [Exact prime memory charge activation positivity and exponential decay](exact-prime-memory-charge-activation-positivity-and-exponential-decay.md). Formalization in `PrimeMemoryChargeDecay.lean` of kernel positivity beyond $\log 2$ (`kernel_nonneg_of_log_two_le`), thresholded memory decay (`memoryDefectKernel_nonneg_decay`), prime seed vanishing before $\log 2$ (`allPrimeSeed_eq_zero_before_log_two`), and all-prime memory defect positivity and exponential decay (`allPrimeSeed_defectKernel_nonneg_decay`).
212. [Exact charge-compensated successor operator and rank-one Hilbert adjoint](exact-charge-compensated-successor-operator-and-rank-one-hilbert-adjoint.md). Formalization in `SuccessorCompensatedL2.lean` of reference state $L^2$ regularity (`reference_memLp`), kernel $L^2$ regularity (`kernel_memLp`), compensated successor stability (`compensated_memLp`), rank-one perturbation adjoint (`compensatedAdjoint`, `compensatedAdjoint_memLp`), and exact $L^2$ duality (`compensated_adjoint`).
213. [Exact floor Abel kernel age derivative and singular boundary decomposition](exact-floor-abel-kernel-age-derivative-and-singular-boundary-decomposition.md). Formalization in `SuccessorFeedbackFloorAgeDerivative.lean` of weight error continuity (`floorWeightError_continuousAt`), moving primitive decomposition (`floorAbelError_eq_primitive_add_clipped`), Leibniz differentiation (`floorErrorPrimitive_hasDerivAt`), exact classical derivative (`floorAbelError_hasDerivAt`), and almost-everywhere differentiability on $(1, \infty)$ (`floorAbelError_hasDerivAt_ae`).
214. [Exact smooth Abel kernel finite-IBP representation and classical age differentiability](exact-smooth-abel-kernel-finite-ibp-representation-and-classical-age-differentiability.md). Formalization in `SuccessorFeedbackSmoothAgeDerivative.lean` of root primitive convolution finite reduction (`rootPrimitiveConvolution_eq_finite`), finite-interval IBP identity (`smoothAbelKernel_eq_rootPrimitive`), and classical age differentiability (`smoothAbelKernel_hasDerivAt`).
215. [Exact physical theta measure finiteness and Hilbert space dictionary](exact-physical-theta-measure-finiteness-and-hilbert-space-dictionary.md). Formalization in `PhysicalThetaMeasure.lean` of hyperbolic cosine weight representation (`b_eq_two_cosh`), global integrability (`weight_integrable`), finite measure space (`nu_finite`), mutual absolute continuity (`nu_absolutelyContinuous`, `volume_absolutelyContinuous`), and physical Hilbert space dictionary (`physical_mass_memLp_dictionary`).
216. [Exact modular Gaussian seed differential identity and Riemann Xi kernel recovery](exact-modular-gaussian-seed-differential-identity-and-riemann-xi-kernel-recovery.md). Formalization in `PhysicalThetaSeed.lean` of Gaussian seed derivatives (`seed_hasDerivAt`, `slope_hasDerivAt`), exact summand differential identity (`differential_identity`), absolute series summability (`seed_summable`, `curvature_summable`), and global Xi kernel recovery (`curvature_tsum_minus_quarter_seed`).
217. [Exact non-local physical theta jump energy subadditivity and submodule form domain](exact-non-local-physical-theta-jump-energy-subadditivity-and-submodule-form-domain.md). Formalization in `PhysicalThetaJumpAddition.lean` of two-point quadratic integrand bound (`integrand_add_bound`), continuous and prime energy subadditivity (`continuous_energy_add_bound`, `prime_energy_add_bound`), exterior energy bound (`exterior_hilbert_energy_add_bound`), domain closure (`maximal_exterior_domain_add`), and submodule form domain construction (`exteriorFormDomain`).
218. [Exact physical continuous arrival kernel near-origin hyperbolic bound and logarithmic integral](exact-physical-continuous-arrival-kernel-near-origin-hyperbolic-bound-and-logarithmic-integral.md). Formalization in `PhysicalContinuousArrival.lean` of rational denominator lower bound (`denominator_lower`), hyperbolic bound on $(0, 1]$ (`kernel_near_bound`), exact truncated logarithmic integral (`truncated_boundary_majorant_integral`, `truncated_kernel_integral_bound`), and far-field integrability (`kernel_far_integrable`).
219. [Exact complete core arrival operator regularity and closed-interval L² stability](exact-complete-core-arrival-operator-regularity-and-closed-interval-l2-stability.md). Formalization in `PhysicalContinuousArrivalMass.lean` of continuous arrival tail mass bound (`tail_mass_bound`), core majorant bound (`continuous_arrival_core_bound`), open and closed interval $L^2$ regularity (`continuous_arrival_core_memLp_two`, `continuous_arrival_closed_core_memLp_two`), and complete arrival $L^2$ stability (`complete_core_arrival_memLp_two`, `complete_core_arrival_square_integrable`).
220. [Exact spectral logarithmic mass envelope differentiation and strict monotonicity](exact-spectral-logarithmic-mass-envelope-differentiation-and-strict-monotonicity.md). Formalization in `LogarithmicMassEnvelope.lean` of the exact scale derivative (`hasDerivAt_envelope`), strict derivative positivity (`envelope_derivative_pos`), strict monotonicity on $(0, \infty)$ (`envelope_strictMonoOn`), and scale monotonicity (`envelope_le`).
221. [Exact unconditional Fourier-spectral envelope for frozen arithmetic sources](exact-unconditional-fourier-spectral-envelope-for-frozen-arithmetic-sources.md). Formalization in `ChargeFrozenSpectralBound.lean` of critical line Mellin modulus (`norm_mellin_atom`), prime sum bound (`norm_prime_sum_le`), continuous density integral bound (`norm_density_integral_le`), numerator bound (`norm_mellin_numerator_le`), and universal quadratic Fourier-spectral decay envelope (`fourier_norm_sq_le`).
222. [Exact frozen arithmetic source logarithmic-moment finiteness and layer-cake bound](exact-frozen-arithmetic-source-logarithmic-moment-finiteness-and-layer-cake-bound.md). Formalization in `ChargeFrozenLogMoment.lean` of whole-frequency density measurability (`measurable_hat`, `measurable_density`), global $L^1$ integrability (`integrable_density`), logarithmic moment finiteness, and non-asymptotic layer-cake bound (`actual_log_moment`).
223. [Exact causal arithmetic source exponential majorant and global Fourier integrability](exact-causal-arithmetic-source-exponential-majorant-and-global-fourier-integrability.md). Formalization in `ChargeFrozenIntegrability.lean` of the exponential decay majorant (`source_abs_le`), global $L^1$ Bochner integrability (`integrable_causalSource`), and global Fourier integrand integrability across all frequencies (`integrable_fourier_integrand`).
224. [Exact charge density product measure Fubini interchange and continuum formula](exact-charge-density-product-measure-fubini-interchange-and-continuum-formula.md). Formalization in `ChargeDensityFubini.lean` of the spatial fiber integral (`betaReal_integral`), joint product measure integrability (`original_density_joint_integrable`), defect operator Fubini interchange (`original_density_charge_interchange`), and continuum representation formula (`original_density_charge_formula`).
225. [Exact prime atom defect charge and reference closed forms](exact-prime-atom-defect-charge-and-reference-closed-forms.md). Formalization in `ChargeOriginalBirth.lean` of pullback formulas (`beta_eq_birthPull`, `successor_beta_eq_birthPull`), prime atom defect identity (`original_defect_beta`), weighted summability (`original_weighted_atom_charge_summable`), and reference closed forms (`original_defect_reference`, `original_defect_first_birth`).
226. [Exact prime wave packet scattering amplification and null-pole smooth carrier](exact-prime-wave-packet-scattering-amplification-and-null-pole-smooth-carrier.md). Formalization in `CompactScatteringOperator.lean` of wave packet translation commutation (`packet_translate`, `history_packet`), disjoint packet $L^2$ isometry (`packet_physical_square`), phase-box bulk lower bound (`phase_box_history_bulk_bound`), prime wave packet amplification (`exists_prime_amplifying_packet`), and amplification on smooth null-pole carriers (`exists_prime_amplifying_narrow_carrier`).
227. [Exact smooth compactly supported null-pole energy amplification under prime scattering](exact-smooth-compactly-supported-null-pole-energy-amplification-under-prime-scattering.md). Formalization in `CompactScatteringRegularity.lean` of wave packet smoothness (`packet_contDiff`), compact support (`packet_compact`), complex moment vanishing (`packet_moment_zero`), and prime energy amplification on smooth null-pole wavelets (`exists_prime_smooth_pole_null_amplification`).
228. [Exact coarse dyadic Mellin compact geometric majorant and half-plane analyticity](exact-coarse-dyadic-mellin-compact-geometric-majorant-and-half-plane-analyticity.md). Formalization in `CoarseContinuation.lean` of the compact geometric majorant (`coarseMellinBlock_compact_majorant_of_bound`), locally uniform convergence (`coarseMellinBlock_tendstoLocallyUniformlyOn`), and critical half-plane analyticity (`coarseMellinSum_analytic`).
229. [Exact dyadic prime error Mellin series and Riemann zeta logarithmic derivative identity](exact-dyadic-prime-error-mellin-series-and-riemann-zeta-logarithmic-derivative-identity.md). Formalization in `CoarseInitialIdentity.lean` of unconditional kernel integrability (`primePsi_kernel_integrable_unconditional`, `primeErrorReal_kernel_integrable_unconditional`), dyadic partition summation (`coarseMellinBlock_hasSum_integral`), and the pole-subtracted logarithmic derivative identity (`coarseMellinSum_eq_logDerivative`).
230. [Exact coarse primitive energy growth criterion for the Riemann hypothesis](exact-coarse-primitive-energy-growth-criterion-for-the-riemann-hypothesis.md). Formalization in `CoarsePrimitiveCriterion.lean` of the holomorphic prime extension construction (`primeExtension_of_coarsePrimitiveBound`) and the formal implication of Mathlib's full Riemann Hypothesis statement `Target` from the coarse primitive energy bound (`target_of_coarsePrimitiveBound`).
231. [Exact Plancherel identity for frozen arithmetic sources and terminal error decomposition](exact-plancherel-identity-for-frozen-arithmetic-sources-and-terminal-error-decomposition.md). Formalization in `ChargeFrozenPlancherel.lean` of the autocorrelation transform identity (`transform_complexCorrelation`), exact real-line Plancherel formula (`mass_eq_physical_norm`), prefix and terminal error decomposition (`mass_eq_prefix_terminal`), and physical logarithmic moment bound (`actual_log_moment_physical`).
232. [Exact coercive lower bound and terminal energy splitting in logarithmic metric](exact-coercive-lower-bound-and-terminal-energy-splitting-in-logarithmic-metric.md). Formalization in `ChargeFrozenMetric.lean` of the exact tail formula (`causalSource_terminal`), tail square integral evaluation (`terminal_square_integral`), prefix/terminal domain splitting (`full_square_integral_split`), and the coercive lower bound (`full_square_integral_lower_bound`).
233. [Exact arithmetic source autocorrelation regularity and origin continuity under discontinuous atoms](exact-arithmetic-source-autocorrelation-regularity-and-origin-continuity-under-discontinuous-atoms.md). Formalization in `ChargeFrozenFourierNorm.lean` of source $L^2$ membership (`memLp_complexSource`), almost-everywhere continuity (`ae_continuous_causalSource`), origin continuity via dominated convergence (`continuousAt_autocorrelation_zero`), and global integrability (`integrable_autocorrelation`).
234. [Exact causal source Fourier-Mellin representation and critical Dirichlet discrepancy](exact-causal-source-fourier-mellin-representation-and-critical-dirichlet-discrepancy.md). Formalization in `ChargeFrozenFourier.lean` of the atomic and centering decomposition (`causalSource_eq_atoms_sub_centering`), prime atom Fourier transform (`beta_fourier_integral`), centering Fourier transform (`centering_fourier_integral`), and grand closed-form Fourier-Mellin formula (`causalSource_fourier_mellin`).
235. [Exact prime birth increment Gram matrix and scale-decoupled factorization](exact-prime-birth-increment-gram-matrix-and-scale-decoupled-factorization.md). Formalization in `ChargeFrozenBirthGram.lean` of Gram cross-product integrability (`birth_gram_integrable`), ramp and terminal product formulas (`birth_gram_ramp`, `birth_gram_terminal`), and exact closed-form inner product evaluation (`birth_gram_integral`).
236. [Exact prime birth increment self-energy and transitional ramp integral](exact-prime-birth-increment-self-energy-and-transitional-ramp-integral.md). Formalization in `ChargeFrozenBirthMetric.lean` of the three-regime piecewise increment formula (`increment_piecewise`), closed-form ramp integral evaluation (`ramp_square_integral`), and total evaluated self-energy formula (`increment_square_integral_evaluated`).
237. [Exact arithmetic source birth work and energy increment formula](exact-arithmetic-source-birth-work-and-energy-increment-formula.md). Formalization in `ChargeFrozenBirthWork.lean` of source-increment cross integrability (`source_increment_integrable`), exact prime birth work formula (`source_increment_integral`), and exact step-by-step energy increment formula (`full_square_integral_increment`).
238. [Exact finite prime birth Gram kernel and unconditional positive semi-definiteness](exact-finite-prime-birth-gram-kernel-and-unconditional-positive-semidefiniteness.md). Formalization in `ChargeFrozenBirthFiniteGram.lean` of the arithmetic Gram kernel identity (`birth_gram_eq_kernel`), finite energy quadratic expansion (`finite_energy_eq_kernel`), and unconditional positive semi-definiteness (`finite_kernel_quadratic_nonnegative`).
239. [Exact unconditional Cauchy-Lorentz spectral envelope for arithmetic sources](exact-unconditional-cauchy-lorentz-spectral-envelope-for-arithmetic-sources.md). Formalization in `ChargeFrozenSpectralBound.lean` of critical Mellin atom modulus (`norm_mellin_atom`), Dirichlet polynomial modulus bound (`norm_prime_sum_le`), continuous density integral bound (`norm_density_integral_le`), and the grand uniform Cauchy-Lorentz spectral envelope (`fourier_norm_sq_le`).
240. [Exact whole-frequency logarithmic moment bound for actual arithmetic sources](exact-whole-frequency-logarithmic-moment-bound-for-actual-arithmetic-sources.md). Formalization in `ChargeFrozenLogMoment.lean` of the measurability of the arithmetic Fourier transform (`measurable_hat`), global spectral integrability (`integrable_density`), and the grand whole-frequency logarithmic moment bound (`actual_log_moment`).
241. [Exact charge density defect and background growth annihilation](exact-charge-density-defect-and-background-growth-annihilation.md). Formalization in `ChargeDensityEvaluation.lean` of the successor shift identity (`successor_cutoff`), background growth annihilation (`original_defect_growth`), and exact density defect formula (`original_density_defect`).
242. [Exact operator defect of prime birth atoms and weighted summability](exact-operator-defect-of-prime-birth-atoms-and-weighted-summability.md). Formalization in `ChargeOriginalBirth.lean` of prime step-atom pull-back (`beta_eq_birthPull`), successor index advancement (`successor_beta_eq_birthPull`), exact operator defect identity (`original_defect_beta`), and absolute weighted summability (`original_weighted_atom_charge_summable`).
243. [Exact global cubic arcsine sandwich inequality](exact-global-cubic-arcsine-sandwich-inequality.md). Formalization in `ChargeArcsinBound.lean` of the quadratic derivative estimate (`derivative_bounds`) and the grand non-asymptotic global cubic sandwich inequality (`arcsin_sub_bounds`).
244. [Exact prime birth discrete correction decay and absolute summability](exact-prime-birth-discrete-correction-decay-and-absolute-summability.md). Formalization in `ChargeBirthSummability.lean` of non-asymptotic decay bound (`correction_bounds`), discrete difference identification (`correction_eq_discrete_difference`), and von Mangoldt absolute summability (`weighted_correction_norm_summable`).
245. [Exact dyadic coarse Mellin block energy bound and geometric decay](exact-dyadic-coarse-mellin-block-energy-bound-and-geometric-decay.md). Formalization in `CoarseDyadicBound.lean` of localized dyadic block energy bound (`coarsePrime_mellin_norm_le`), negative power exponent (`coarseMellinBlock_power_bound`), and the grand uniform geometric decay theorem (`coarseMellinBlock_geometric_bound`).
246. [Exact entirety and global analyticity of arithmetic dyadic Mellin blocks](exact-entirety-and-global-analyticity-of-arithmetic-dyadic-mellin-blocks.md). Formalization in `CoarseBlockAnalytic.lean` of the entirety of finite prime error Mellin integrals (`coarsePrime_finiteMellin_differentiable`) and the global analyticity of dyadic Mellin blocks on $\mathbb{C}$ (`coarseMellinBlock_analytic`).
247. [Exact coarse primitive energy criterion and half-plane analytic continuation](exact-coarse-primitive-energy-criterion-and-half-plane-analytic-continuation.md). Formalization in `CoarseContinuation.lean` of summable compact majorants (`coarseMellinBlock_compact_majorant_of_bound`), locally uniform convergence (`coarseMellinBlock_tendstoLocallyUniformlyOn`), and the grand half-plane analyticity theorem (`coarseMellinSum_analytic`).
248. [Exact dyadic coarse Mellin zeta logarithmic derivative identity](exact-dyadic-coarse-mellin-zeta-logarithmic-derivative-identity.md). Formalization in `CoarseInitialIdentity.lean` of kernel integrability (`primeErrorReal_kernel_integrable_unconditional`), dyadic partition sum (`coarseMellinBlock_hasSum_integral`), and the grand log-derivative identity with `riemannZeta` (`coarseMellinSum_eq_logDerivative`).
249. [Exact coarse primitive energy sufficiency criterion for the Riemann hypothesis](exact-coarse-primitive-energy-sufficiency-criterion-for-the-riemann-hypothesis.md). Formalization in `CoarsePrimitiveCriterion.lean` of the holomorphic prime extension from coarse bounds (`primeExtension_of_coarsePrimitiveBound`) and the grand conditional sufficiency theorem for Mathlib's native RH (`target_of_coarsePrimitiveBound`).
250. [Exact Chebyshev linear prime bound and zeta log-derivative integral representation](exact-chebyshev-linear-prime-bound-and-zeta-log-derivative-integral-representation.md). Formalization in `CoarsePrimeBounds.lean` of Legendre's formula (`logFactorial_eq_weighted_prime_sum`), Chebyshev's linear bound (`psi_le_linear`), and the zeta log-derivative representation (`zeta_logDerivative_eq_prime_integral_unconditional`).
251. [Exact geometric dyadic block summation and half-plane analyticity theorem](exact-geometric-dyadic-block-summation-and-half-plane-analyticity-theorem.md). Formalization in `CoarseSummation.lean` of the abstract compact majorant extraction (`coarseMellinBlock_compact_majorant`), locally uniform convergence (`coarseMellinBlock_tendstoLocallyUniformlyOn_of_geometric`), and half-plane analyticity transfer (`coarseMellinSum_analytic_of_geometric`).
252. [Exact coarse primitive energy terminal mass and test function estimates](exact-coarse-primitive-energy-terminal-mass-and-test-function-estimates.md). Formalization in `CoarsePrimitive.lean` of terminal mass control (`coarse_terminal_mass_sq_le`), primitive Cauchy-Schwarz (`coarsePrefix_abs_integral_sq_le`), step integration by parts (`coarsePrime_summation_by_parts`), and the master test-function bound (`coarsePrime_test_bound`).
253. [Exact polygonal coarse energy integral and discrete vertex sandwich bounds](exact-polygonal-coarse-energy-integral-and-discrete-vertex-sandwich-bounds.md). Formalization in `PolygonalCoarseEnergy.lean` of the exact continuous cell integral (`cellEnergy_integral`), optimal lower/upper vertex bounds (`cellEnergy_lower`, `cellEnergy_upper`), and weighted partition bounds (`polygonal_energy_bounds`).
254. [Exact signed Abel prime discrepancy and successor dilation row bounds](exact-signed-abel-prime-discrepancy-and-successor-dilation-row-bounds.md). Formalization in `SuccessorCoarseParentSignedAbel.lean` of the exact discrete Abel identity (`actual_prime_discrepancy_abel`), sharp monotone envelope bound (`actual_prime_discrepancy_uniform_bound`), quotient dilation identification (`child_dilations_eq_quotient_Ioc`), and row bound (`child_dilation_signed_inv_sqrt_bound`).
255. [Exact Fubini defect interchange and causal charge density integrability](exact-fubini-defect-interchange-and-causal-charge-density-integrability.md). Formalization in `ChargeDensityFubini.lean` of 2D product measurability and integrability (`original_density_joint_integrable`), Fubini defect interchange (`original_density_charge_interchange`), and the continuum charge defect formula (`original_density_charge_formula`).
256. [Exact causal renewal cell increment factorization and positivity](exact-causal-renewal-cell-increment-factorization-and-positivity.md). Formalization in `CausalRenewalFiniteCells.lean` of logarithmic-mean factorization (`rawCellIncrement_eq_cellIncrement`), cubic gap comparison (`sqrt_succ_cube_lt_three`), and square-root cell positivity (`sqrt_cellIncrement_pos_of_euler_bounds`).
257. [Exact entire Riemann xi factorization and analytic vanishing orders](exact-entire-riemann-xi-factorization-and-analytic-vanishing-orders.md). Formalization in `ActualArchimedeanFactor.lean` of the entirety and functional equation of $\xi(s)$ (`xi_entire`, `xi_functional_equation`), zero order equality (`xi_zeta_zero_order`), and logarithmic derivative residue limits (`analytic_log_derivative_order`, `zeta_log_derivative_order`).
258. [Exact centered distinct-prime Riesz-Mellin representation](exact-centered-distinct-prime-riesz-mellin-representation.md). Formalization in `ActualCenteredMellin.lean` of the centered tent decomposition (`V_eq_N_sub_T`), explicit cutoff formula (`V_eq_real_cutoff`), and the grand Mellin transform identity on $\text{Re}(s) > 1$ (`hasMellin_V`).
259. [Exact compact prime-error heat source Laplace identity and neutral charge](exact-compact-prime-error-heat-source-laplace-identity-and-neutral-charge.md). Formalization in `ActualCompactHeatEndpoint.lean` of the Goldbach packet comparison (`compactHeat_eq_goldbach_packet`), Laplace error integral (`compactHeat_eq_error_integral`), and neutral charge theorem (`compactHeat_at_zero`).
260. [Exact completed zeta circle pole cancellation and xi contour equivalence](exact-completed-zeta-circle-pole-cancellation-and-xi-contour-equivalence.md). Formalization in `CompletedZetaPoleCorrections.lean` of logarithmic derivative pole corrections (`completed_log_derivative_corrections`), circle contour correction formula (`entire_completed_circle_corrections`), and grand pole cancellation (`pole_null_completed_circle`).
261. [Exact critical transform double-pole obstruction and conditional Riemann hypothesis](exact-critical-transform-double-pole-obstruction-and-conditional-riemann-hypothesis.md). Formalization in `CriticalTransformRH.lean` and `CriticalTransformPoles.lean` of the order $-2$ pole at hypothetical zeros (`filled_pole_at_zeta_zero`), elimination of right-half zeros (`noRightZeros_of_analyticOnNhd_filledTransform`), and conditional sufficiency for Mathlib's `RiemannHypothesis` (`RiemannHypothesis_of_analyticOnNhd_filledTransform`).
262. [Exact real-axis zeta negativity and critical transform analyticity](exact-real-axis-zeta-negativity-and-critical-transform-analyticity.md). Formalization in `CriticalTransformRealAxis.lean` of fractional-part Mellin non-negativity (`floorMellin_re_nonneg`), real-axis zeta negativity on $(0, 1)$ (`riemannZeta_re_neg`), non-vanishing on $(0, \infty) \setminus \{1\}$ (`riemannZeta_ne_zero_real`), and real-axis analyticity of `filledTransform` (`analyticAt_filledTransform_real`).
263. [Exact centered Poisson covariance kernel geometric tail identity and positivity](exact-centered-poisson-covariance-kernel-geometric-tail-identity-and-positivity.md). Formalization in `ActualCenteredPoissonKernel.lean` of the shifted geometric tail factorization (`centeredKernel_eq_tail`) and entrywise non-negativity on $[0, 1]$ (`centeredKernel_nonneg`).
264. [Exact two-bump Hermitian Gram lower bound and strict positivity](exact-two-bump-hermitian-gram-lower-bound-and-strict-positivity.md). Formalization in `ActualAnnulusGramAlgebra.lean` of the exact Hermitian Gram lower bound (`signed_gram_lower`) and strict positivity under spectral margin (`signed_gram_pos`).
265. [Exact compensated causal memory commutator and relative charge integrability](exact-compensated-causal-memory-commutator-and-relative-charge-integrability.md). Formalization in `CompensatedMemoryKernel.lean` of the uniform memory bound (`causalMemory_bound`), defect charge integrability (`integrable_memory_defect`), and the exact compensated commutator identity (`compensated_memory_commutator`).
266. [Exact arithmetic correlation support cutoff and prime-power bijection](exact-arithmetic-correlation-support-cutoff-and-prime-power-bijection.md). Formalization in `CompleteArithmeticCorrelation.lean` of the von Mangoldt prime power bijection (`weighted_von_mangoldt_prime_powers`), correlation support width vanishing (`correlation_zero_of_width`), finite truncation equivalence (`arithmetic_row_eq_cutoff`), and prime-power representation (`arithmetic_cutoff_eq_prime_powers`).
267. [Exact critical exponential coordinate isometry and boundary contact form reconstruction](exact-critical-exponential-coordinate-isometry-and-boundary-contact-form-reconstruction.md). Formalization in `CriticalCoordinatePairing.lean` of the critical half-density coordinate isometry (`physicalLift_pair_integral`), successor/predecessor shift intertwining (`critical_successor_pair`, `critical_predecessor_pair`), and the boundary contact form reconstruction (`critical_contact_identity`).
268. [Exact biorthogonal constraint projection and cubic Hardy obstruction](exact-biorthogonal-constraint-projection-and-cubic-hardy-obstruction.md). Formalization in `ConstrainedHardyObstruction.lean` of the simultaneous biorthogonal constraint kernel projection (`constraintCorrected_mem_kernel`), cubic-vs-quadratic scaling dominance (`cubic_weight_beats_quadratic_energy`), and finite correction failure (`corrected_packet_failure`).
269. [Exact general index contraction recovery and quotient difference transfer](exact-general-index-contraction-recovery-and-quotient-difference-transfer.md). Formalization in `ContractionRecovery.lean` of the general index contraction recovery bound (`contraction_recovery_bound`), asymptotic difference transfer (`contraction_recovery_tendsto`), and quotient difference recovery (`quotient_difference_recovery`).
270. [Exact compact scattering packet energy identity and orthogonal translate isometry](exact-compact-scattering-packet-energy-identity-and-orthogonal-translate-isometry.md). Formalization in `CompactScatteringPackets.lean` of the geometric packet energy identity (`isolated_packet_energy`), bulk gain threshold (`exists_finite_bulk_gain`), translate support disjointness (`separated_translate_values`), and the exact $L^2$ translate isometry (`physical_translate_sum_square`).
271. [Exact conditional score disagreement penalty and bilinear Schur residual](exact-conditional-score-disagreement-penalty-and-bilinear-schur-residual.md). Formalization in `ConditionalScoreQuadratic.lean` of the exact scalar score coupling penalty (`separate_penalty`), joint minimum monotonicity (`separate_minimum_le_joint`), bilinear Schur residual semidefiniteness (`scoreResidual_nonnegative`), and additive Schur decomposition (`joint_form_penalty`).
272. [Exact finite scattering box history coefficients and bulk prime amplification](exact-finite-scattering-box-history-coefficients-and-bulk-prime-amplification.md). Formalization in `CompactScatteringCoefficients.lean` of the interior history formula (`history_box_interior`), phase circle factorization (`history_phase_box_interior`), bulk intensity invariance (`history_phase_box_normSq`), and unconditional bulk prime amplification (`prime_interior_amplification`).
273. [Exact core-port generalized eigenvalue domination and Young complement certificate](exact-core-port-generalized-eigenvalue-domination-and-young-complement-certificate.md). Formalization in `CorePortGeneralizedEigen.lean` of the generalized Rayleigh quotient equivalence (`generalizedRayleigh_le_one_iff`), eigenvalue domination certificate (`generalizedEigenvalue_certificate`), failure coordinate witness (`compressed_failure_witness`), and trial-complement Young certificate (`trial_complement_young_certificate`).
274. [Exact finite prime-cross Laplace factorization and zeta logarithmic derivative convergence](exact-finite-prime-cross-laplace-factorization-and-zeta-logarithmic-derivative-convergence.md). Formalization in `CriticalChirpedLaplaceFinite.lean` of the exact finite Laplace factorization (`actual_cross_laplace_dirichlet_finite`), prime Dirichlet polynomial convergence (`prime_dirichlet_polynomial_tendsto_zeta`), and the grand finite Laplace limit theorem (`finite_cross_laplace_tendsto_logderiv`).
275. [Exact two-frequency cancellation and Sylvester Gram criterion](exact-two-frequency-cancellation-and-sylvester-gram-criterion.md). Formalization in `CollectiveLambertGramFinite.lean` of the two-frequency cancellation identity (`two_frequency_cancellation_identity`), cancellation upper bound (`two_frequency_cancellation_upper`), and the Sylvester $2 \times 2$ non-negativity criterion (`two_by_two_nonneg_of_det`).
276. [Exact signed three-part covariance contraction and inner difference identity](exact-signed-three-part-covariance-contraction-and-inner-difference-identity.md). Formalization in `CollectiveSourceRangeFinite.lean` of linear preimage conservation (`signed_source_preimage`), exact three-part spectral split commutation (`signed_three_part_contraction`), and inner product difference identity (`inner_difference_identity`).
277. [Exact orthogonal feasible minimum norm and scalar Schur bifurcation](exact-orthogonal-feasible-minimum-norm-and-scalar-schur-bifurcation.md). Formalization in `CollectiveSourceGalerkinFinite.lean` of the orthogonal feasible minimum norm (`orthogonal_feasible_is_minimum`), energy cap equivalence (`feasible_energy_cap_iff_minimum`), pseudoinverse minimality (`pseudoinverse_lift_is_minimum`), nonsingular Schur equivalence (`positive_scalar_schur_iff`), and singular Schur bifurcation (`zero_scalar_schur_iff`).
278. [Exact canonical Bessel cell observation and four-constraint duality](exact-canonical-bessel-cell-observation-and-four-constraint-duality.md). Formalization in `CanonicalFiniteObservation.lean` of the finite Bessel cell observation inequality (`canonical_cell_observation_le`), four real constraints equivalence (`four_real_constraints_iff_scalar`), and affine optimizer completion of squares (`affine_optimizer_identity`).
279. [Exact finite successor-cell dilation semigroup and collar weight densities](exact-finite-successor-cell-dilation-semigroup-and-collar-weight-densities.md). Formalization in `SuccessorCellTransferFinite.lean` of the finite cutoff semigroup composition law (`transfer_mul`), dilation fiber partition (`div_eq_iff_child`), logarithmic cell telescoping (`sum_log_cell_length`), uniform carry bound (`log_successor_carry_bound`), phase perturbation bound (`norm_exp_successor_carry_le`), and exact triangular collar overlap weights (`collarWeight_lower_band`, `collarWeight_upper_band`, `collarWeight_zero_outside`).
280. [Exact regular source Möbius floor-inverse obstruction via Banach-Steinhaus](exact-regular-source-mobius-floor-inverse-obstruction-via-banach-steinhaus.md). Formalization in `RegularSource.lean` of the reciprocal square sum bound (`reciprocal_square_sum_Icc_bound`), squarefree lower density on terminal half intervals (`squarefree_terminal_half_lower`), non-uniform boundedness of evaluation functionals (`regularMobiusFunctional_not_uniformly_bounded`), and the Banach-Steinhaus counterexample existence theorem (`exists_fixed_regular_source_with_unbounded_inverse`).
281. [Exact dual fractional-approximation L² lower bound and Mertens obstruction](exact-dual-fractional-approximation-l2-lower-bound-and-mertens-obstruction.md). Formalization in `FractionalApproximation.lean` of the quotient coefficient sum (`quotient_coefficient_sum`), periodic reciprocal error mean (`reciprocalError_mean_zero_one`), Jacobian transformation on shrinking window (`dualWindowEnergy_eq_weighted`), unconditional $L^2$ error lower bound (`dualMeanSquareError_lower`), Mertens normalized limit (`mertens_sq_normalized_tendsto_of_dual_error`), and conditional route exclusion (`dual_error_not_tendsto_of_mertens_oscillation`).
282. [Exact logarithmic prime window residual and autocorrelation Mellin factorization](exact-logarithmic-prime-window-residual-and-autocorrelation-mellin-factorization.md). Formalization in `LogarithmicPrimeWindow.lean` of the window derivative bridge (`window_hasDerivAt`), exponential interval substitution (`exp_interval_substitution`), logarithmic prime window residual identity (`window_residual_frozen_source`), autocorrelation density moment factorization (`densityMoment_autocorrelation`), autocorrelation residual identity (`autocorrelation_residual_frozen_source`), and multi-window signed assembly (`signed_windows_frozen_source`).
283. [Exact compact-strip theta payment and explicit rational core margin](exact-compact-strip-theta-payment-and-explicit-rational-core-margin.md). Formalization in `ThetaCompactStripRationalCore.lean` of the order-theoretic compact payment principle (`compact_payment`), Schur kernel bound (`schur_at_epsilonOf`), explicit numerical payment margin (`explicit_payment_margin`), logarithmic arrival payment (`explicit_arrival_payment`), coarse resolvent operator bound (`explicit_resolvent_bound`), and aggregated strip loss lower bounds (`aggregate_strip_lower`, `explicit_strip_lower`).
284. [Exact local scattering frequency bifurcation and finite-history non-contraction](exact-local-scattering-frequency-bifurcation-and-finite-history-non-contraction.md). Formalization in `CompactLocalScatteringSigns.lean` of the finite-history numerator identity (`finite_history_numerator`), phase factorization (`compact_phase_factorization`), prime phase attenuation and amplification (`actual_prime_compact_signs`), and physical frequency non-contraction theorem (`original_prime_compact_signs`).
285. [Exact same-prime Dirichlet series and half-plane holomorphy](exact-same-prime-dirichlet-series-and-half-plane-holomorphy.md). Formalization in `SamePrimeDirichlet.lean` of prime majorant summability (`primeMajorant_summable`), geometric power summation (`powerTerm_hasSum`), double-sum rearrangement (`expanded_eq_H`), uniform majorant control (`uniform_majorant`), locally uniform convergence (`expanded_uniformConvergence`), and half-plane holomorphy (`H_differentiableOn`, `H_analyticAt`).
286. [Exact Goldbach three-phase Fourier reconstruction of Chebyshev prime error](exact-goldbach-three-phase-fourier-reconstruction-of-chebyshev-prime-error.md). Formalization in `GoldbachThreePhaseFourier.lean` of 3-adic phase multiplicativity (`threeComplexPhase_mul`), real-valued centered Goldbach triangle (`complexCenteredGoldbach_im_zero`), identification of angular integral with algebraic harmonic (`threeAngularHarmonic_eq`), and exact Fourier inversion to Chebyshev prime error (`threeAngularHarmonic_prime_error`, `actual_prime_error_from_complex_angular_harmonic`).
287. [Exact discrete prime error energy balance and Lyapunov drift criterion](exact-discrete-prime-error-energy-balance-and-lyapunov-drift-criterion.md). Formalization in `PrimeEnergy.lean` of the weighted integration-by-parts identity (`weighted_energy_balance`), discrete prime error energy balance (`prime_energy_balance`), $O(\log^2 N)$ increment energy bound (`primeIncrementEnergy_le_log_square`), conditional Lyapunov error bound (`prime_error_square_upper_of_drift_nonpos`), and early drift positivity (`primeErrorDrift_two_pos`).
288. [Exact causal successor Jacobian and charge-conserving compensation](exact-causal-successor-jacobian-and-charge-conserving-compensation.md). Formalization in `SuccessorCharge.lean` of the successor change-of-variables integral (`integral_successor`), uniform kernel bound (`kernel_abs_le`), relative defect formula (`integral_successor_sub`), reference probe normalization (`reference_integral`), and total charge conservation (`charge_compensated`).
289. [Exact all-prime seed $L^2$ integrability and three-region tail splitting](exact-all-prime-seed-l2-integrability-and-three-region-tail-splitting.md). Formalization in `PrimeSeedAllPrimeSquare.lean` of the three-region prime bound (`finite_seed_three_region_bound`), age-weighted decay (`allPrimeSeed_age_bound`), age-normalized integrability (`integrable_allPrimeSeed_div_age`), quadratic majorization (`allPrimeSeed_square_tail_bound`), and unconditional $L^2$ membership (`allPrimeSeed_memLp_two`).
290. [Exact second-window dual obstruction and Mertens Cauchy bound](exact-second-window-dual-obstruction-and-mertens-cauchy-bound.md). Formalization in `SecondWindow.lean` of the second-window reciprocal kernel evaluation (`reciprocalKernel_first_second_window`), exact second-window hyperbolic profile (`correctedImage_second_window`), scaled inverse-square integral evaluation (`scaled_inverse_square_integral`), non-asymptotic Cauchy lower bound (`corrected_second_window_cauchy_bound`), and asymptotic convergence obstruction (`secondWindowCoefficient_sq_tendsto_of_corrected_convergence`).
291. [Exact reciprocal kernel isometry and fractional Gram equivalence](exact-reciprocal-kernel-isometry-and-fractional-gram-equivalence.md). Formalization in `ReciprocalIsometry.lean` of the kernel bounds (`fractionKernel_bounds`, `reciprocalKernel_bounds`), product integrability on $(0, \infty)$ (`fractionKernel_product_integrable`), hyperbolic involutive Gram equality (`fractionKernel_gram_eq`), Gram matrix isometry (`kernelSum_gram_eq`), and universal distance preservation (`kernelSum_distance_eq`).
292. [Exact fixed-coefficient decay and finite-quotient repair impossibility](exact-fixed-coefficient-decay-and-finite-quotient-repair-impossibility.md). Formalization in `FixedCoefficient.lean` of the window moment lower bound (`array_window_distance_lower`), window moment decay (`windowMoment_sq_tendsto_of_cauchy`), triangular normalized split (`windowMoment_normalized_split`), fixed coefficient decay (`fixed_coefficient_tendsto_of_cauchy`), and finite quotient repair impossibility (`finite_quotient_repair_not_cauchy`, `finite_quotient_repair_not_convergent`).
293. [Exact distinct-prime cell dynamics and arithmetic velocity jump dichotomy](exact-distinct-prime-cell-dynamics-and-arithmetic-velocity-jump-dichotomy.md). Formalization in `DistinctPrimeDynamics.lean` of the discrete integer area identification (`cell_at_integer`), continuous cell interface gluing (`cell_join`), velocity jump formula (`slope_jump`), prime-power deceleration shock (`slope_jump_prime_pow`, `slope_jump_prime_pow_neg`), composite acceleration impulse (`slope_jump_nonneg_of_not_prime_pow`), and initial condition (`initial_cell_and_slope`).
294. [Exact paired scattering defect and uniform critical-strip bound](exact-paired-scattering-defect-and-uniform-critical-strip-bound.md). Formalization in `PairedScatteringDefect.lean` of the exact paired multiplier identity (`paired_identity`), closed-form half-strip moment (`halfStripMoment_eq`), universal moment bound (`halfStripMoment_le_two`), and uniform critical strip bound $\le 3$ (`norm_paired_actual_prime_le_three`).
295. [Exact Goldbach cofactor generating series and analytic parity projections](exact-goldbach-cofactor-generating-series-and-analytic-parity-projections.md). Formalization in `GoldbachCofactorParitySeries.lean` of the infinite Abel identity (`ratioSeries_abel`), discrete convolution antidiagonal representation (`cofactor_eq_antidiagonal`), generating series product factorization (`cofactorSeries_eq_mul`), and even/odd parity projection operators (`evenCofactorSeries_projection`, `oddCofactorSeries_projection`).
296. [Exact logarithmic overlap loss and algebraic spacing bounds](exact-logarithmic-overlap-loss-and-algebraic-spacing-bounds.md). Formalization in `LogarithmicOverlap.lean` of the exact overlap decomposition (`overlap_identity`), logarithmic overlap loss bound (`overlap_loss`), uniform error compensation (`uniform_error_bound`), and normalized spacing bound (`uniform_error_fraction_bound`).
297. [Exact moving inverse test continuity and Fundamental Calculus Theorem](exact-moving-inverse-test-continuity-and-fundamental-calculus-theorem.md). Formalization in `FactorialRealCellTest.lean` of the exact boundary matching (`finiteInverseTest_entry_match`), global continuity on $[1, \infty)$ (`movingInverseTest_continuousOn`), right derivative recovery (`movingInverseTest_hasDerivWithinAt_right`), interval integrability (`inverseTestPhase_intervalIntegrable`), and Fundamental Theorem of Calculus (`movingInverseTest_integral_from_one`).
298. [Exact theta prime-return outer coercivity and exponential tail certification](exact-theta-prime-return-outer-coercivity-and-exponential-tail-certification.md). Formalization in `ThetaPrimeReturnOuterCore.lean` of outer coercivity (`outer_coercivity`), end-to-end main mass and BV interface (`outer_coercivity_from_main_bv`), exact parameter identity (`outerU_parameter_identity`), and high-precision tail bounds (`fortyfive_two_pow_tail`, `coarse_error_below_gap_quarter`, `bv_error_scale`).
299. [Exact factorial interior energy linear budget and dyadic transfer](exact-factorial-interior-energy-linear-budget-and-dyadic-transfer.md). Formalization in `FactorialBirthInteriorBound.lean` of clock origin linear vanishing (`beta_le_half`), two-regime interior density bounds (`interior_row_uniform`, `interior_row_tail`), unconditional linear budget (`interior_energy_linear`), dyadic horizon budget (`dyadic_interior_budget`), and energy-boundary transfers (`actual_energy_upper`, `actual_boundary_upper`).
300. [Exact Mangoldt-weighted birth-source majorization and joint convergence](exact-mangoldt-weighted-birth-source-majorization-and-joint-convergence.md). Formalization in `IntegerBirthSourceBound.lean` of the first-moment source bounds (`U_abs_bound`, `interpolant_abs_bound`), von Mangoldt $L$-series convergence at $3/2$ (`summable_mangoldt_bound`), complete weighted source majorization (`actual_source_interpolation_le`), and two-dimensional clock absolute convergence (`exponential_clock_absolute`).
301. [Exact physical theta jump Dirichlet form and Beurling–Deny Markov property](exact-physical-theta-jump-dirichlet-form-and-beurling-deny-markov-property.md). Formalization in `PhysicalThetaJumpEnergy.lean` of the time-reversal symmetry (`edge_mass_reverse`), Beurling–Deny modulus contraction (`jump_energy_abs_contract`, `killed_energy_abs_contract`), and three-way killed edge algebraic decomposition (`killed_edge_square_split`).
302. [Exact Dirichlet hyperbola divisor forcing and explicit Stirling cancellation](exact-dirichlet-hyperbola-divisor-forcing-and-explicit-stirling-cancellation.md). Formalization in `DivisorForcingBound.lean` of the exact hyperbola decomposition (`floor_sum_hyperbola`), harmonic discrepancy bounds (`harmonic_euler_error_bounds`), explicit divisor sum remainder (`divisorSum_error_le`), log factorial integral envelopes (`logFactorial_integral_bounds`), and non-asymptotic divisor forcing bound (`centeredDivisorForcing_sqrt_bound`).
303. [Exact real-floor response integral reconstruction and rational step pairing](exact-real-floor-response-integral-reconstruction-and-rational-step-pairing.md). Formalization in `FactorialRealCellSteps.lean` of the rational step counting identity (`real_floor_step_sum`), single quotient floor renewal (`real_floor_inversePhase_integral`), and full continuous response integral reconstruction (`actual_real_floor_response_integral`).
304. [Exact prime-admission energy flux positivity and layerwise obstruction](exact-prime-admission-energy-flux-positivity-and-layerwise-obstruction.md). Formalization in `PrimeAdmissionX4Obstruction.lean` of the positive prime-admission energy fluxes (`first_packet_flux_pos`, `second_packet_flux_pos`), cross-interaction positivity (`second_packet_cross_pos`), and finite quotient packets (`quotientPacket_three`, `quotientPacket_five`).
305. [Exact physical block Mellin scattering and low-high frequency exponential decoupling](exact-physical-block-mellin-scattering-and-low-high-frequency-exponential-decoupling.md). Formalization in `FullComplexHistoryMellin.lean` of the exact Mellin multiplier action (`actual_history_mellin`), multiplicative paired weight factorization (`finite_prime_block_paired_weight`), and low-high frequency exponential decoupling bound (`physical_block_low_high_bound`).
306. [Exact narrow pole-null wave packets and critical pole annihilation](exact-narrow-pole-null-wave-packets-and-critical-pole-annihilation.md). Formalization in `NarrowPoleNullPacket.lean` of the characteristic polynomial pole vanishing (`pole_polynomial`), carrier pole moment vanishing (`carrier_pole_moments`), and smooth narrow complex pole-null packet construction (`exists_narrow_complex_pole_null`).
307. [Exact Möbius–Stokes two-face boundary algebra and prime dilation](exact-mobius-stokes-two-face-boundary-algebra-and-prime-dilation.md). Formalization in `MobiusStokesFiniteBoundary.lean` of the prime dilation bulk splitting (`bulkMass_prime_split`), primitive multiples reindexing (`primitive_multiples_reindex`), two-face discrete Stokes boundary identity (`prime_dilation_two_face`), and finite observed Stokes prefix identity (`finite_stokes_observed`).
308. [Exact shifted zeta multiplier holomorphy and scaled double-pole nonvanishing](exact-shifted-zeta-multiplier-holomorphy-and-scaled-double-pole-nonvanishing.md). Formalization in `ShiftedZetaMultiplier.lean` of multiplier holomorphy (`analyticOnNhd_M`), nonvanishing on $\text{Re}(s)>1/2$ (`M_ne_zero`), and nonvanishing of scaled double-pole zero-multiplicity residues (`multiplicity_coefficient_ne_zero`).
309. [Exact factorial binary infinite energy recurrence and Cauchy majorant](exact-factorial-binary-infinite-energy-recurrence-and-cauchy-majorant.md). Formalization in `FactorialBinaryIntegrability.lean` of the global Cauchy majorant (`energyRow_majorant`), improper Lebesgue integrability (`energyRow_integrable`, `densityRow_integrable`, `workRow_integrable`), scale-invariance under dilation (`scaled_energy_integral`), and infinite-horizon dyadic energy balance recurrence (`binary_energy_infinite`).
310. [Exact successor-feedback floor Abel kernel holomorphy and Volterra error](exact-successor-feedback-floor-abel-kernel-holomorphy-and-volterra-error.md). Formalization in `SuccessorFeedbackFloorKernel.lean` of parametric complex differentiation (`floorAbelIntegrand_hasDerivAt`), holomorphy on $\text{Re}(z)>0$ (`floorAbelKernel_analyticOnNhd`), and the exact Volterra discrepancy integral (`floorAbelKernel_sub_smooth`).
311. [Exact ordered division rod telescoping and additive gap majorization](exact-ordered-division-rod-telescoping-and-additive-gap-majorization.md). Formalization in `DivisionRod.lean` of multiplicative prefix telescoping (`ordered_prefix_product_le_last`, `ordered_division_product_le`), additive gap majorization (`ordered_product_le_one_add_total_gap`), and strict gap bounds (`ordered_product_lt_one_add_total_gap`).
312. [Exact prime-memory charge cutoff decay and finite-rank defect error](exact-prime-memory-charge-cutoff-decay-and-finite-rank-defect-error.md). Formalization in `MemoryChargeCutoff.lean` of pointwise seed remainder bounds (`primeSeedRemainder_bound`), exponential remainder mass bounds (`exponential_remainder_mass_bound`), energy-bounded functional bounds (`remainder_memory_charge_bound_energy`), and explicit rank-defect cutoff bounds (`cutoff_memory_charge_rank_error`).
313. [Exact Riemann zeta floor-Mellin continuation and positive-domain identity](exact-riemann-zeta-floor-mellin-continuation-and-positive-domain-identity.md). Formalization in `FloorMellin.lean` of fractional-part Mellin transform convergence (`floorRemainder_mellinConvergent`), holomorphy on $\text{Re}(s)>0$ (`floorMellin_analytic`), punctured positive-domain identity theorem (`identity_on_positiveDomain`), and exact continuation across the critical strip (`zeta_eq_floorMellin`).
314. [Exact mixed-energy symmetric translation kernel and contact form](exact-mixed-energy-symmetric-translation-kernel-and-contact-form.md). Formalization in `MixedEnergyKernel.lean` of energy finiteness on $L^2$ (`energy_lt_top_of_memLp`), cross-orientation shift adjointness (`lag_cross_orientation`), bilinear kernel decomposition (`bilinear_eq_symmetricKernelPair`), and exact diagonal cancellation in the physical contact form (`contactForm_eq_kernel`).
315. [Exact additive heat Gram positivity and Cauchy-Lipschitz embedding](exact-additive-heat-gram-positivity-and-cauchy-lipschitz-embedding.md). Formalization in `AdditiveHeatPairing.lean` of Laplace inner product pairing (`exponential_pair_integral`), finite additive Gram positivity (`finite_additive_gram_nonnegative`), exact metric distance tensor (`birth_distance_square`), and global 1/2-Lipschitz continuity into $L^2(0, \infty)$ (`birth_lipschitz`).
316. [Exact cyclotomic-Farey tri-partition and conductor interaction control](exact-cyclotomic-farey-tri-partition-and-conductor-interaction-control.md). Formalization in `CyclotomicInteraction.lean` of conductor disjoint tri-partition (`oldConductors_partition`), logarithmic-divisor bounds (`selectedAbsolute_le`), Farey interaction splitting (`oldInteraction_partition`), and boundary cost control (`controlledInteraction_abs_le`).
317. [Exact Hermitian optimizer-mismatch identity and joint-null covariance compression](exact-hermitian-optimizer-mismatch-identity-and-joint-null-covariance-compression.md). Formalization in `OptimizerMismatch.lean` of Hermitian affine completion of squares (`affine_completion_of_square`), exact optimizer mismatch identity (`optimizer_mismatch_identity`), observed mismatch under joint nullspace (`observed_optimizer_mismatch_joint_null`), and covariance compression (`jointNull_loss_le_covariance_ratio`).
318. [Exact odd weak ground prime reflection and polarity flip identity](exact-odd-weak-ground-prime-reflection-and-polarity-flip-identity.md). Formalization in `OddWeakGroundPrimeReflectionFinite.lean` of odd scale antisymmetry (`oddScale_reflect`), pointwise polarity threshold flip (`pair_with_flip`), von Mangoldt reflected sum (`finite_vonMangoldt_reflected_row`), and product scaled row identity (`finite_vonMangoldt_scaled_row`).
319. [Exact compact physical Mellin holomorphy and pole-null Cauchy residue vanishing](exact-compact-physical-mellin-holomorphy-and-pole-null-cauchy-residue-vanishing.md). Formalization in `CompactPhysicalMellinAnalytic.lean` of entire holomorphy of compact Mellin transforms (`physical_mellin_entire`), Cauchy circle formula for entire functions (`entire_weight_circle_coefficient`), and vanishing of residue circle corrections at $s = 0, 1$ (`pole_null_circle_corrections`, `finite_block_pole_null_circle_corrections`).
320. [Exact logarithmic layercake representation and Cauchy moment bound](exact-logarithmic-layercake-representation-and-cauchy-moment-bound.md). Formalization in `LogarithmicLayercake.lean` of the exact logarithmic layer-cake formula (`logarithmic_layercake`), two-regime truncation bound (`logarithmic_layercake_le`), optimal threshold bound (`optimized_increment_bound`), and density log-moment majorization (`density_log_moment_nonnegative_mass`).
321. [Exact boundary mean correction and reciprocal dual compact vanishing](exact-boundary-mean-correction-and-reciprocal-dual-compact-vanishing.md). Formalization in `MeanCorrection.lean` of boundary correction mean -1/2 (`boundaryCorrection_mean`), exact mean zero cancellation (`meanCorrectedError_mean_zero`), reciprocal duality (`meanCorrectedDual_reciprocal`), and compact support vanishing outside (0, 1] (`meanCorrectedDual_zero_above_one`).
322. [Exact smooth filtration prime bifurcation and generator rank dynamics](exact-smooth-filtration-prime-bifurcation-and-generator-rank-dynamics.md). Formalization in `Model.lean` of smooth filtration prime state bifurcation (`state_changes_iff_prime`), prime exclusion (`new_generator_iff_prime`), canonical monoid product isomorphism (`prime_step_equiv`), and rank indicator update (`generatorRank_succ`).
323. [Exact finite centered-Goldbach heat scale increment and shell decomposition](exact-finite-centered-goldbach-heat-scale-increment-and-shell-decomposition.md). Formalization in `CenteredGoldbachHeatScaleIncrementFinite.lean` of the integral squared heat-flow identity (`finiteHeatScaleIncrement_eq_integral`), horizon doubling formula (`finiteHeatHorizon_double_sub`), 4-way marginal decomposition (`finiteHeatScaleIncrement_decomposition`), and parity split (`finiteHeatScaleIncrement_parity`).
324. [Exact adjacent additive totals discrete derivative and Goldbach pairing oscillation](exact-adjacent-additive-totals-discrete-derivative-and-goldbach-pairing-oscillation.md). Formalization in `CenteredGoldbachAdjacentTotalsFinite.lean` of adjacent discrete derivative identity (`boundedCoefficient_adjacent`), interior centering cancellation (`centered_adjacent_derivative_eq_mangoldt`), odd-total factor of 2 (`odd_primeCoefficient_eq_two_evenPrimeRow`), and adjacent pair sign oscillations (`weightedAdjacentPair_three_two_pos`, `weightedAdjacentPair_three_four_neg`).
325. [Exact fixed-profile Green admission and scaled Möbius tent sign formula](exact-fixed-profile-green-admission-and-scaled-mobius-tent-sign-formula.md). Formalization in `FixedProfileGreenAdmissionFinite.lean` of the scaled Green admission sign identity (`scaled_increment_main_sign`), truncated tent decomposition (`truncatedTClosed_eq_main_term`), Gram kernel packet equivalence (`kernelIncrement_eq_doubledIncrement`), and lower/upper cross splitting (`oldCross_eq_lower_add_upper`).
326. [Exact finite Haar energy decomposition and coarse-mode conservation](exact-finite-haar-energy-decomposition-and-coarse-mode-conservation.md). Formalization in `CrossHorizonHaarFinite.lean` of inductive dyadic Haar energy decomposition (`squareEnergy_eq_coarse_add_haar`), coarse merging recursion (`child_coarse_recursion`), detail gauge invariance (`haarCoefficients_gaugeShift`), and coarse remainder conservation (`coarse_remainder_conserved`).
327. [Exact Goldbach Hankel minor negativity and dyadic screw kernel inequality](exact-goldbach-hankel-minor-negativity-and-dyadic-screw-kernel-inequality.md). Formalization in `GoldbachScrewMinorFinite.lean` of Goldbach coefficient evaluations (`prime_goldbach_four`, `prime_goldbach_five`, `prime_goldbach_six`), negative Hankel determinant (`first_nonzero_goldbach_hankel_minor_neg`), refutation of Hankel moment positivity (`raw_goldbach_not_hankel_positive`), and dyadic screw minor bound (`dyadic_screw_inequality`).
328. [Exact factorial floor-derivative obstruction and N=13 Mertens counterexample](exact-factorial-floor-derivative-obstruction-and-n13-mertens-counterexample.md). Formalization in `FactorialFloorDerivativeObstruction.lean` of the $N=13$ integer certificate (`certificateDenominator_lt_numerator`, `certificate_log_ratio_pos`), floor row evaluations (`floorRow13_eq_one`), discrete derivative distribution (`floorRow13_difference_interior`, `floorRow13_difference_terminal`), and 2D telescope (`rectangle_double_telescope`).
329. [Exact complete cofactor prime source polarization and Goldbach minorization](exact-complete-cofactor-prime-source-polarization-and-goldbach-minorization.md). Formalization in `FactorialBinetGoldbachFinite.lean` of prime source polarization (`primeSource_prime_eq_direct`, `primeSource_composite_le`), rational hard wall (`primeSource_composite_le_rational`), sharpness at $n=4$ (`primeSource_four`), and Goldbach minorization (`direct_goldbach_le_pair`).
330. [Exact Goldbach successor logarithmic escape and Chebyshev symmetric reduction](exact-goldbach-successor-logarithmic-escape-and-chebyshev-symmetric-reduction.md). Formalization in `GoldbachSuccessorLogFinite.lean` of logarithmic transport non-negativity (`logPair_nonneg`, `logEscape_nonneg`), symmetric reduction to Chebyshev convolution (`logEscapeNumerator_eq_psi`), endpoint trimming (`logEscape_eq_psi_trim`), and rational cofactor error splitting (`rationalCofactor_eq_baseline_add_error`).
331. [Exact odd-Möbius factorization and 2-adic collar splitting of von Mangoldt](exact-odd-mobius-factorization-and-2-adic-collar-splitting-of-von-mangoldt.md). Formalization in `SuccessorCollarOddMobius.lean` of 2-adic difference factorization of Möbius (`moebius_eq_odd_factor`), universal $\log 2$ collapse on even cofactors (`oddLogBracket_apply`), odd-Möbius factorization of von Mangoldt (`vonMangoldt_eq_odd_convolution`), and finite hyperbola formula (`weighted_vonMangoldt_odd_factor_pairs`).
332. [Exact cumulative odd Goldbach lacunary Chebyshev convolution and step dynamics](exact-cumulative-odd-goldbach-lacunary-chebyshev-convolution-and-step-dynamics.md). Formalization in `GoldbachOddCumulativeFinite.lean` of cumulative dyadic reindexing (`cumulativeOdd_eq_power_sum`), lacunary Chebyshev error convolution (`oddResidual_eq_error_convolution`), one-step discrete derivative (`oddResidual_succ`), unconditional lower bound (`oddResidual_succ_lower`), and odd-to-even binary pair collapse (`oddResidual_succ_odd`).
333. [Exact high-parent successor dilation uniqueness and prime row energy domination](exact-high-parent-successor-dilation-uniqueness-and-prime-row-energy-domination.md). Formalization in `SuccessorHighParentPrimeRow.lean` of dilation uniqueness above square-root scale (`high_parent_child_dilation_unique`, `highParentChildren_card_le_one`), elimination of cross-terms (`highParent_vonMangoldt_sum_sq`), cell length ratio bound (`highParent_cell_ratio_le`), and normalized row energy tail bound (`highParent_vonMangoldt_normalized_row_energy_le`).
334. [Exact finite prime future-cone energy admission and strict supermultiplicative growth](exact-finite-prime-future-cone-energy-admission-and-strict-supermultiplicative-growth.md). Formalization in `FinitePrimeFutureConeEnergy.lean` of prime adjunction identity (`cubePrefix_insert`), dilated energy scaling (`dilated_energy`), reverse triangle bound (`finite_energy_reverse_triangle`), prime admission energy growth (`cubeEnergy_insert`), and strict growth for $p \ge 5$ (`cubeEnergy_insert_strict`).
335. [Exact harmonic prime weight square-shell summation and closed arithmetic bound](exact-harmonic-prime-weight-square-shell-summation-and-closed-arithmetic-bound.md). Formalization in `HarmonicPrimeWeightBound.lean` of 2D square-shell identity (`sum_pow_max_Ico`), infinite geometric bound on max exponent sum (`same_geometric_sum_le`), same-prime bound (`samePrimeReciprocalSum_le`), coprime cross-prime decoupling (`reciprocal_lcm_sum_eq`), and closed arithmetic bound on harmonic prime weight (`harmonicPrimeWeight_le_closed`).
336. [Exact gradient realization of mixed additive energy and contact form norm-nine bound](exact-gradient-realization-of-mixed-additive-energy-and-contact-form-norm-nine-bound.md). Formalization in `MixedEnergyBilinear.lean` of lag measure mass (`lagMeasure_mass`), gradient measure energy isometry (`energy_eq_gradient`), bilinear Cauchy-Schwarz bound (`bilinear_bound`), unit-pair bound (`unitPair_bound`), and contact form norm-9 bound (`contactForm_bound`).
337. [Exact harmonic phase defect domination and LCM valuation overlap identity](exact-harmonic-phase-defect-domination-and-lcm-valuation-overlap-identity.md). Formalization in `HarmonicPrimePhase.lean` of harmonic phase defect domination by primes (`harmonicPhaseDefect_le_prime_sum`), valuation indicator identity (`valuation_eq_sum_power_divisors`), valuation overlap LCM sum (`valuation_overlap_eq_lcm_sum`), and same-prime valuation square max sum (`same_prime_valuation_sq_eq_max_sum`).
338. [Exact cyclotomic prefix derivative multiplicative update and root deficit stability](exact-cyclotomic-prefix-derivative-multiplicative-update-and-root-deficit-stability.md). Formalization in `CyclotomicPrefix.lean` of inductive polynomial step (`prefixPolynomial_step`), derivative multiplicative update at old roots (`prefixDerivative_old_step`), non-vanishing of prefix derivative (`prefixDerivative_ne_zero`), and logarithmic root deficit update bound (`oldRootUpdate_abs_le_factorLog`, `oldRootUpdate_abs_le`).
339. [Exact pointwise Goldbach cofactor Abel decomposition and packet variation bound](exact-pointwise-goldbach-cofactor-abel-decomposition-and-packet-variation-bound.md). Formalization in `GoldbachCofactorPointwiseAbelFinite.lean` of finite difference Abel summation (`finite_difference_abel`), variation error bound (`finite_difference_abs_le_variation`), centered remainder derivative convolution (`centeredPairRemainder_as_difference`), pointwise packet Abel decomposition (`centeredPairRemainder_packet_abel`), and packet error estimate (`centeredPairRemainder_packet_error_bound`).
340. [Exact factorial real-cell cross kernel isometry and log-two budget majorization](exact-factorial-real-cell-cross-kernel-isometry-and-log-two-budget-majorization.md). Formalization in `FactorialRealCellCrossKernel.lean` of bilinear phase integral identity (`phase_bilinear_integral`), cross-phase energy integral evaluation (`inverseTestPhase_cross_integral`), log 2 budget majorization bound (`actualCrossKernel_norm_le`), and kernel symmetry (`actualCrossKernel_symm`).
341. [Exact integer birth density difference L1 decay and Mangoldt series integration](exact-integer-birth-density-difference-l1-decay-and-mangoldt-series-integration.md). Formalization in `IntegerBirthDifferenceL1.lean` of common tail difference (`common_tail_difference`), strip-plus-tail majorant (`difference_norm_le`), $n^{-3/2}$ $L^1$ decay bound (`difference_integral_norm_birth_bound`), von Mangoldt $L^1$ summability (`mangoldt_difference_integral_norm_summable`), and termwise series integration (`all_birth_difference_integral`).
342. [Exact odd Goldbach dyadic lacunary reduction and uniform two-log-squared bound](exact-odd-goldbach-dyadic-lacunary-reduction-and-uniform-two-log-squared-bound.md). Formalization in `GoldbachOddPowerBoundFinite.lean` of even von Mangoldt sum (`even_vonMangoldt_sum`), exact dyadic lacunary reduction (`odd_primeCoefficient_power_sum`), logarithmic count bound (`odd_primeCoefficient_le_log_count`), and uniform $2(\log N)^2$ bound (`odd_primeCoefficient_le_two_log_sq`).
343. [Exact critical three-halves screw remainder budget and scale invariance](exact-critical-three-halves-screw-remainder-budget-and-scale-invariance.md). Formalization in `GoldbachScrewRemainderThresholdFinite.lean` of dyadic remainder budget (`dyadic_remainder_budget`), critical 3/2 scale-invariant threshold constant (`critical_three_halves_budget`), and two-sided margin transfer (`robust_dyadic_of_margin`, `raw_dyadic_of_corrected`).
344. [Exact dual Dirichlet offline decay and power suppression threshold](exact-dual-dirichlet-offline-decay-and-power-suppression-threshold.md). Formalization in `DualDirichletOfflineDecay.lean` of dual exponent displacement identity (`dual_exponent_identity`), critical delta threshold (`criticalDelta`), negative exponent for off-line zeros (`dual_exponent_neg`), positive decay rate (`dual_exponent_decay_rate`), critical line value (`dual_exponent_critical_line`), offline power decay (`offline_power_decay`), strict asymptotic monotonicity (`offline_power_strict_monotonicity`), and product bound (`offline_dual_product_bound`).
345. [Exact proper-cofactor Dirichlet dual algebra and multiplicative prime-row convolution](exact-proper-cofactor-dirichlet-dual-algebra-and-multiplicative-convolution.md). Formalization in `ActualCofactorDirichletDualAlgebra.lean` of divisor involution swap (`sum_divisors_swap`), hyperbolic factor-pair transposition (`factor_pairs_swap`), proper-cofactor transposition (`proper_cofactor_pairs_swap`), exact prime-row decomposition into logarithmic row minus proper cofactors (`primeRow_eq_logRow_sub_cofactors`), square-root normalized Weil form (`primeRowSqrt_eq_logRowSqrt_sub_cofactors`), complete multiplicative Dirichlet convolution (`multiplicative_dirichlet_convolution_primeRow`), and truncated proper-cofactor duality (`truncated_prime_cofactor_identity`).
346. [Exact proper-cofactor spectral neutralization and asymptotic margin dominance](exact-proper-cofactor-spectral-neutralization-and-margin-dominance.md). Formalization in `ActualCofactorSpectralNeutralization.lean` of exact multiplier neutralization (`amplitude_scattering_exact_neutralization`), strict exponent depression (`compensated_exponent_strictly_less`), arbitrary sub-power suppression (`compensated_exponent_arbitrarily_small`), sub-half bound (`compensated_exponent_sub_half`), unit magnitude at dual length 1 (`neutralized_spectral_magnitude_at_one`), net spectral suppression (`net_spectral_offline_suppression`), and asymptotic dominance of the logarithmic Weil margin over neutralized off-line mass (`eventual_dominance_weil_margin`).
347. [Exact Weil sign incompatibility and off-line pair negativity obstruction](exact-weil-sign-incompatibility-and-offline-pair-negativity-obstruction.md). Formalization in `ActualWeilSignObstruction.lean` of off-line growth exponent positivity (`offline_growth_exponent_pos`), linear off-line negative drift divergence (`offline_growth_divergence`, `offline_negative_drift_divergence`), universal drop below any constant threshold (`offline_paired_deficit_eventual`), sub-power dominance over proper-cofactor remainders (`subpower_dominance`), uncompensated net negative deficit (`offline_net_negativity_with_remainder`), and the definitive Weil sign incompatibility theorem (`weil_sign_incompatibility`).
348. [Exact chirped spectral-arithmetic exclusion and logarithmic power dominance](exact-chirped-spectral-arithmetic-exclusion-and-logarithmic-power-dominance.md). Formalization in `ChirpedSpectralArithmeticExclusion.lean` of chirped power exponent positivity (`chirped_power_exponent_pos`), effective coupling coefficient positivity (`chirped_effective_coeff_pos`), chirped growth factor identity (`chirped_growth_factor_eq`), universal half-power logarithmic bound (`log_le_rpow_half`), power dominance over logarithmic background (`power_dominance_over_log`), chirped spectral deficit theorem (`chirped_spectral_deficit_eventual`), and the definitive chirped spectral-arithmetic exclusion theorem (`chirped_spectral_arithmetic_exclusion`).
349. [Exact chirped spectral-RH bridge and off-line zero inadmissibility](exact-chirped-spectral-rh-bridge-and-offline-zero-inadmissibility.md). Formalization in `ChirpedSpectralRHBridge.lean` of filter contradiction lemma on $\mathbb{R}$ (`eventually_contradiction`), chirped off-line zero configuration inadmissibility (`chirped_offline_zero_inadmissible`), zero-freeness of the right half-plane (`rightHalfZeroFree_of_chirped_configuration`), and formal deduction of Mathlib's `RiemannHypothesis` (`RiemannHypothesis_of_chirped_configuration`).
350. [Exact chirped stationary phase carrier tuning and band localization](exact-chirped-stationary-phase-carrier-tuning.md). Formalization in `ChirpedStationaryPhaseCarrier.lean` of exact chirped phase derivative (`hasDerivAt_chirpedPhase`), tuned carrier positivity (`tunedCarrier_pos`), exact stationary phase cancellation at support point $x_0$ (`tunedCarrier_stationary`), stationary point uniqueness (`stationary_point_eq`, `tuned_stationary_point_eq`), strict interior band localization (`tunedCarrier_in_band`), non-degenerate curvature (`chirpedPhase_curvature_pos`), and carrier divergence at infinity (`tendsto_tunedCarrier_atTop`).
351. [Exact chirped Fresnel-Gaussian localization and stationary phase core](exact-chirped-fresnel-gaussian-localization.md). Formalization in `ChirpedFresnelGaussianLocalization.lean` of exact quadratic Taylor expansion with zero higher remainder (`chirpedPhase_sub_eq_quadratic`), exact collapse to pure Gaussian phase at tuned carrier (`chirpedPhase_tuned_quadratic`), exact central phase value (`chirpedPhase_at_tuned_center`), Fresnel radius definition and positivity (`fresnelRadius`, `fresnelRadius_pos`), unit phase shift across Fresnel radius (`fresnel_phase_shift_eq_one`), uniform quadratic phase deviation bound (`chirpedPhase_deviation_le`), high-frequency Fresnel core shrinkage (`tendsto_fresnelRadius_atTop`), and eventual strict containment of the stationary phase core inside the packet window (`eventually_fresnel_core_in_support`).
352. [Exact chirped Fresnel phase coherence and core lower bound](exact-chirped-fresnel-phase-coherence.md). Formalization in `ChirpedFresnelPhaseCoherence.lean` of elementary trigonometric cosine bound (`cos_ge_half_of_abs_le_one`), exact phase deviation bound $|\Delta \Phi| \le 1$ on the Fresnel core (`fresnel_phase_deviation_le_one`), uniform cosine coherence $\cos(\Delta \Phi) \ge 1/2$ (`fresnel_core_cos_ge_half`), pointwise envelope projection lower bound (`fresnel_core_lower_bound`), exact carrier product identity (`tunedCarrier_product_eq`), strict positivity of Fresnel coupling energy (`fresnel_effective_coupling_pos`), and exact coherent core energy formula (`fresnel_energy_eq`).

