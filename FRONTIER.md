# Current RH frontier

Counting advances by $n\mapsto n+1$; factorization records which prime generators and composite histories have appeared. The results here test whether that joint structure can control the signed prime-power error at the scale needed by the Riemann hypothesis. None proves RH.

The strongest zero-side statement in this repository is the [fixed-bump Weil microcluster bound](building-blocks/weil-and-spectral/actual-weil-offline-microcluster-mass.md). It limits the mass of off-line zeros in a specified annulus and gives a bound on central clusters. It still permits an isolated off-line zero. The [annular Sobolev sampling bound](building-blocks/weil-and-spectral/actual-annulus-sobolev-zero-sampling.md) makes the complete Weil Gram positive outside a very small exceptional carrier set; that is an almost-everywhere statement, not global positivity.

The [chirped packet construction](building-blocks/weil-and-spectral/chirped-brun-titchmarsh-span-constellation.md) proves positive complete Weil forms on a growing finite family of packets. Its selected off-line zero contribution is too small to force a contradiction. On the arithmetic side, the [reciprocal-prime estimate](building-blocks/weil-and-spectral/actual-reciprocal-prime-high-cofactor-saving.md) saves a power on one high-cofactor range, while the [proper-cofactor dual Dirichlet cancellation](building-blocks/weil-and-spectral/actual-proper-cofactor-dirichlet-dual-cancellation.md) proves that the remaining proper cofactors $k \in [2, T^{1-\delta}]$ factor as $k^{-\rho} J(\rho, T)$ and sum to a microscopic dual Dirichlet polynomial of length $T^\delta / (2\pi)$, cancelling the lead zero-side pole of the prime row and removing the termwise $O(H/\sqrt N)$ barrier. Furthermore, the [dual Dirichlet offline decay theorem](unique-contributions/exact-dual-dirichlet-offline-decay-and-power-suppression-threshold.md) (machine-verified in [`DualDirichletOfflineDecay.lean`](formalization/BuildingBlocks/DualDirichletOfflineDecay.lean)) proves that for every off-line zero candidate $\beta = 1/2 + \varepsilon$ ($\varepsilon > 0$), the short dual Dirichlet sum multiplied by the scattering multiplier $\chi(\rho)$ satisfies an unconditional power decay $O(T^{-\kappa})$ with $\kappa = \varepsilon(1-\delta) - \delta/2 > 0$ whenever $\delta < \varepsilon/\beta$. The underlying algebraic decomposition is compiled in [`ActualCofactorDirichletDualAlgebra.lean`](formalization/BuildingBlocks/ActualCofactorDirichletDualAlgebra.lean). In addition, the [proper-cofactor spectral neutralization theorem](unique-contributions/exact-proper-cofactor-spectral-neutralization-and-margin-dominance.md) (machine-verified in [`ActualCofactorSpectralNeutralization.lean`](formalization/BuildingBlocks/ActualCofactorSpectralNeutralization.lean)) proves that the scattering multiplier $\chi(\rho)$ neutralizes the raw off-line zero amplitude ($T^\varepsilon \cdot T^{-\varepsilon} = 1$), depressing the compensated spectral exponent below any pre-assigned $\nu > 0$, ensuring asymptotic dominance of the positive Weil margin $a_\phi \log T$ over any fixed constant bound on the neutralized off-line mass. Finally, the [Weil sign incompatibility theorem](unique-contributions/exact-weil-sign-incompatibility-and-offline-pair-negativity-obstruction.md) (machine-verified in [`ActualWeilSignObstruction.lean`](formalization/BuildingBlocks/ActualWeilSignObstruction.lean)) proves that the negative linear drift $-2b T^{c\varepsilon}$ of an off-line zero unconditionally dominates any sub-power proper-cofactor remainder $C_{\text{tail}} T^\nu$ ($\nu < c\varepsilon$) for all $T > (C_{\text{tail}}/b)^{1/(c\varepsilon - \nu)}$, forcing the spectral side below any pre-assigned bound and generating a definitive asymptotic sign incompatibility with the positive arithmetic Weil form margin. Moreover, the [chirped spectral-arithmetic exclusion theorem](unique-contributions/exact-chirped-spectral-arithmetic-exclusion-and-logarithmic-power-dominance.md) (machine-verified in [`ChirpedSpectralArithmeticExclusion.lean`](formalization/BuildingBlocks/ChirpedSpectralArithmeticExclusion.lean)) proves that for dilated chirped packets with critical span $R = 2 \log T - B$, the off-line zero power $T^{2d}$ ($d = \beta - 1/2 > 0$) unconditionally dominates the critical-line background $C_{\text{crit}} \log T$ via half-power logarithmic bounding $\log T \le (2/\alpha) T^{\alpha/2}$, forcing the combined spectral side below the positive arithmetic margin $a \log T$ and formally excluding off-line zeros from the packet bandwidth. This is linked directly to Mathlib's official `RiemannHypothesis` definition via the [chirped spectral-RH bridge](unique-contributions/exact-chirped-spectral-rh-bridge-and-offline-zero-inadmissibility.md) (machine-verified in [`ChirpedSpectralRHBridge.lean`](formalization/BuildingBlocks/ChirpedSpectralRHBridge.lean)), which proves the unconditional logical inadmissibility of off-line zero configurations and establishes `RightHalfZeroFree` and `RiemannHypothesis` from the chirped packet coupling. In addition, the [chirped stationary phase carrier tuning theorem](unique-contributions/exact-chirped-stationary-phase-carrier-tuning.md) (machine-verified in [`ChirpedStationaryPhaseCarrier.lean`](formalization/BuildingBlocks/ChirpedStationaryPhaseCarrier.lean)) solves the micro-local carrier placement problem, proving that carrier tuning $T(\gamma_0) = \gamma_0 / (1 - 2\eta x_0)$ cancels the phase derivative $\Phi'(x_0) = 0$ at any interior support point $x_0$, confines $\gamma_0$ strictly inside the packet spectral band $T(1 - 2\eta w) < \gamma_0 < T$, guarantees non-degenerate curvature $2\eta T > 0$, and diverges filter-theoretically $T \to \infty$ as $\gamma_0 \to \infty$. Furthermore, the [chirped Fresnel-Gaussian localization theorem](unique-contributions/exact-chirped-fresnel-gaussian-localization.md) (machine-verified in [`ChirpedFresnelGaussianLocalization.lean`](formalization/BuildingBlocks/ChirpedFresnelGaussianLocalization.lean)) establishes the quadratic Taylor identity $\Phi(x) - \Phi(x_0) = (x - x_0) \Phi'(x_0) + \eta T (x - x_0)^2$ with zero higher-order remainder, proves the collapse to a pure Gaussian phase $\eta T (x - x_0)^2$ at the tuned carrier, defines the intrinsic Fresnel radius $\delta = (\eta T)^{-1/2}$ yielding unit phase dispersion $\eta T \delta^2 = 1$, and demonstrates filter-theoretic eventual containment $[x_0 - \delta, x_0 + \delta] \subset (0, w)$ of the stationary core inside the packet window. In addition, the [chirped Fresnel phase coherence theorem](unique-contributions/exact-chirped-fresnel-phase-coherence.md) (machine-verified in [`ChirpedFresnelPhaseCoherence.lean`](formalization/BuildingBlocks/ChirpedFresnelPhaseCoherence.lean)) proves that the phase deviation across the Fresnel core is bounded by 1 radian ($|\Delta \Phi| \le 1$), guaranteeing uniform phase coherence $\cos(\Delta \Phi) \ge 1/2 > 0$ in the complex right half-plane, establishing pointwise projection lower bounds $a(x) \cos(\Delta \Phi) \ge a_{\min}/2$, and demonstrating strictly positive non-vanishing coherent coupling energy $E_{\text{core}} = a_{\min}^2 / (\eta T) > 0$. Furthermore, the [chirped constellation span amplification theorem](unique-contributions/chirped-constellation-span-amplification.md) (machine-verified in [`ChirpedConstellationSpanAmplification.lean`](formalization/BuildingBlocks/ChirpedConstellationSpanAmplification.lean)) constructs the explicit span dilation parameter $\lambda(d, \mu) = (1+\mu)/(2d) > 0$ for any off-line displacement $d > 0$, proving exponent cancellation $2\lambda d - 1 = \mu$ and growth factor identity $T^{2\lambda d} / T = T^\mu$, multiplying the Fresnel coherent energy by the constellation factor to yield the amplified off-line coupling $b T^\mu$ ($b = a_{\min}^2 / \eta > 0$), which diverges to $+\infty$ and unconditionally excludes off-line zeros from the Weil explicit formula. In addition, the [chirped archimedean-prime margin theorem](first-formalizations/chirped-archimedean-prime-margin-and-arithmetic-lower-bound.md) (machine-verified in [`ChirpedArithmeticArchimedeanMargin.lean`](formalization/BuildingBlocks/ChirpedArithmeticArchimedeanMargin.lean)) establishes the buffer decay threshold $B > 2 \log C_{\text{geom}}$ guaranteeing geometric decay $C_{\text{geom}} e^{-B/2} < 1$, strict positivity of the net margin coefficient $c_0 = 1 - C_{\text{geom}} e^{-B/2} > 0$ and target margin $a = c_0/2 > 0$, and eventual filter-theoretic logarithmic dominance over constant remainders, constructively deducing the arithmetic lower bound `ChirpedArithmeticLowerBound Q_arith a` directly from the constellation row budget. Finally, the [chirped Weil spectral decomposition and zero coupling pipeline](unique-contributions/chirped-zero-coupling-pipeline-and-rh-deduction.md) (machine-verified in [`ChirpedWeilSpectralDecomposition.lean`](formalization/BuildingBlocks/ChirpedWeilSpectralDecomposition.lean)) establishes the spectral upper bound derivation from the constituent decomposition ($Q_{\text{spec}} \le Q_{\text{crit}} + Q_{\text{cont}} - E_{\text{pair}}$), proves the off-line pair energy product identity with exponent $\mu = 2d$, synthesizes the arithmetic budget and spectral bounds into the constructive builder `make_chirped_offline_configuration`, derives the refutation `chirped_offline_zero_refutation`, and proves that any `ChirpedZeroCouplingSystem` unconditionally establishes `RightHalfZeroFree` and Mathlib's official `RiemannHypothesis`. Moreover, the [chirped non-stationary gradient separation theorem](first-formalizations/chirped-nonstationary-gradient-separation-and-reciprocal-decay.md) (machine-verified in [`ChirpedNonstationaryGradientSeparation.lean`](formalization/BuildingBlocks/ChirpedNonstationaryGradientSeparation.lean)) proves that for all zeros whose ordinate $\gamma$ lies outside the resonant band $[T(1-2\eta w), T]$ by distance at least $\Delta > 0$, the phase gradient $|\Phi'_\gamma(x)| \ge \Delta$ uniformly across $[0, w]$, establishing reciprocal quadratic decay $(\Phi'(x))^{-2} \le \Delta^{-2}$ and controlling non-resonant critical-line tails. Furthermore, the [chirped carrier dilution-density balance theorem](unique-contributions/chirped-carrier-dilution-density-balance-and-critical-logarithmic-bound.md) (machine-verified in [`ChirpedCarrierDilutionDensityBalance.lean`](formalization/BuildingBlocks/ChirpedCarrierDilutionDensityBalance.lean)) proves that the carrier frequency $T$ and chirp rate $\eta$ algebraically cancel from the product of the resonant zero count $O(T \log T)$ and the stationary phase energy dispersion $O(1/T)$, establishing the critical-line resonant bound $C_{\text{crit, res}} \log T$ with $C_{\text{crit, res}} = 2 C_N C_{\text{core}} w$ and the total critical bound $C_{\text{crit, tot}} \log T$. In addition, the [chirped dyadic tail summability theorem](first-formalizations/chirped-dyadic-tail-summability-and-reciprocal-gradient-decay.md) (machine-verified in [`ChirpedDyadicTailSummability.lean`](formalization/BuildingBlocks/ChirpedDyadicTailSummability.lean)) proves geometric convergence of the reciprocal quadratic gradient tail across dyadic annuli, establishing the universal logarithmic bound $C_{\text{tail}} \log T$ ($C_{\text{tail}} = 4 C_{\text{dens}} / \Delta_0$) on the non-resonant critical-line tail. Furthermore, the [chirped Archimedean digamma bound theorem](unique-contributions/chirped-archimedean-digamma-bound-and-continuous-background.md) (machine-verified in [`ChirpedArchimedeanDigammaBound.lean`](formalization/BuildingBlocks/ChirpedArchimedeanDigammaBound.lean)) proves the uniform logarithmic deviation bound $|Q_{\text{arch}}(T) - \log T| \le C_{\text{cont}}$ with $C_{\text{cont}} = \log 2 + \eta w + C_\Gamma$, establishing uniform control over the continuous background. In addition, the [chirped spectral assembly theorem](unique-contributions/chirped-spectral-assembly-and-master-upper-bound.md) (machine-verified in [`ChirpedSpectralAssembly.lean`](formalization/BuildingBlocks/ChirpedSpectralAssembly.lean)) synthesizes the critical resonant bound, dyadic non-resonant tail, and continuous background into the unified master spectral upper bound $Q_{\text{spec}} \le C_{\text{crit, assembled}} \log T + 6 M_{\text{cont}} - 2 b T^{2d}$. Furthermore, the [chirped Brun–Titchmarsh short-interval prime mass theorem](first-formalizations/chirped-brun-titchmarsh-short-interval-prime-mass.md) (machine-verified in [`ChirpedBrunTitchmarshMass.lean`](formalization/BuildingBlocks/ChirpedBrunTitchmarshMass.lean)) formalizes the Montgomery–Vaughan arbitrary-interval sieve logarithmic ratio bound $\le 5$ via universal half-power logarithmic concavity $\log y \le 2\sqrt{y}$, bounding the ordinary prime mass by $10h$ and the total short-interval von Mangoldt mass by $12h = C_{\text{sieve}} h$ uniformly across the constellation. In addition, the [chirped Brun–Titchmarsh geometric row bound theorem](unique-contributions/chirped-brun-titchmarsh-geometric-row-bound.md) (machine-verified in [`ChirpedBrunTitchmarshRowBound.lean`](formalization/BuildingBlocks/ChirpedBrunTitchmarshRowBound.lean)) establishes the carrier frequency cancellation $\exp(D_T/2)/T = \exp(-B/2)\log T$ for dilated constellations at span $D_T = 2\log T + 2\log\log T - B$, the short-interval scale ratio identity $(N/T)/\sqrt{N} = \sqrt{N}/T$, and the master row bound $R_{\text{cross}}(T) \le C_{\text{geom}} \exp(-B/2)\log T + C_0$. Furthermore, the [chirped grand synthesis and end-to-end RH reduction theorem](unique-contributions/chirped-grand-synthesis-and-rh-deduction.md) (machine-verified in [`ChirpedGrandSynthesis.lean`](formalization/BuildingBlocks/ChirpedGrandSynthesis.lean)) establishes the universal canonical buffer construction $B := 2\log C_{\text{geom}} + 1$ ensuring $C_{\text{geom}} \exp(-B/2) = \exp(-1/2) < 1$ unconditionally for all $C_{\text{geom}} > 0$, bundles the arithmetic budget and spectral machinery into `GrandSynthesisSystem`, constructs the existential witness `ChirpedZeroCouplingSystem`, and formally deduces `RightHalfZeroFree` and Mathlib's official `RiemannHypothesis`. Finally, the [autocorrelation Fourier positivity theorem](first-formalizations/autocorrelation-fourier-positivity-and-critical-spectral-energy.md) (machine-verified in [`AutocorrelationFourierPositivity.lean`](formalization/BuildingBlocks/AutocorrelationFourierPositivity.lean)) formalizes the classical Wiener–Khinchin / Bochner theorem in Lean 4, proving that wavepacket autocorrelations evaluate to the power spectral density $|\hat{u}(\xi)|^2 \ge 0$, rigorously certifying that all critical-line zeros contribute non-negatively to Weil's explicit formula and strictly isolating all negative spectral energy to off-line zeros.

**Remaining frontier challenge.** With off-line zeros proved to produce uncompensated negative power drifts that are asymptotically incompatible with positive arithmetic Weil margins on both fixed-bump and chirped-packet constellations, micro-local carrier tuning guaranteeing non-vanishing stationary phase coupling at any target zero ordinate $\gamma_0$, Fresnel-Gaussian localization proving quadratic phase collapse with zero remainder, Fresnel phase coherence guaranteeing non-vanishing positive projection $\cos(\Delta \Phi) \ge 1/2$, geometric span dilation $\lambda = (1+\mu)/(2d)$ amplifying the coherent coupling to power divergence $T^\mu$, the arithmetic lower bound certified from the archimedean-prime budget, non-stationary gradient separation bounding non-resonant spectral tails, carrier dilution-density balance bounding resonant critical zeros by $O(\log T)$, dyadic tail summability bounding the non-resonant critical tail by $O(\log T)$, the chirped Archimedean digamma bound bounding continuous background deviation by $O(1)$, the chirped spectral assembly establishing the master upper bound $C_{\text{crit}} \log T + 6M - 2b T^{2d}$, the Montgomery–Vaughan Brun–Titchmarsh short-interval mass bound providing uniform $O(h)$ von Mangoldt mass control, the chirped Brun–Titchmarsh geometric row bound proving $R_{\text{cross}}(T) \le C_{\text{geom}} e^{-B/2} \log T + C_0$, the canonical buffer guaranteeing universal geometric decay $C_{\text{geom}} e^{-B/2} = e^{-1/2} < 1$, the grand synthesis pipeline constructively deducing RH from any grand synthesis system, autocorrelation Fourier positivity certifying that critical-line zeros can never generate negative energy, and the chirped zero partition synthesis proving that low-frequency computational certification ($|\gamma| \le 3 \cdot 10^{12}$) combined with high-frequency spectral-arithmetic grand synthesis unconditionally deduces `RightHalfZeroFree` and Mathlib's `RiemannHypothesis` (machine-verified in [`ChirpedZeroPartitionSynthesis.lean`](formalization/BuildingBlocks/ChirpedZeroPartitionSynthesis.lean)), the remaining bridge to unconditional RH is establishing uniform spectral-ordinate localization across all frequencies $T$ simultaneously, or closing the Mellin-potential eventual sign condition $\forall^\infty x, \operatorname{Re}(W(x)) \le 0$ established in [`ActualCriticalSignCriterion.lean`](formalization/BuildingBlocks/ActualCriticalSignCriterion.lean). None of these estimates yet proves unconditional RH.

Analytic estimates in the linked notes are written proofs unless their text names a Lean module. Lean verification of finite identities does not certify the analytic estimates that use them.
