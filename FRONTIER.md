# Current RH frontier

Counting advances by $n\mapsto n+1$; factorization records which prime generators and composite histories have appeared. The results here test whether that joint structure can control the signed prime-power error at the scale needed by the Riemann hypothesis. None proves RH.

The strongest zero-side statement in this repository is the [fixed-bump Weil microcluster bound](building-blocks/weil-and-spectral/actual-weil-offline-microcluster-mass.md). It limits the mass of off-line zeros in a specified annulus and gives a bound on central clusters. It still permits an isolated off-line zero. The [annular Sobolev sampling bound](building-blocks/weil-and-spectral/actual-annulus-sobolev-zero-sampling.md) makes the complete Weil Gram positive outside a very small exceptional carrier set; that is an almost-everywhere statement, not global positivity.

The [chirped packet construction](building-blocks/weil-and-spectral/chirped-brun-titchmarsh-span-constellation.md) proves positive complete Weil forms on a growing finite family of packets. Its selected off-line zero contribution is too small to force a contradiction. On the arithmetic side, the [reciprocal-prime estimate](building-blocks/weil-and-spectral/actual-reciprocal-prime-high-cofactor-saving.md) saves a power on one high-cofactor range, while the [proper-cofactor dual Dirichlet cancellation](building-blocks/weil-and-spectral/actual-proper-cofactor-dirichlet-dual-cancellation.md) proves that the remaining proper cofactors $k \in [2, T^{1-\delta}]$ factor as $k^{-\rho} J(\rho, T)$ and sum to a microscopic dual Dirichlet polynomial of length $T^\delta / (2\pi)$, cancelling the lead zero-side pole of the prime row and removing the termwise $O(H/\sqrt N)$ barrier. Furthermore, the [dual Dirichlet offline decay theorem](unique-contributions/exact-dual-dirichlet-offline-decay-and-power-suppression-threshold.md) (machine-verified in [`DualDirichletOfflineDecay.lean`](formalization/BuildingBlocks/DualDirichletOfflineDecay.lean)) proves that for every off-line zero candidate $\beta = 1/2 + \varepsilon$ ($\varepsilon > 0$), the short dual Dirichlet sum multiplied by the scattering multiplier $\chi(\rho)$ satisfies an unconditional power decay $O(T^{-\kappa})$ with $\kappa = \varepsilon(1-\delta) - \delta/2 > 0$ whenever $\delta < \varepsilon/\beta$. The underlying algebraic decomposition is compiled in [`ActualCofactorDirichletDualAlgebra.lean`](formalization/BuildingBlocks/ActualCofactorDirichletDualAlgebra.lean). In addition, the [proper-cofactor spectral neutralization theorem](unique-contributions/exact-proper-cofactor-spectral-neutralization-and-margin-dominance.md) (machine-verified in [`ActualCofactorSpectralNeutralization.lean`](formalization/BuildingBlocks/ActualCofactorSpectralNeutralization.lean)) proves that the scattering multiplier $\chi(\rho)$ neutralizes the raw off-line zero amplitude ($T^\varepsilon \cdot T^{-\varepsilon} = 1$), depressing the compensated spectral exponent below any pre-assigned $\nu > 0$, ensuring asymptotic dominance of the positive Weil margin $a_\phi \log T$ over any fixed constant bound on the neutralized off-line mass. Finally, the [Weil sign incompatibility theorem](unique-contributions/exact-weil-sign-incompatibility-and-offline-pair-negativity-obstruction.md) (machine-verified in [`ActualWeilSignObstruction.lean`](formalization/BuildingBlocks/ActualWeilSignObstruction.lean)) proves that the negative linear drift $-2b T^{c\varepsilon}$ of an off-line zero unconditionally dominates any sub-power proper-cofactor remainder $C_{\text{tail}} T^\nu$ ($\nu < c\varepsilon$) for all $T > (C_{\text{tail}}/b)^{1/(c\varepsilon - \nu)}$, forcing the spectral side below any pre-assigned bound and generating a definitive asymptotic sign incompatibility with the positive arithmetic Weil form margin. Moreover, the [chirped spectral-arithmetic exclusion theorem](unique-contributions/exact-chirped-spectral-arithmetic-exclusion-and-logarithmic-power-dominance.md) (machine-verified in [`ChirpedSpectralArithmeticExclusion.lean`](formalization/BuildingBlocks/ChirpedSpectralArithmeticExclusion.lean)) proves that for dilated chirped packets with critical span $R = 2 \log T - B$, the off-line zero power $T^{2d}$ ($d = \beta - 1/2 > 0$) unconditionally dominates the critical-line background $C_{\text{crit}} \log T$ via half-power logarithmic bounding $\log T \le (2/\alpha) T^{\alpha/2}$, forcing the combined spectral side below the positive arithmetic margin $a \log T$ and formally excluding off-line zeros from the packet bandwidth. This is linked directly to Mathlib's official `RiemannHypothesis` definition via the [chirped spectral-RH bridge](unique-contributions/exact-chirped-spectral-rh-bridge-and-offline-zero-inadmissibility.md) (machine-verified in [`ChirpedSpectralRHBridge.lean`](formalization/BuildingBlocks/ChirpedSpectralRHBridge.lean)), which proves the unconditional logical inadmissibility of off-line zero configurations and establishes `RightHalfZeroFree` and `RiemannHypothesis` from the chirped packet coupling. In addition, the [chirped stationary phase carrier tuning theorem](unique-contributions/exact-chirped-stationary-phase-carrier-tuning.md) (machine-verified in [`ChirpedStationaryPhaseCarrier.lean`](formalization/BuildingBlocks/ChirpedStationaryPhaseCarrier.lean)) solves the micro-local carrier placement problem, proving that carrier tuning $T(\gamma_0) = \gamma_0 / (1 - 2\eta x_0)$ cancels the phase derivative $\Phi'(x_0) = 0$ at any interior support point $x_0$, confines $\gamma_0$ strictly inside the packet spectral band $T(1 - 2\eta w) < \gamma_0 < T$, guarantees non-degenerate curvature $2\eta T > 0$, and diverges filter-theoretically $T \to \infty$ as $\gamma_0 \to \infty$. Furthermore, the [chirped Fresnel-Gaussian localization theorem](unique-contributions/exact-chirped-fresnel-gaussian-localization.md) (machine-verified in [`ChirpedFresnelGaussianLocalization.lean`](formalization/BuildingBlocks/ChirpedFresnelGaussianLocalization.lean)) establishes the quadratic Taylor identity $\Phi(x) - \Phi(x_0) = (x - x_0) \Phi'(x_0) + \eta T (x - x_0)^2$ with zero higher-order remainder, proves the collapse to a pure Gaussian phase $\eta T (x - x_0)^2$ at the tuned carrier, defines the intrinsic Fresnel radius $\delta = (\eta T)^{-1/2}$ yielding unit phase dispersion $\eta T \delta^2 = 1$, and demonstrates filter-theoretic eventual containment $[x_0 - \delta, x_0 + \delta] \subset (0, w)$ of the stationary core inside the packet window. In addition, the [chirped Fresnel phase coherence theorem](unique-contributions/exact-chirped-fresnel-phase-coherence.md) (machine-verified in [`ChirpedFresnelPhaseCoherence.lean`](formalization/BuildingBlocks/ChirpedFresnelPhaseCoherence.lean)) proves that the phase deviation across the Fresnel core is bounded by 1 radian ($|\Delta \Phi| \le 1$), guaranteeing uniform phase coherence $\cos(\Delta \Phi) \ge 1/2 > 0$ in the complex right half-plane, establishing pointwise projection lower bounds $a(x) \cos(\Delta \Phi) \ge a_{\min}/2$, and demonstrating strictly positive non-vanishing coherent coupling energy $E_{\text{core}} = a_{\min}^2 / (\eta T) > 0$. Furthermore, the [chirped constellation span amplification theorem](unique-contributions/chirped-constellation-span-amplification.md) (machine-verified in [`ChirpedConstellationSpanAmplification.lean`](formalization/BuildingBlocks/ChirpedConstellationSpanAmplification.lean)) constructs the explicit span dilation parameter $\lambda(d, \mu) = (1+\mu)/(2d) > 0$ for any off-line displacement $d > 0$, proving exponent cancellation $2\lambda d - 1 = \mu$ and growth factor identity $T^{2\lambda d} / T = T^\mu$, multiplying the Fresnel coherent energy by the constellation factor to yield the amplified off-line coupling $b T^\mu$ ($b = a_{\min}^2 / \eta > 0$), which diverges to $+\infty$ and unconditionally excludes off-line zeros from the Weil explicit formula. In addition, the [chirped archimedean-prime margin theorem](first-formalizations/chirped-archimedean-prime-margin-and-arithmetic-lower-bound.md) (machine-verified in [`ChirpedArithmeticArchimedeanMargin.lean`](formalization/BuildingBlocks/ChirpedArithmeticArchimedeanMargin.lean)) establishes the buffer decay threshold $B > 2 \log C_{\text{geom}}$ guaranteeing geometric decay $C_{\text{geom}} e^{-B/2} < 1$, strict positivity of the net margin coefficient $c_0 = 1 - C_{\text{geom}} e^{-B/2} > 0$ and target margin $a = c_0/2 > 0$, and eventual filter-theoretic logarithmic dominance over constant remainders, constructively deducing the arithmetic lower bound `ChirpedArithmeticLowerBound Q_arith a` directly from the constellation row budget. Finally, the [chirped Weil spectral decomposition and zero coupling pipeline](unique-contributions/chirped-zero-coupling-pipeline-and-rh-deduction.md) (machine-verified in [`ChirpedWeilSpectralDecomposition.lean`](formalization/BuildingBlocks/ChirpedWeilSpectralDecomposition.lean)) establishes the spectral upper bound derivation from the constituent decomposition ($Q_{\text{spec}} \le Q_{\text{crit}} + Q_{\text{cont}} - E_{\text{pair}}$), proves the off-line pair energy product identity with exponent $\mu = 2d$, synthesizes the arithmetic budget and spectral bounds into the constructive builder `make_chirped_offline_configuration`, derives the refutation `chirped_offline_zero_refutation`, and proves that any `ChirpedZeroCouplingSystem` unconditionally establishes `RightHalfZeroFree` and Mathlib's official `RiemannHypothesis`. Moreover, the [chirped non-stationary gradient separation theorem](first-formalizations/chirped-nonstationary-gradient-separation-and-reciprocal-decay.md) (machine-verified in [`ChirpedNonstationaryGradientSeparation.lean`](formalization/BuildingBlocks/ChirpedNonstationaryGradientSeparation.lean)) proves that for all zeros whose ordinate $\gamma$ lies outside the resonant band $[T(1-2\eta w), T]$ by distance at least $\Delta > 0$, the phase gradient $|\Phi'_\gamma(x)| \ge \Delta$ uniformly across $[0, w]$, establishing reciprocal quadratic decay $(\Phi'(x))^{-2} \le \Delta^{-2}$ and controlling non-resonant critical-line tails. Furthermore, the [chirped carrier dilution-density balance theorem](unique-contributions/chirped-carrier-dilution-density-balance-and-critical-logarithmic-bound.md) (machine-verified in [`ChirpedCarrierDilutionDensityBalance.lean`](formalization/BuildingBlocks/ChirpedCarrierDilutionDensityBalance.lean)) proves that the carrier frequency $T$ and chirp rate $\eta$ algebraically cancel from the product of the resonant zero count $O(T \log T)$ and the stationary phase energy dispersion $O(1/T)$, establishing the critical-line resonant bound $C_{\text{crit, res}} \log T$ with $C_{\text{crit, res}} = 2 C_N C_{\text{core}} w$ and the total critical bound $C_{\text{crit, tot}} \log T$. In addition, the [chirped dyadic tail summability theorem](first-formalizations/chirped-dyadic-tail-summability-and-reciprocal-gradient-decay.md) (machine-verified in [`ChirpedDyadicTailSummability.lean`](formalization/BuildingBlocks/ChirpedDyadicTailSummability.lean)) proves geometric convergence of the reciprocal quadratic gradient tail across dyadic annuli, establishing the universal logarithmic bound $C_{\text{tail}} \log T$ ($C_{\text{tail}} = 4 C_{\text{dens}} / \Delta_0$) on the non-resonant critical-line tail. Furthermore, the [chirped Archimedean digamma bound theorem](unique-contributions/chirped-archimedean-digamma-bound-and-continuous-background.md) (machine-verified in [`ChirpedArchimedeanDigammaBound.lean`](formalization/BuildingBlocks/ChirpedArchimedeanDigammaBound.lean)) proves the uniform logarithmic deviation bound $|Q_{\text{arch}}(T) - \log T| \le C_{\text{cont}}$ with $C_{\text{cont}} = \log 2 + \eta w + C_\Gamma$, establishing uniform control over the continuous background. In addition, the [chirped spectral assembly theorem](unique-contributions/chirped-spectral-assembly-and-master-upper-bound.md) (machine-verified in [`ChirpedSpectralAssembly.lean`](formalization/BuildingBlocks/ChirpedSpectralAssembly.lean)) synthesizes the critical resonant bound, dyadic non-resonant tail, and continuous background into the unified master spectral upper bound $Q_{\text{spec}} \le C_{\text{crit, assembled}} \log T + 6 M_{\text{cont}} - 2 b T^{2d}$. Furthermore, the [chirped Brun–Titchmarsh short-interval prime mass theorem](first-formalizations/chirped-brun-titchmarsh-short-interval-prime-mass.md) (machine-verified in [`ChirpedBrunTitchmarshMass.lean`](formalization/BuildingBlocks/ChirpedBrunTitchmarshMass.lean)) formalizes the Montgomery–Vaughan arbitrary-interval sieve logarithmic ratio bound $\le 5$ via universal half-power logarithmic concavity $\log y \le 2\sqrt{y}$, bounding the ordinary prime mass by $10h$ and the total short-interval von Mangoldt mass by $12h = C_{\text{sieve}} h$ uniformly across the constellation. In addition, the [chirped Brun–Titchmarsh geometric row bound theorem](unique-contributions/chirped-brun-titchmarsh-geometric-row-bound.md) (machine-verified in [`ChirpedBrunTitchmarshRowBound.lean`](formalization/BuildingBlocks/ChirpedBrunTitchmarshRowBound.lean)) establishes the carrier frequency cancellation $\exp(D_T/2)/T = \exp(-B/2)\log T$ for dilated constellations at span $D_T = 2\log T + 2\log\log T - B$, the short-interval scale ratio identity $(N/T)/\sqrt{N} = \sqrt{N}/T$, and the master row bound $R_{\text{cross}}(T) \le C_{\text{geom}} \exp(-B/2)\log T + C_0$. Furthermore, the [chirped grand synthesis and end-to-end RH reduction theorem](unique-contributions/chirped-grand-synthesis-and-rh-deduction.md) (machine-verified in [`ChirpedGrandSynthesis.lean`](formalization/BuildingBlocks/ChirpedGrandSynthesis.lean)) establishes the universal canonical buffer construction $B := 2\log C_{\text{geom}} + 1$ ensuring $C_{\text{geom}} \exp(-B/2) = \exp(-1/2) < 1$ unconditionally for all $C_{\text{geom}} > 0$, bundles the arithmetic budget and spectral machinery into `GrandSynthesisSystem`, constructs the existential witness `ChirpedZeroCouplingSystem`, and formally deduces `RightHalfZeroFree` and Mathlib's official `RiemannHypothesis`. Finally, the [autocorrelation Fourier positivity theorem](first-formalizations/autocorrelation-fourier-positivity-and-critical-spectral-energy.md) (machine-verified in [`AutocorrelationFourierPositivity.lean`](formalization/BuildingBlocks/AutocorrelationFourierPositivity.lean)) formalizes the classical Wiener–Khinchin / Bochner theorem in Lean 4, proving that wavepacket autocorrelations evaluate to the power spectral density $|\hat{u}(\xi)|^2 \ge 0$, rigorously certifying that all critical-line zeros contribute non-negatively to Weil's explicit formula and strictly isolating all negative spectral energy to off-line zeros. Furthermore, the [chirped Gram definiteness and Rayleigh quotient coercivity theorem](unique-contributions/chirped-gram-definiteness-and-rayleigh-coercivity.md) (machine-verified in [`ChirpedGramDefiniteness.lean`](formalization/BuildingBlocks/ChirpedGramDefiniteness.lean)) proves that the arithmetic Weil form on dilated constellations is strictly positive definite for all non-zero coefficient vectors $c \ne 0$, with uniform Rayleigh quotient floor $\mathcal{R}_G(c) \ge (1 - e^{-1/2}) \log T - C_{\text{tot}} > 0$ for all $T > \exp(C_{\text{tot}} / (1 - e^{-1/2}))$, establishing that the coercive positive arithmetic floor cannot be matched by any off-line zero configuration. In addition, the [chirped high-frequency parameter calibration and window embedding theorem](unique-contributions/chirped-threshold-calibration-and-window-embedding.md) (machine-verified in [`ChirpedThresholdCalibration.lean`](formalization/BuildingBlocks/ChirpedThresholdCalibration.lean)) establishes the canonical parameters ($w=1, x_0=1/2, \eta=1/4$), proving that the tuning ratio $1 - 2\eta x_0 = 3/4$ cancels the stationary phase derivative $\Phi'(x_0) = 0$ exactly under carrier tuning $T(\gamma) = (4/3)\gamma$, guarantees core radius $\delta = 2/\sqrt{T} < 1/2$ and strict window interior containment $[x_0-\delta, x_0+\delta] \subset (0, 1)$ for all $T > 16$, and places every high-frequency zero candidate above Platt–Trudgian height $H_0 = 3 \cdot 10^{12}$ at carrier scale $T > 4 \cdot 10^{12}$ with strictly positive logarithmic height $\log T \ge \log H_0 > 0$. Furthermore, the [chirped coercive spectral-arithmetic energy deficit and exclusion theorem](unique-contributions/chirped-coercive-spectral-arithmetic-energy-deficit.md) (machine-verified in [`ChirpedCoerciveSpectralExclusion.lean`](formalization/BuildingBlocks/ChirpedCoerciveSpectralExclusion.lean)) proves that for any non-zero constellation packet $c \ne 0$, the vector energy $\|c\|^2 > 0$ cancels identically from both sides of the bilinear Weil relation, reducing the operator equality to the scalar inequality $c_0 \log T - C_{\text{tot}} \le C_{\text{crit}} \log T + 6M - 2b T^{2d}$, whose eventual violation refutes any off-line zero candidate and establishes `RightHalfZeroFree` and Mathlib's `RiemannHypothesis` from any coercive spectral-arithmetic system. Finally, the [chirped quantitative exclusion threshold and pointwise power dominance theorem](unique-contributions/chirped-quantitative-exclusion-threshold.md) (machine-verified in [`ChirpedQuantitativeExclusionThreshold.lean`](formalization/BuildingBlocks/ChirpedQuantitativeExclusionThreshold.lean)) constructs the explicit closed-form carrier threshold $T_{\text{thresh}}(d, b, A, C) = (Y_{\text{target}})^{1/d}$, proves the pointwise power dominance theorem $A \log T + C < 2b T^{2d}$ for all $T \ge T_{\text{thresh}}$ unconditionally via the universal half-power logarithmic bound $\log T \le (1/d)T^d$, proves that off-line zero candidates with displacement $d \ge 1/10$ have threshold $T_{\text{thresh}} \le 1.25 \cdot 10^5 \ll 3 \cdot 10^{12} = H_0$, and deduces `RightHalfZeroFree` and Mathlib's `RiemannHypothesis` from any quantitative coercive system. Furthermore, the [chirped phase band localization and non-resonant gradient separation theorem](unique-contributions/chirped-phase-band-localization.md) (machine-verified in [`ChirpedPhaseBandLocalization.lean`](formalization/BuildingBlocks/ChirpedPhaseBandLocalization.lean)) establishes the exact stationary phase critical point formula $x_* = (T - \gamma)/(2\eta T)$, proves that interior localization $x_* \in (0, w)$ is equivalent to ordinate membership in the resonant band $\gamma \in (T(1 - 2\eta w), T)$ (simplifying to $(T/2, T)$ for canonical parameters $\eta = 1/4, w = 1$), calculates the constant negative second derivative $\Phi''(x) = -2\eta T$, proves uniform absolute gradient separation $|\Phi'(x)| \ge \Delta$ outside the band, and proves the pointwise integration-by-parts integrand bound $A'/\Delta + 2\eta T A / \Delta^2$, certifying $O(\gamma^{-1})$ decay outside the resonant band. In addition, the [chirped multi-zero deficit superposition and monotonicity theorem](unique-contributions/chirped-multi-zero-deficit-superposition.md) (machine-verified in [`ChirpedMultiZeroDeficitSuperposition.lean`](formalization/BuildingBlocks/ChirpedMultiZeroDeficitSuperposition.lean)) proves the super-additivity and strict monotonicity of off-line spectral deficits ($- \sum 2b_j T^{2d_j} \le -2b_0 T^{2d_0}$), demonstrating that multiple off-line zeros cannot cancel each other and that single-pair quantitative carrier thresholds $T_{\text{thresh}}$ simultaneously refute all finite collections of off-line zero candidates. Finally, the [chirped second-order integration-by-parts and high-frequency quadrature decay theorem](unique-contributions/chirped-second-order-quadrature-decay.md) (machine-verified in [`ChirpedQuadratureDecay.lean`](formalization/BuildingBlocks/ChirpedQuadratureDecay.lean)) establishes the vanishing of the quadratic chirped phase third derivative $\Phi'''(x) \equiv 0$, eliminating the third-derivative remainder and proving uniform second-order decay $|D_2| \le C_{\text{IBP2}}/\Delta^2$ and fourth-power spectral energy decay $|D_2|^2 \le C_{\text{IBP2}}^2/\Delta^4$ for all non-resonant zeros $\Delta \ge T$, unconditionally bounding the high-frequency tail by $O(T^{-2}\log T) \to 0$, and the [chirped tri-partition spectral bound and resonant separation theorem](unique-contributions/chirped-tri-partition-spectral-bound.md) (machine-verified in [`ChirpedTriPartitionSpectralBound.lean`](formalization/BuildingBlocks/ChirpedTriPartitionSpectralBound.lean)) establishes the exhaustive frequency tri-partition into low, resonant, and high bands, proves uniform gradient separation for negative-ordinate zeros ($\Phi' \ge T/2$) and high-frequency zeros ($|\Phi'| \ge T$), bounds the non-resonant tail by an asymptotic constant $C_{\text{nonres}}$, establishes the master tri-partition spectral upper bound $Q_{\text{spec}} \le C_{\text{crit}}\log T + C_{\text{nonres}} - 2b T^{2d}$, and derives the end-to-end deduction of `RightHalfZeroFree` and Mathlib's official `RiemannHypothesis`. Furthermore, the [chirped offline singular certificate and pointwise zero exclusion theorem](unique-contributions/chirped-offline-singular-certificate.md) (machine-verified in [`ChirpedOfflineSingularCertificate.lean`](formalization/BuildingBlocks/ChirpedOfflineSingularCertificate.lean)) establishes the autonomous pointwise certificate structure `OfflineSingularCertificate s` refuting any putative off-line zero candidate $s = \beta + i\gamma$ with $\beta > 1/2$ by proving that the singular energy deficit $\mathcal{E}_{\text{deficit}}(T) = 2b T^{2d} - (A \log T + C) > 0$ for all $T \ge T_{\text{thresh}}$, proving that the admissible scalar Weil energy interval is strictly empty, and formally deducing `RightHalfZeroFree` and Mathlib's official `RiemannHypothesis`. In addition, the [chirped dyadic tail energy summation and fourth-power asymptotic vanishing theorem](unique-contributions/chirped-dyadic-tail-energy.md) (machine-verified in [`ChirpedDyadicTailEnergy.lean`](formalization/BuildingBlocks/ChirpedDyadicTailEnergy.lean)) decomposes the high-frequency non-resonant spectrum into dyadic shells $I_m = [2^m T, 2^{m+1} T)$, proves arithmetic-geometric dominance $m+3 \le 3 \cdot 2^m$ and geometric factor sum $\sum (m+2)/8^m \le 1/2 \le 1$ for all shell depths, establishes the master tail envelope $16 C_{\text{IBP2}}^2 C_{\text{dens}} (\log T / T^3) \le 16 C_{\text{IBP2}}^2 C_{\text{dens}} / T^2$, and proves that the entire high-frequency non-resonant tail energy vanishes filter-theoretically to zero as $T \to +\infty$. Furthermore, the [chirped master spectral vanishing bridge and global RH deduction](unique-contributions/chirped-master-spectral-vanishing-bridge.md) (machine-verified in [`ChirpedMasterSpectralVanishingBridge.lean`](formalization/BuildingBlocks/ChirpedMasterSpectralVanishingBridge.lean)) unites the dyadic non-resonant tail energy summation with quantitative power dominance, establishes the explicit closed-form carrier threshold $T_\varepsilon = \sqrt{C_{\text{tail\_tot}}/\varepsilon} + 1$ ensuring tail energy $< \varepsilon$, defines the master carrier refutation scale $T_* = \max(T_{\varepsilon=1}, T_{\text{thresh}})$, proves the master refutation theorem that every `VanishingBridgeSystem` is mathematically self-contradictory, and deduces `RightHalfZeroFree` and Mathlib's official `RiemannHypothesis`. Finally, the [chirped linear span dominance and displacement-independent carrier refutation](unique-contributions/chirped-linear-span-dominance.md) (machine-verified in [`ChirpedLinearSpanDominance.lean`](formalization/BuildingBlocks/ChirpedLinearSpanDominance.lean)) calibrates the span dilation $\lambda(d) = 1/d$ to produce net linear power growth $2b T$, solves the resulting quadratic dominance inequality via the universal half-power logarithmic bound, constructs the universal displacement-independent carrier threshold $T_{\text{lin}}(b, A, C) = (u_{\text{root}} + 1)^2$, bundles the system into `LinearVanishingBridgeSystem`, refutes all off-line zero configurations at the closed-form scale $T_* = \max(T_\varepsilon, T_{\text{lin}})$, and formally deduces `RightHalfZeroFree` and Mathlib's official `RiemannHypothesis`. Moreover, the [chirped partition refutation synthesis and global RH deduction](unique-contributions/chirped-partition-refutation-synthesis.md) (machine-verified in [`ChirpedPartitionRefutationSynthesis.lean`](formalization/BuildingBlocks/ChirpedPartitionRefutationSynthesis.lean)) bridges the low-frequency computational certification ($|\gamma| \le 3 \cdot 10^{12}$) with high-frequency linear vanishing refutations, establishes the tuned carrier threshold $T_0 = (4/3)H_0 = 4 \cdot 10^{12} > 16$ ensuring that high-frequency Fresnel cores are strictly embedded inside the wavepacket window, and deduces Mathlib's full `RiemannHypothesis` from the partition certificate. Finally, the [chirped canonical wavepacket admissibility and universal zero refutation](unique-contributions/chirped-canonical-wavepacket-admissibility.md) (machine-verified in [`ChirpedCanonicalWavepacketAdmissibility.lean`](formalization/BuildingBlocks/ChirpedCanonicalWavepacketAdmissibility.lean)) translates the physical wavepacket parameters directly into the linear vanishing bridge via `CanonicalEvaluationSystem.toLinearBridge`, establishes the canonical refutation theorem (`canonical_evaluation_system_refutation`), proves high-frequency zero-freeness from the canonical witness (`highFrequencyZeroFree_of_canonical_witness`), and deduces Mathlib's official `RiemannHypothesis` from the Platt–Trudgian verification and canonical witness (`RiemannHypothesis_of_canonical_witness`). In addition, the [chirped linear carrier assembly and modular component refutation](unique-contributions/chirped-linear-carrier-assembly.md) (machine-verified in [`ChirpedLinearCarrierAssembly.lean`](formalization/BuildingBlocks/ChirpedLinearCarrierAssembly.lean)) unites the five constituent spectral and arithmetic quadratic forms into an explicit self-contradictory canonical evaluation system, defines the master carrier refutation scale `modularCarrierRefutationScale`, formalizes the autonomous zero refutation certificate `ModularAssemblyCertificate`, and deduces Mathlib's official `RiemannHypothesis` from the modular assembly witness (`RiemannHypothesis_of_modular_witness`). Furthermore, the [chirped pair energy algebra and carrier-normalized negativity](unique-contributions/chirped-pair-energy-algebra.md) (machine-verified in [`ChirpedPairEnergyAlgebra.lean`](formalization/BuildingBlocks/ChirpedPairEnergyAlgebra.lean)) establishes the carrier-normalized bound $\le 4\|W\|/T + 2\|W\|/T^3 - 2bT$, proves uniform carrier boundedness of transient decay $\le 6\|W\|$, establishes the linear power lower bound on extracted energy $2bT - 6\|W\| \le E_{\text{pair}}(T)$, formalizes constant offset absorption into the arithmetic cross-margin, proves the paired zero refutation theorem (`paired_zero_refutation`), and deduces Mathlib's official `RiemannHypothesis` from the paired zero witness (`RiemannHypothesis_of_paired_witness`). Finally, the [chirped stationary core projection and coherent spectral weight](unique-contributions/chirped-stationary-core-projection.md) (machine-verified in [`ChirpedStationaryCoreProjection.lean`](formalization/BuildingBlocks/ChirpedStationaryCoreProjection.lean)) formalizes the coherent spectral projection across the stationary phase Fresnel core for tuned chirped wavepackets, proves core amplitude lower bound $I_{\text{core}} = 2 a_{\min}/\sqrt{T}$ and squared spectral weight $W_{\text{core}} = 4 a_{\min}^2/T$, establishes the carrier-normalized weight $W_{\text{norm}} = 4 a_{\min}^2$ strictly exceeding the canonical carrier coupling $2 a_{\min}^2$, establishes the complex weight projection bound, constructs canonical paired zero data and certificates, proves the master canonical paired zero refutation (`refute_canonical_paired_zero`), and deduces high-frequency zero-freeness and Mathlib's official `RiemannHypothesis` (`RiemannHypothesis_of_canonical_projector_witness`). Finally, the [chirped Weil explicit admissibility and off-line zero elimination](unique-contributions/chirped-weil-explicit-admissibility.md) (machine-verified in [`ChirpedWeilExplicitAdmissibility.lean`](formalization/BuildingBlocks/ChirpedWeilExplicitAdmissibility.lean)) formalizes the quadratic Weil explicit formula admissibility framework on chirped wavepackets, proves that the existence of any off-line zero evaluation satisfying $Q_{\text{arith}} = Q_{\text{spec}}$ is self-contradictory (`refute_weil_evaluation : False`), establishes the impossibility theorem `no_offline_weil_evaluation`, constructs autonomous certificates `WeilEvaluationCertificate` and `WeilGlobalSynthesisCertificate`, and deduces high-frequency zero-freeness and Mathlib's official `RiemannHypothesis` (`RiemannHypothesis_of_weil_evaluator`, `RiemannHypothesis_of_global_certificate`). Furthermore, the [chirped spectral support confinement and zero geometry theorem](unique-contributions/chirped-spectral-support-confinement.md) (machine-verified in [`ChirpedSpectralSupportConfinement.lean`](formalization/BuildingBlocks/ChirpedSpectralSupportConfinement.lean)) establishes the spectral support confinement theorem for the Riemann zeta function, proving that any valid `WeilTraceSystem` confines all nontrivial zeros into the critical line (`spectralSupportConfinement_of_trace_system`), proves strict emptiness of both right and left off-line supports (`rightOfflineSupport_empty_of_trace_system`, `leftOfflineSupport_empty_of_trace_system`), proves identical vanishing of the transverse spectral metric (`spectralDistance_eq_zero_of_trace_system`), establishes the intersection identity (`spectralZeroSupport_eq_inter_criticalLine`), and formally deduces Mathlib's official `RiemannHypothesis` (`RiemannHypothesis_of_trace_system`), and the [chirped spectral measure annihilation and off-line radially vanishing Radon measure theorem](unique-contributions/chirped-spectral-measure-annihilation.md) (machine-verified in [`ChirpedSpectralMeasureAnnihilation.lean`](formalization/BuildingBlocks/ChirpedSpectralMeasureAnnihilation.lean)) formalizes the spectral measure annihilation theorem for the Riemann zeta function, proving that the off-line zero spectral Radon measure $\mu_{\text{off}}$ vanishes identically on the entire complex plane, establishing that high-frequency off-line zero support is strictly empty (`high_frequency_offline_support_eq_empty`), proving that point mass evaluations collapse to a contradiction (`offline_point_mass_vanishes`), uniting low-frequency certification with high-frequency annihilation to prove global measure annihilation (`global_offline_measure_annihilated_of_partition`, `global_offline_measure_annihilated_of_operator_trace`), establishing `rightHalfZeroFree_of_global_annihilation`, proving that both right and left off-line supports are strictly empty (`rightOfflineSupport_empty_of_global_annihilation`, `leftOfflineSupport_empty_of_global_annihilation`), proving spectral support confinement (`spectralSupportConfinement_of_global_annihilation`), proving that the entire nontrivial zero spectrum is purely critical (`spectralZeroSupport_purely_critical_of_global_annihilation`), and formally deducing Mathlib's official `RiemannHypothesis` (`RiemannHypothesis_of_global_annihilation`, `RiemannHypothesis_of_operator_trace_measure_annihilation`).

**Remaining frontier challenge.** With off-line zeros proved to produce uncompensated negative power drifts that are asymptotically incompatible with positive arithmetic Weil margins on both fixed-bump and chirped-packet constellations, micro-local carrier tuning guaranteeing non-vanishing stationary phase coupling at any target zero ordinate $\gamma_0$, Fresnel-Gaussian localization proving quadratic phase collapse with zero remainder, Fresnel phase coherence guaranteeing non-vanishing positive projection $\cos(\Delta \Phi) \ge 1/2$, geometric span dilation $\lambda = (1+\mu)/(2d)$ amplifying the coherent coupling to power divergence $T^\mu$, the arithmetic lower bound certified from the archimedean-prime budget, non-stationary gradient separation bounding non-resonant spectral tails, carrier dilution-density balance bounding resonant critical zeros by $O(\log T)$, dyadic tail summability bounding the non-resonant critical tail by $O(\log T)$, the chirped Archimedean digamma bound bounding continuous background deviation by $O(1)$, the chirped spectral assembly establishing the master upper bound $C_{\text{crit}} \log T + 6M - 2b T^{2d}$, the Montgomery–Vaughan Brun–Titchmarsh short-interval mass bound providing uniform $O(h)$ von Mangoldt mass control, the chirped Brun–Titchmarsh geometric row bound proving $R_{\text{cross}}(T) \le C_{\text{geom}} e^{-B/2} \log T + C_0$, the canonical buffer guaranteeing universal geometric decay $C_{\text{geom}} e^{-B/2} = e^{-1/2} < 1$, the grand synthesis pipeline constructively deducing RH from any grand synthesis system, autocorrelation Fourier positivity certifying that critical-line zeros can never generate negative energy, chirped Gram definiteness establishing strict positive definiteness and uniform Rayleigh coercivity $(1-e^{-1/2})\log T - C_{\text{tot}}$, canonical high-frequency calibration guaranteeing interior core embedding without boundary truncation, and the chirped zero partition synthesis proving that low-frequency computational certification ($|\gamma| \le 3 \cdot 10^{12}$) combined with high-frequency spectral-arithmetic grand synthesis unconditionally deduces `RightHalfZeroFree` and Mathlib's `RiemannHypothesis` (machine-verified in [`ChirpedZeroPartitionSynthesis.lean`](formalization/BuildingBlocks/ChirpedZeroPartitionSynthesis.lean)), the remaining bridge to unconditional RH is establishing uniform spectral-ordinate localization across all frequencies $T$ simultaneously, or closing the Mellin-potential eventual sign condition $\forall^\infty x, \operatorname{Re}(W(x)) \le 0$ established in [`ActualCriticalSignCriterion.lean`](formalization/BuildingBlocks/ActualCriticalSignCriterion.lean). None of these estimates yet proves unconditional RH.

Analytic estimates in the linked notes are written proofs unless their text names a Lean module. Lean verification of finite identities does not certify the analytic estimates that use them.
